
1.
    我发现现在的【新业务流程6.md】中似乎缺少了yolo识别与库存清单的双向握手的步骤
    现在都是yolo看到上面然后再去出来库存清单, 但运行过程中库存清单本身并没有进行检测

所以我打算现在这样：
    进行快照对比之前，先对库存清单进行一次检测处理
具体步骤：
    先对库存清单里面的【遮挡】状态的物品来检查一下他附近是否有其他物品 (这个"附近"不需要这么严谨, 可以宽松一点, 其实这个步骤不执行也无所谓)
    然后对库存清单中【可见】状态的物品与 stable_snapshot 中yolo识别到的物品进行对比, 如果库存清单中显示【可见】但 yolo 识别不到, 则将库存清单中【可见】状态的物品标记为【拿走】
    但是这样是不是不太好？直接就修改状态为【拿走】不太好吧





2.
    现在是使用 baseline_snapshot、contrast_snapshot、current_snapshot、stable_snapshot、operation_context 来进行处理的
    我在想：只有contrast_snapshot、current_snapshot这两个来进行差异的比较会不会太快了？两个快照包含6帧, 够不够用？会不会是应为这个6帧太少了才导致重影？我在想要不要 contrast_1_snapshot、current_2_snapshot、current_snapshot 用三个快照来比较会不会更好, 三个快照包含9帧, 这样或许不会出现重影？
或者：
    原先是3帧一个快照，现在改成5帧一个快照, 这样依旧使用两个快照进行差异比较的话包含10帧, 这样是不是更稳，不会出现重影？
其实：
    无论使用【三个快照来比较】或是【5帧一个快照】, 我的思路都是使用更多的帧来避免重影
    这种方法是不是解决重影的思路？能不能解决重影？




3.
    在 OperationContext 里增加“物品被操作的时间信息”

我现在觉得这个方向比单纯增加快照帧数更有意义。

因为多个物品连续整理时，
系统最终看到的是：
    baseline_snapshot 里有 A1 / A2 / A3
    stable_snapshot 里有 B1 / B2 / B3

但系统并不知道用户真实操作顺序是：
    先整理 A1
    再整理 A2
    最后整理 A3

所以现在 10.2 如果只是按 disappeared_objects 的遍历顺序处理，
就有可能把 A1 错配给 B2，
然后后面的匹配全部跟着乱掉。


可以增加一个轻量的 ItemOperationEvidence：

    item_id
    class_id
    operation_order

    first_touched_frame
    last_touched_frame

    first_moving_frame
    last_moving_frame

    first_occluded_frame
    last_occluded_frame

    last_visible_frame
    last_visible_box

    evidence_score


这些信息都放在 OperationContext 里。
它不是库存状态，
也不是事件结果，
只是这一轮 baseline_snapshot 到 stable_snapshot 之间的操作证据。


怎么判断整理先后顺序：

    1. 当手第一次明显接触某个库存物品时，
       如果这个 item 还没有 operation_order，
       就分配一个新的 operation_order。

    2. 如果没有明确手接触，
       但某个已关联库存物品的 track 开始明显移动，
       也可以分配 operation_order。

    3. 如果既没有明确手接触，也没有明确 track，
       但某个可见库存物品在手附近消失，
       可以作为较弱证据分配 operation_order。

    4. operation_order 一旦分配，就不要因为后续证据变化而重排。
       后续证据只更新 evidence_score / last_xxx_frame。


证据强弱建议：

    强证据：
        手与库存 item bbox 明显重叠
        item 被确认 HELD
        item 对应 track 有明显位移

    中等证据：
        手在 item 附近停留
        item 先被遮挡，随后旧位置消失

    弱证据：
        item 在手附近消失
        同类物品数量变化


如果同一帧接触多个物品，怎么区分先后：

    先后顺序不能用重叠面积来决定。

    真正的先后顺序只能来自时间：
        谁更早第一次被手接触
        谁更早开始移动
        谁更早被遮挡 / HELD
        谁更早在手附近消失

    如果多个物品是在同一帧第一次被接触，
    那么它们在时间上就是并列的。

    这时不要强行排出 1、2、3。
    应该把它们放进同一个 operation_order_group。

    重叠面积、手中心距离、后续移动证据，
    只能作为同组里的辅助证据，
    不能作为“谁先被整理”的主依据。

也就是说：
    operation_order 不一定必须是绝对严格的 1、2、3。
    可以允许几个物品属于同一个操作组。


在 10.2 整理匹配时的使用方式：

    不是直接说“先操作的一定匹配第一个新位置”。

    而是：
        1. 先为所有 A -> B 生成 relocation 候选
        2. 每个候选计算原有匹配分数：
            reid
            bbox 合理性
            尺寸变化
            移动距离
            手 / HELD / track 证据
        3. 再加入时间分数：
            A 是否有 operation_order
            A 的操作时间是否早于其他 A
            B 出现的位置是否符合 A 的移动时间窗口
        4. 最后优先处理 operation_order 更早、总分更高的候选
        5. 每个 A / B 只能被消费一次


这样做的好处：

    1. 多物品连续整理时，处理顺序更接近用户真实动作
    2. 同类物品整理时，不容易被快照遍历顺序误导
    3. 普通拿走也能受益：
        物品消失前刚被手碰过，则【拿走】可信度更高
    4. 普通放入也能受益：
        新物品出现前附近有手进入，则【放入】可信度更高
    5. 重影也能受益：
        如果新框只是在某个已操作同类物品的时间窗口里冒出来，
        就更应该进入 PendingNewObject，而不是马上入库


注意：
    时间信息不能作为唯一判定条件。

    因为手检测、track、YOLO 都可能漏检。
    所以时间信息应该只是辅助证据：
        可以决定候选处理顺序
        可以给 relocation_match 加分
        可以让 PendingNewObject 更谨慎

    但不能只因为“时间上靠近”就强行确认整理。


最小实现版本：

    先不要做复杂全局时间线。

    只在 OperationContext 里增加：
        item_id -> first_touched_frame
        item_id -> last_touched_frame
        item_id -> operation_order
        item_id -> evidence_score

    然后 10.2 处理整理候选时：
        优先处理 operation_order 更早的 A
        同时把 operation_order 作为 relocation_match 的一个加分项

    这样改动相对可控，
    也不会把主业务流程改得太重。




4.
    更严谨的“整理时间线 / 操作顺序”设计

核心原则：
    整理先后顺序只能由时间信息决定。

    不能用：
        bbox 距离
        重叠面积
        reid 分数
        物品在数组里的顺序

    这些都不能代表用户实际先整理了哪个物品。

    它们只能作为匹配分数，
    不能作为 operation_order 的来源。


一、OperationContext 里应该记录什么

可以为每个库存物品维护一个 ItemOperationEvidence：

    item_id
    class_id

    operation_order
    operation_group

    first_touch_frame
    last_touch_frame

    first_move_frame
    last_move_frame

    first_occluded_frame
    last_occluded_frame

    first_missing_near_hand_frame
    last_visible_frame
    last_visible_box

    evidence_score
    evidence_flags


字段解释：

    operation_order：
        这个物品被认为“开始被操作”的顺序。
        值越小，说明越早被操作。

    operation_group：
        如果多个物品在同一帧无法区分先后，
        它们属于同一个 group。

    first_touch_frame：
        手第一次明显接触这个库存物品的帧。

    first_move_frame：
        这个物品或它关联的 track 第一次明显移动的帧。

    first_occluded_frame：
        这个物品第一次被手或其他物体遮挡的帧。

    first_missing_near_hand_frame：
        这个物品第一次在手附近消失的帧。

    evidence_flags：
        记录证据来源，例如：
            touched_by_hand
            moved_track
            held_proxy
            occluded_by_hand
            missing_near_hand


二、怎么给 item 分配 operation_order

每一帧更新 OperationContext 时，
对库存中每个可见 / 遮挡物品检查是否有操作证据。

优先级如下：

    1. 手明确接触 item
        如果 item 还没有 operation_order：
            记录 first_touch_frame = 当前帧
            分配 operation_order

    2. item 关联的 track 明显移动
        如果 item 还没有 operation_order：
            记录 first_move_frame = 当前帧
            分配 operation_order

    3. item 被手遮挡或进入 HELD
        如果 item 还没有 operation_order：
            记录 first_occluded_frame = 当前帧
            分配 operation_order

    4. item 在手附近消失
        如果 item 还没有 operation_order：
            记录 first_missing_near_hand_frame = 当前帧
            分配 operation_order


注意：
    这个优先级不是“整理先后顺序”。
    它只是同一帧内多种证据同时出现时，
    选择哪种证据作为 first_operation_frame。

    真正排序仍然看 first_operation_frame 的时间早晚。


三、operation_order 怎么递增

OperationContext 里维护：

    next_operation_order
    last_assigned_operation_frame
    last_operation_group

当某个 item 第一次出现操作证据时：

    如果当前帧 > last_assigned_operation_frame：
        next_operation_order += 1
        last_operation_group += 1
        item.operation_order = next_operation_order
        item.operation_group = last_operation_group
        last_assigned_operation_frame = 当前帧

    如果当前帧 == last_assigned_operation_frame：
        item.operation_order = next_operation_order
        item.operation_group = last_operation_group

也就是说：
    不同帧第一次被操作的物品，有明确先后顺序。
    同一帧第一次被操作的物品，属于同一组，不强行区分先后。


四、同一帧多个物品同时被接触怎么办

这种情况不能说谁先谁后。

正确处理是：

    1. 它们使用同一个 operation_order
    2. 它们使用同一个 operation_group
    3. 后续 10.2 整理匹配时，对同组物品做一对一匹配
    4. 不要因为手与某个物品重叠面积更大，就说它更早被整理

重叠面积可以做什么：

    只能用于同组内的候选评分。

例如同一组里有：
    A1 / A2

stable_snapshot 里有：
    B1 / B2

那么可以计算：
    A1 -> B1 的总分
    A1 -> B2 的总分
    A2 -> B1 的总分
    A2 -> B2 的总分

这个总分里面可以包含：
    reid
    bbox 尺寸合理性
    移动距离
    手重叠面积
    手中心距离
    track 方向
    HELD 证据

但这些分数只用于 A / B 配对，
不用于判断 A1 和 A2 谁先被整理。


五、10.2 整理匹配时怎么使用时间线

原来的风险是：

    disappeared_objects 按快照数组顺序遍历。
    这个顺序不等于用户操作顺序。

改进后应该是：

    1. 先为所有 disappeared A 和 appeared B 建立 relocation 候选

    2. 每个候选都计算 relocation_score：
        reid_score
        bbox_score
        move_distance_score
        hand_evidence_score
        track_evidence_score
        time_score

    3. 先按 operation_order / operation_group 处理 A

    4. 不同 group：
        早的 group 先处理。

    5. 同一个 group：
        不强行按顺序。
        做组内一对一最优匹配。

    6. 每个 A 只能匹配一个 B。
       每个 B 也只能被一个 A 消费。


六、time_score 怎么设计

time_score 不应该很重，
它是辅助加分。

可以这样理解：

    A 有 operation_order：
        加分

    A 是较早被操作的 group：
        在处理顺序上优先，
        不是直接提高所有匹配分数。

    B 出现在 A 被操作之后：
        加分

    B 出现在 A 被操作之前：
        不加分，甚至降分

    A 消失时间与 B 出现时间接近：
        加分

    A 被操作很久以后 B 才出现：
        少加分或不加分


但要注意：
    stable_snapshot 只记录稳定后的结果，
    不一定知道 B 最早出现在哪一帧。

所以如果要让 time_score 更准确，
可能还需要 SnapshotBuffer 或 OperationContext 记录：
    某个候选 B 第一次出现的帧
    某个候选 B 连续出现了多少帧
    B 出现时附近有没有手

这个可以后续再做，
最小版本可以先只用 A 的操作时间顺序。


七、最小可落地版本

第一版不要做太复杂。

只做这些：

    1. OperationContext 增加 item_operation_evidences

    2. 每个 item 只记录：
        item_id
        class_id
        operation_order
        operation_group
        first_operation_frame
        first_touch_frame
        first_move_frame
        first_occluded_frame
        evidence_score

    3. update_operation_context 每帧发现某 item 第一次被操作时，
       给它分配 operation_order / operation_group。

    4. compare_snapshots 的 10.2 中，
       不再直接按 disappeared_indices 顺序处理。
       而是把 disappeared A 按 operation_order 排序。

    5. 没有 operation_order 的 A 放在后面。

    6. 同一个 operation_group 里的 A，
       不强行排序，
       先生成全部 A -> B 候选，
       再做一对一匹配。


八、这个方案解决什么问题

它主要解决：

    用户连续整理多个物品时，
    系统按快照数组顺序乱配对的问题。

它不能保证解决：

    YOLO 漏检
    物品完全遮挡
    两个同类物品外观极其相似
    手检测完全失败

所以它不是万能判定器。
它的作用是：
    把“用户真实操作时间线”
    引入到 10.2 的候选处理顺序和候选评分里。


九、一句话总结

    用时间决定整理顺序。
    用重叠面积、距离、reid、track 只决定匹配可信度。

    不同帧第一次被操作：
        可以排序。

    同一帧第一次被操作：
        不强行排序，放进同一个 group。

    group 内部：
        做一对一最优匹配。
