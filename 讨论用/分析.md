其实不会有这种情况的吧：
即便【A：HAND_FULL，暂时看不见; B：HAND_PARTIAL，只露出一部分】
只要当前帧看得见 B 的框D(B)
我们就可以用 D(B) 与 B.box 先[严格匹配]后[宽松匹配], 只要匹配到就说明 D(B) 与 B 的原位置匹配
这时候只要 D(B) 与 A.box 先[严格匹配]后[宽松匹配] 也不会匹配成功的啊, 以为 D(B) 不是 A 的原位置啊, 必然是无法与 A.box 匹配的啊

D(B) 顶多与 A 后续的估计框匹配而已啊, D(B) 与 A 估计框匹配说明 D(B) 怀疑是 A 的移动路径后的位置而已
这时必然是B的优先级高的啊

也就是说：同一个位置只会是一个物品的原位置，不会是多个物品共同在同一个原文中的



而且：
    就是真的好巧不巧 D(B) 都被认为是 A 与 B 的原位置的话
    那么看看 A.box 与 B.box 随离 D(B) 更近, 更近的哪个才是原位置匹配, 剩下的哪个当作是移动路径匹配咯
但是：
    如果 D(B) 都被认为是 A 与 B 的移动路径匹配的话, 这时候我就不太清楚怎办了, 这就需要你来给我建议了



这个思路你觉得如何呢？


