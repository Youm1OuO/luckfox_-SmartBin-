你说的我明白了，但是我好奇的是：
    假设原先的物体位置称为A,B。当前帧的物体A,B的位置分别称为C(A),D(B)
    既然一开始误判为A移动到D(B)的位置, 然后C(A)位置以为现在是空的
    但是这时候B物体怎么处理了？不可能B看不到了然后也不管B的死活, 然后就直接把D(B)当作是A移动后的位置吧
    A，B两个位置上, 如果看见D(B)的话应该首先与B匹配才对啊
    现在B是被手遮挡的状态(hand_*), 那么这个时候我们用[严格匹配]必然是无法将旧B与新B进行严格匹配的, 但是这时候使用[局部匹配]的话D(B)与B就能匹配成功了啊


而且我觉得：
    一个物体原位置匹配了的话，这个是更高优先级的，这时候要先将他们匹配
也就是说：
    即便D(B)与A匹配了, 但是这样只是认为A"移动"到D(B)
    如果后续我们D(B)也B匹配了, 但是这个匹配是 B 与原位置匹配, 认为B"停"在D(B)的原位置，这样"在原位置"的要优先于"移动"
    所以我们任务 B 匹配 D(B), 而不是 D(B) 匹配 A

你觉得怎么样？