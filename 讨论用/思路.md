其实我觉得new_cls_id=Model( .. ) 这种方法挺好的

我的思路是对yolo模型进行修改, 然后重新训练：

模型正常输入
    然后我们根据模型输出的 cls_id 与 box, 根据box去找到输入图中的位置的像素颜色color
    然后 Model 是一个新的小模型(一个简单的mlp), 输入color、box、cls_id, 输出一个新的cls_id
即：
    new_logits = old_logits + DeltaLogits(old_logits, color, size)
    new_cls_id = argmax(new_logits)


------


其实我想要实现：P(class | YOLO feature, color, size)
但是我能想到的具体的操作就只有我刚刚说的
你说的方法其实我不知道他们具体是怎么实现的, 所以难以评价好不好。
如果你能提供具体的实现方法，我或许可以研究一下