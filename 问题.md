 # 这个操作可写成一个函数，供 Track 和 hold_and_move 共用
def 确认_A_整理到_B(A, B):
    A.affirm = True
    B.item_id = A.item_id
    A.box = B.box
    A.base_box = new_A_base_box
    matched_snapshot_of_item[A.item_id] = B

    # 按假设：整理后的 A 不被其他物品遮挡
    A.block_ids.clear()

    # 整理物品后, 更新 block_ids
    for C in 库存物品:
        if C.item_id == A.item_id:
            continue

        if A.item_id in C.block_ids:
            if intersection_area(A.base_box, C.base_box) <= overlap_area_eps:
                C.block_ids.erase(A.item_id)

        if intersection_area(A.base_box, C.base_box) > overlap_area_eps:
            C.block_ids.insert(A.item_id)


先用 Track 收集、确认整理候选：

track_move_A_to_B[A.item_id]：A 可由 Track 证明整理到的 B.temporary_id 列表
track_move_B_to_A[B.temporary_id]：B 可由 Track 证明来自的 A.item_id 列表

for A in 库存物品:
    if A.status != 【可见】 or A.affirm:
        continue

    for B in 快照物品:
        if B.item_id != -1:
            continue

        if A.cls_id != B.cls_id:
            continue

        if abs(B.box.width - A.box.width) > eps_w:
            continue

        if abs(B.box.height - A.box.height) > eps_h:
            continue

        if Track 能唯一证明 A 与 B 是整理 or A.hold_and_move==True:
            track_move_A_to_B[A.item_id].add(B.temporary_id)
            track_move_B_to_A[B.temporary_id].add(A.item_id)


for A in 库存物品:
    if A.status != 【可见】 or A.affirm:
        continue

    if track_move_A_to_B[A.item_id].size != 1:
        continue

    B_id = track_move_A_to_B[A.item_id][0]

    if track_move_B_to_A[B_id].size != 1:
        continue

    B = 根据 B_id 找到对应快照物品

    # 防御性检查；正常双向唯一时本来就应成立
    if B.item_id != -1:
        continue

    确认_A_整理到_B(A, B)

然后只对剩余未绑定的 A、B 使用 hold_and_move：

hold_move_A_to_B[A.item_id]：A 可由 hold_and_move 整理到的 B.temporary_id 列表
hold_move_B_to_A[B.temporary_id]：B 可由 hold_and_move 来自的 A.item_id 列表

for A in 库存物品:
    if A.status != 【可见】 or A.affirm:
        continue

    if A.hold_and_move != True:
        continue

    for B in 快照物品:
        if B.item_id != -1:
            continue

        if A.cls_id != B.cls_id:
            continue

        if abs(B.box.width - A.box.width) > eps_w:
            continue

        if abs(B.box.height - A.box.height) > eps_h:
            continue

        hold_move_A_to_B[A.item_id].add(B.temporary_id)
        hold_move_B_to_A[B.temporary_id].add(A.item_id)


for A in 库存物品:
    if A.status != 【可见】 or A.affirm:
        continue

    if hold_move_A_to_B[A.item_id].size != 1:
        continue

    B_id = hold_move_A_to_B[A.item_id][0]

    if hold_move_B_to_A[B_id].size != 1:
        continue

    B = 根据 B_id 找到对应快照物品

    if B.item_id != -1:
        continue

    确认_A_整理到_B(A, B)

这样保证：

- Track 的强证据优先；
- hold_and_move 只处理 Track 未确认的剩余对象；
- hold 情况下 A、B 都必须各只有一个候选；
- 一个 B 不可能被绑定给两个 A；
- 不需要额外写 break 来碰运气防重复。