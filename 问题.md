其实我现在想到的一个track的思路是：
> 把当前这个 Yolo() 的输出结果其实可以看作是一个小的临时快照

> 如果检测到手的话
1. 当然要 no_hand_buffer.clear()
2. 被手挡住的物品我们先给他们创建一个track (如果已经有对应的track就不用创建)
3. 遍历检查是否已有track且匹配成功 (理应通过item_id必然匹配成功, 如果有某条track的item_id没有匹配上, 则说明track已经结束或出错, 我们直接删除这条track)

**思路**
> 第一帧如果物品被手遮挡 (部分遮挡或完全遮挡), 我们就直接为他创建track, 此时 hold_and_move = False
> 第二帧(或后几帧)
    > 1. 如果物品一开始是被部分遮挡的, 那么此时是看得见手跟怀疑被握住并移动的物品
        > 1.1 如果此时手在移动, 并且这个物品也在移动, 而且移动的方向与大小一样, 说明这个物品被手握住并且移动了, hold_and_move = True
        > 1.2 如果此时手在移动, 但这个物品没在移动, 说明这个物品并没有被手握住并且移动了, 直接删除他的track
    > 2. 如果物品一开始是被完全遮挡的, 那么此时是只看看得见手
        > 2.1 如果此时手在移动, 并且这个物品没能在原来的位置看到他, 说明这个物品被手握住并且移动了, hold_and_move = True
        > 2.1.1 如果此时手在移动, 但这个物品能在原来的位置看到他, 说明这个物品并没有被手握住并且移动了, 直接删除他的track
    
> 而且我希望每个track里面不仅保留物体自身的估计框的轨迹 proxy_box, 而且连同手框的移动轨迹也一起保存起来; 或许没有派上用场, 但是以后可能会用上, 增加这个没有什么坏处吧


例如：
```python
# frozen_tracks：
# 本次手操作结束后被冻结、且尚未清除的 Track
# 此函数内部会自动跳过：
# - Track 证据不足的情况；
# - hold_and_move != True 的情况；
# - A / B 不唯一的情况；
# - 已在严格匹配、局部匹配中绑定的 A / B。
unique_move_pairs = get_unique_move_pairs(
    库存物品=库存物品,
    快照物品=快照物品,
    frozen_tracks=frozen_tracks
)

for A_item_id, B_temp_id in unique_move_pairs:
    A = item_by_id[A_item_id]
    B = 根据 B_temp_id 找到对应快照物品

    # 防御性检查; 正常情况下 get_unique_move_pairs 已保证它们成立
    if A.status != 【可见】 or A.affirm:
        continue

    if B.item_id != -1:
        continue

    # B.box 就是整理后 A 的新 base_box
    A.box = B.box
    A.base_box = new_A_base_box

    # 正式确认 A 整理到 B
    A.affirm = True
    B.item_id = A.item_id
    matched_snapshot_of_item[A.item_id] = B

    # 按当前业务假设：整理后的 A 不被其他物品遮挡
    A.block_ids.clear()

    # 更新“被 A 遮挡”的其他库存物品 C
    for C in 库存物品:
        if C.item_id == A.item_id:
            continue

        if A.item_id in C.block_ids \
            and intersection_area(A.base_box, C.base_box) <= overlap_area_eps:
            C.block_ids.erase(A.item_id)

        if intersection_area(A.base_box, C.base_box) > overlap_area_eps:
            C.block_ids.insert(A.item_id)
```
