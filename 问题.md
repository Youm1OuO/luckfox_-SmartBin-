1. 每一帧我们识别到手后, 就先与old_hand计算得到[移动值],先把移动值加上

2. 先查看状态为【被手部分遮挡】的物品C, 查看上一帧中C旧位置的情况：
    > 2.1 如果旧位置仍然匹配到 C → 说明 C 只是被手短暂遮挡，没有移动, 但是不要在怀疑的第一帧就把C的hold_and_move设置为False, 先看看是否为 C.doubt_no_hold_and_move=True
        > 2.1.1 如果现在 C.doubt_no_hold_and_move=False, 说明我们是第一帧怀疑他不是被手握住并移动的, 我们先将设置 C.doubt_no_hold_and_move=True, 更新坐标C的坐标 C.box = E.box
        > 2.1.2  如果现在 C.doubt_no_hold_and_move=True, 说明我们前一阵就怀疑他不是被手握住并移动的, 我们现在就把C的状态设置为可见, C.hold_and_move=False, 更新坐标C的坐标 C.box = E.box (还有更新其他设置)
    > 2.2 如果旧位置出现了一个已知库存物品 D，并且 D 原本就在 C 后方、之前被 C 遮挡 → 说明 C 被移走，D 被重新露出。
    > 2.3 如果旧位置出现了一个不属于库存的 D，并且 D 是手附近出现的疑似物品 → 说明这是新物品放入，我们认为 C 可能仍在原处
    > 2.4 如果旧位置没有任何检测 → 说明  C 可能被拿走, 现在这里什么都没有

    > 2.2 或 2.4 的情况出现的话说明C有可能被拿走, 我们再来通过[移动值]计算一下【被手部分遮挡】的物品C这一帧的估计位置**用的是上一帧的last_box与[移动值]计算估计框, 而不是完整的框的估计框**, 并且用估计位置与手相贴的这些物品B看能不能[严格匹配](阈值可以松一点, 允许略微抖动或偏移)
        > 如果能[严格匹配](阈值可以松一点, 允许略微抖动或偏移), 那么说明这个物品C被手握住并移动到了物体B处, 更新坐标C的坐标 C.box = B.box, 更新track的信息 (还有更新其他设置)
        > 如果不能[严格匹配](阈值可以松一点, 允许略微抖动或偏移), 尝试看

3. 如果【被手部分遮挡】的物品都匹配不上这些与手相贴的物品B的话, 再来看看能不能与库存中【可见】的物品A符合[局部匹配], 如果是的话就说明这个物品B是之前的物品A

4. 如果还是无法匹配的话, 就与查看【被手完全遮挡】的物品的cls_id是否与这个物品B的cls_id一致, 如果一致的话就说明这个物品B是这些【被手完全遮挡】物品(只不过现在这些物品的露出变多了)

5. 如果还是无法匹配的话, 可能是手原先拿着一个新物品D进来, 但是新物品D一直被手完全挡住, 然后现在终于露出来了(有可能是手放下了,也有可能是视角变得可见了), 反正这个物品B就认为是这个物品D
