测试后发现的, 客观的问题（这几个问题是真真切切纯在的缺陷, 虽然业务流程大致用起来非常好, 甚至有一点完美, 但是能修正的话还是必须要修正）：


1. 有时候依旧会出现yolo识别不稳定（有一段时间识别错, 识别漏）
    具体的问题, 例如：
        1. 物品被拿走了，但YOLO没识别到，导致库存中还保留着这个不存在的物品
        2. 物品根本没拿进来，但YOLO一时误判以为拿进来了，导致库存中多了一个不存在的物品

    我感觉这可能无法在yolo中继续修改了（主要是时间、内存空间等问题, 在yolo上花功夫没必要了）, 所以我打算在业务流程中增加一个小的矫正机制：
        在【新业务流程6.md】中【十、快照差异判断】的【10.5 第四步：例行清理】后面增加：
        先给每个物品增加一个 stable_frames 计数 (或去其他名字, 我怕变量名已经被占用了)
        库存目录中【可见】的物品与yolo识别到的物品进行对比，存在库存中显示【可见】但yolo并未识别到的物品的话，我们就给他的 stable_frames 加1, 直到 stable_frames >= 10 时, 就认为这个物品已经不再可见了, 就删除这个物品
注意：
    stable_frames机制只是一个补充的辅助小机制，是增加到原来的逻辑中，而不是替换原来的逻辑。
    只对【可见】的物品进行这个触发stable_frames机制是判定删除（如果他是【遮挡】状态的话我们不增加他的stable_frames，只对库存中为【可见】状态并且yolo没事别到时才增加stable_frames） 



感觉这样或许会有点矫正的效果？我的这个方法有需要改善的地方吗？或者你有没有更好的方法？


注意：
    原本代码本身就已经特别特别好了, 你不要修改这些问题的时候导致之前没问题的地方又搞出问题了