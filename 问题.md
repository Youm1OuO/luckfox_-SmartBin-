我现在打算的思路是：
> 开门后先令 `operation_pending=False`：如果已经有本地库存，不申请后台库存，也不收集开门快照；只有本地没有库存时才申请后台库存，并单独用首次快照做只读校验（这项校验不把 `operation_pending` 设为 True）
> 摄像头拍摄图片, 然后将图片处理好输入给yolo
> 得到yolo的识别结构, 然后检查是否有识别到"手"
    > 1. 如果有识别到"手", 清空snapshot_buffer、operation_pending=True(还有其他操作，我只是将这两个比较关键的专门说说而已), 并且那么开始尝试track的处理
    > 2. 如果没有识别到"手", 判断现在 operation_pending 是否为True；如果为True就将当前无手的yolo结构增加到 snapshot_buffer；如果为False，但冷启动首次校验仍在等待，也把它加入首次校验的 snapshot_buffer；其余情况清空 snapshot_buffer，然后继续轮询，直到下一次识别到"手"
**注意：我们要时刻保留前后连续的两个yolo输出的结果, 因为我们要用来对比(处理track)**
> 如果当前yolo输出的结果有识别到"手", 那么我们就判断这两个前后的yolo结果的区别:
    > 1. 如果前一张yolo结果没有识别到"手"。我们就用后一张yolo结果中手的框这位置去跟前一张yolo结果检查, 看"手"的框跟库存物品是否有重叠(交集)
        > 1.1 如果"手"跟物品A有重叠, 就查看这个A是否已经存在track
            >  1.1.1 如果已经存在track就不管
            >  1.1.2 如果不存在track, 就创建一个track
        > 1.2 如果"手"跟物品A没有重叠, 就不管
    > 2. 如果前一张yolo结果有识别到"手"。我们就用对比前后两张yolo结果中, 那些原本有track的物品是否移动了
        > 2.1 有移动的话就更新track, 计数器清空
        > 2.2 没有移动的话就, 我们就给这个track的计数器加1

**简单说明一下track的思路:**
我们在 track 中最好必须要设置两个变量: shelter_or_hold、hold_and_move
当手遮挡了一个物品时我们就 shelter_or_hold=True, 并且给他创建 track
当我们发现这个物品移动了, 就给 hold_and_move=True
然后后续生成快照后, 那些 hold_and_move=False的 track 我们就删除掉, 剩下的就是都是hold_and_move=True的了
这些物品必然被手移动了

**判断移动的思路:**
如果前一张yolo图中,
如果手是部分遮挡了物品(物品部分露出), 然后我们这一帧图片就可以对比: 如果手移动了, 并且这个物品也移动了, 并且这两个物品移动的距离大于阈值、而且这两个移动最好大小与方向一致, 那么我们就认为物品被移动了
如果手是完全遮挡了物品(物品完全没有露出), 然后我们这一帧图片就可以对比: 如果手移动了, 并且这个物品也没有被再次发现, 我们就认定这个物品被手完握住并且移动了


> 然后继续这样“如果有识别到"手", 清空snapshot_buffer、operation_pending=True”, “如果没有识别到"手", operation_pending=True 时将当前无手的yolo结构增加到正常 snapshot_buffer；冷启动首次校验等待时则增加到只读校验 snapshot_buffer”
> 当 snapshot_buffer 存储了 n 长 yolo图是时候, 就检查 track 中是否hold_and_move=True, 如果为False就认定这个track失效, 先清楚这个track
> 然后再冻结 track开始生成快照
> 快照生成后就与库存进行对比, 最后修改库存
> 库存修改完之后就将相关的需要重置的变量重置**注意:这时 operation_pending 要重置为False**，然后继续开始循环
