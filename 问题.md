我说说其中几个点吧:
1. 快照的“投票门槛”我打算使用N/2或者2*N/3(2*N/3可以用在N是偶数的情况下更严厉,但是我怕这个过于严厉导致出错);


2. 【遮挡】也确实是不能永久停留,我想到的解决方法是: 
    给库存中的【遮挡】物品增加一个2个计数器status_1,status_2;
    如果【遮挡】我们在最后的“例行清理”中增加检查库存中,【遮挡】物品“附近”是否存在【可见】物品
    > 2.1 如果不存在【可见】物品, 那么就有很大的可能是之前某种原因导致的错判, 但是我们也不能直接就丢弃他, 我们就将这个【遮挡】物品的计数器status_1加1, 当计数器status_1达到阈值时我们就这个所谓的【遮挡】物品是误判、漏判的, 直接将其【出库】, 或者直接丢弃它(这是唯一的一种【遮挡】直接变【出库】的情况);
    > 2.2 如果存在【可见】物品的话, 我们就对这个【遮挡】物品与这个【可见】物品进行严格的身份匹配
        > 2.2.1 如果匹配成功, 说明这个【遮挡】物品极有可能是这个【可见】物品的一个误判, 误判、漏判的, 但是我们也不能直接就丢弃他, 我们这时也将这个【遮挡】物品的计数器status_2加1, 当计数器status_2达到阈值时我们就这个所谓的【遮挡】物品是误判、漏判的直接丢弃它(因为这个物品极大可能是这个【可见】物品的一个误判);
        > 2.2.2 如果匹配失败, 说明这个【遮挡】物品就大概率这个【遮挡】物品依旧时被这个【可见】物品遮挡住了,这时我们不增加他的计时


## 审视与补充

### 1. 快照投票门槛

你的方向是对的，门槛应当做成可调参数。

```text
推荐第一版：min_votes = ceil(2 * N / 3)

N = 5  → 至少出现 4 次
N = 6  → 至少出现 4 次
N = 7  → 至少出现 5 次
```

`2N/3` 不只适用于 N 为偶数。若真实物品在测试视频中经常漏检，再降为严格多数：

```text
min_votes = floor(N / 2) + 1
```

不要直接用恰好 `N/2`；N 为偶数时它只有一半帧支持，证据偏弱。

另外，进入快照时还应要求平均 `score` 不低于一个基础阈值，避免低分误检只靠次数混进快照。

### 2. 【遮挡】物品的两个计数器

这个想法可行，建议把名字改得更直白：

```text
status_1 → no_occluder_count（附近长期没有可见物品）
status_2 → duplicate_count（长期和某个可见物品严格重合）
```

需要补四条规则：

```text
1. 计数器只在“完成一次新的稳定快照对比”后加 1，不能按每一帧加；
   否则几秒钟就可能把物品错误出库。

2. 条件不再成立时，对应计数器清零。
   例如后来附近又出现了一个不相同的可见物品，no_occluder_count 清零。

3. 若【遮挡】物品重新被看见：直接改为【可见】，两个计数器都清零。

4. 阈值达到后，建议改为【出库】并保留 OUT 记录，不能直接物理删除。
   这样后续若又识别到它，仍有机会恢复原 item_id。
```

附近可能同时有多个【可见】物品，所以不能只拿第一个来判断。对每个【遮挡】物品，先找出附近全部可见物品，再统计其中有多少个能与它严格匹配：

```text
附近可见物品数量 = 0：
    no_occluder_count += 1
    duplicate_count = 0

严格匹配数量 = 1：
    说明只有一个可见物品很像它自己。
    duplicate_count += 1
    no_occluder_count = 0

严格匹配数量 = 0：
    说明附近可见物品都不是它自己，仍可能在遮挡它。
    两个计数器都清零。

严格匹配数量 > 1：
    多个候选都太像，无法确定哪个才是同一个物品。
    两个计数器都清零，不做自动删除。
```

这里的“严格匹配”使用 `MATCH_ORIGIN`；只有唯一匹配才允许增加 `duplicate_count`。

### 3. planned_changes 的冲突处理

`planned_changes` 用 `item_id` 做键，每个物品本轮只有一个最终计划。

```text
两次计划相同：合并即可。

两次计划矛盾：
例如既计划【出库】又计划【可见】。
    → 不自动提交这个物品的修改；
    → 保留原库存状态，并记录一次冲突日志。
```

这只是本次快照对比函数里的临时清单，不是第三张长期表。

### 4. Box 坐标必须统一

库存的 `anchor_box`、快照中的 box、Track.path 中的框，必须全部使用同一种坐标。

```text
建议统一使用：旋转后的原始摄像头画面坐标。

YOLO 输入的 704×704 letterbox 坐标：
    必须先还原到原始画面坐标，才能写入快照、库存和 Track。
```

不能把 704×704 的框和原始摄像头画面的框混在一起比较。
