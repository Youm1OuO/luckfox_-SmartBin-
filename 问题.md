    我之前说"快照"是无手、稳定的, 并且是由n次Yolo的识别结果合成一个"快照"的
    但是我现在发现: 这个"快照"其实并不能确保是稳定的
因为：
    我们只是用了n次Yolo的识别结果合成一个"快照", 但是这n次的过程中有可能是物体被放入与拿出过程中的结果来的
    也就是说, 这个快照是有可能使用一个移动中的物体的结果来生成的（只要这个yolo没识别到手）
    这样的话其实就说明了这个快照是有可能不稳定的(不是一个稳定时的快照结果)
    虽然大部分的结果都是stable的, 但是也有一部分是unstable的，这样的理解时对的吧？


想到的方法：
    我们把 n 的含义换成: 要连续 n 次 Yolo 识别到的物体没有太大变化(且连续无手)
    这样得到的"快照"就必然时n个Yolo 识别的一个稳定结果了了吧
但是这样有一个问题：
    要连续n帧识别到的物体没有太大变化, 并且这n帧是连续无手的才行
    这样的话这个快照很可能要实际使用较长时间才能生成出来啊, 这样会不会对实时性影响较大的呢？


=====================================================================
对以上想法的分析（2026-07-25）
=====================================================================

结论：你的理解是对的。

现有的“连续 N 帧投票”主要解决的是 YOLO 单帧漏检、误检和 bbox 抖动；它不能保证这 N 帧发生在物品静止时。
如果手漏检，或者手在画面外推动物品，移动中的物品仍可能进入这 N 帧，进而生成一个“不稳定快照”, 从而导致物品变多。

当前代码其实还有一次“相邻两个快照是否相似”的检查，但它只要求大部分物品相似。冰箱里其他物品都静止时，一个正在移动的苹果仍可能被放行。所以这不能完全替代“连续稳定帧”的要求。


建议的改法：把快照分成两个阶段
---------------------------------------------------------------------

1. 先做“稳定窗口”判断
   - 连续无手；
   - 当前帧与前一帧相比，物品数量、类别和位置没有明显变化；
   - 位置比较必须允许 YOLO 小幅抖动，不能要求 bbox 完全一样。

2. 只有连续稳定 N 帧后，才用这 N 帧合成 stable_snapshot。
   - 中途出现手：清空稳定窗口，重新等待；
   - 中途发生明显移动、物品出现或消失：从当前帧重新开始计数；
   - 得到 stable_snapshot 后，才与 baseline_snapshot 做库存差异判断。

这样，“投票”负责去噪，“稳定窗口”负责确认动作已经结束；两个职责是不同的，最好不要混在一起。


实时性会不会太差？
---------------------------------------------------------------------

通常影响不大。假设实际处理速度约 10 FPS：

    N = 4  → 用户放好物品后约等待 0.4 秒
    N = 6  → 约等待 0.6 秒

这段等待发生在用户动作结束之后，不会妨碍实时画面和 ByteTrack 每帧运行。相比把“整理”误判成“取出 + 放入”，这点延迟是值得的。

更好的做法是按时间而非死板帧数判断，例如“连续稳定 400~600 ms”；这样帧率降低时也不会因为凑不够帧而等待过久。


几个注意点
---------------------------------------------------------------------

1. N 不一定必须是奇数。只要投票门槛使用 `ceil(N * 比例)`，N=4、门槛 60% 就表示至少 3 帧出现，也没有问题。

2. “没有太大变化”建议按类别匹配后判断中心距离、IoU、框大小差异，而不是直接比较所有检测框是否完全相同；否则 YOLO 正常抖动会导致稳定窗口不断重置。

3. 这个稳定窗口应放在 SnapshotBuffer 之前。现有的“相邻快照相似”检查可以保留为第二层保护，但不应再是唯一的稳定性判断。

一句话总结：
    连续 N 帧投票 ≠ 连续 N 帧稳定。
    应先确认连续稳定，再用这些稳定帧投票生成快照。


=====================================================================
移动期间不生成快照，那 ByteTrack 做什么？（2026-07-25）
=====================================================================

## 先给结论

“移动时不生成库存快照”是正确的，而且正是快照方案的目的。

快照表示的是：
    用户这一轮操作结束后，冰箱内最终稳定的状态。

移动中的画面不是最终库存，不能拿来直接修改库存；否则一个苹果从左移到右的中间位置，也可能被误认为一个真实位置。

但是“不生成快照”不等于“不处理帧”。移动期间每一帧仍然要运行：

    YOLO 检测 → ByteTrack 跟踪 → 记录 OperationContext 证据


## 实际上不是“先停下来等 N 帧”

程序不会暂停。每来一帧都立刻处理，只是维护一个“连续稳定计数器”：

    当前帧无手且与上一帧没有明显变化 → stable_count + 1
    当前帧有手或发生明显变化         → stable_count 从当前帧重新开始
    stable_count 达到 N              → 用这 N 帧生成 stable_snapshot

所以移动刚开始时，快照候选会被重置；但 YOLO、ByteTrack、画面显示和轨迹记录仍持续运行。


## ByteTrack 和快照各自负责什么

| 阶段 | 快照负责的事 | ByteTrack 负责的事 |
| --- | --- | --- |
| 物品正在移动 | 不生成库存快照，不改库存 | 给连续检测到的物品分配相同 `track_id`，记录它的移动起点、终点和过程。 |
| 手遮挡物品 | 不生成库存快照，不改库存 | 物品断轨后，记录“哪件库存物品被哪只手挡住”；必要时暂时记录手框路径作为代理。 |
| 画面重新稳定 | 生成稳定快照，得到最终的“旧位置少了 A、新位置多了 B” | 把此前记录的 A 的轨迹/手代理路径交给快照差异逻辑。 |
| 最终裁决 | 决定是取出、放入还是整理 | 只是为“是否 A 移到了 B”提供强弱证据，不直接改库存。 |

因此，你的理解是正确的：**ByteTrack 是快照差异判断的辅助信息；库存事件仍然由稳定快照对比统一决定。**


## A 从左边移到右边时，完整过程是什么

假设旧稳定快照中有左边的苹果 A：

1. 系统已知道左边苹果对应库存 `item_id=A`。
2. 用户移动苹果时，每帧 ByteTrack 尝试让苹果保持同一个 `track_id`；程序记录：
   `A 的 item_id、轨迹起点框、当前终点框、经过的帧`。
3. 若手挡住苹果，YOLO 看不到苹果、物品 track 断开：
   程序记录 `HeldProxyEvidence`，即“item_id=A 可能被这只手拿着”，并持续保存手框的移动路径。
4. 手离开、画面稳定后，得到新快照：左边 A 消失，右边出现苹果 B。
5. 快照差异逻辑检查：
   - A 与 B 是否同类、框大小和形状是否接近；
   - A 的物品轨迹是否从左边走到 B 附近；或者手代理路径是否走到 B 附近；
   - B 是否是唯一合理候选。
6. 条件足够强：判定为【整理】，保留 `item_id=A`，只把 A 的位置更新到右边 B。
   条件不足：先进入 `PendingRelocation` 等下一份稳定快照；仍无法确认才退回普通的【取出 + 放入】。

可以把它理解为：

    快照说：“操作前后发生了什么变化？”
    ByteTrack 说：“这段变化的过程像不像同一个物品在移动？”
    业务层根据两者一起下结论。


## 与当前代码的对应关系

当前代码已经有这个基本分工：

    main.cc
      每帧 tracker.update(detections)
      每帧 session.update_hand(hand_boxes, tracks, ...)
          └─ 写入 TrackEvidence / HeldProxyEvidence
      快照生成后 session.push_snapshot(snapshot, ...)
          └─ compare_snapshots() 用上述证据决定 MOVED / IN / OUT

当前需要改进的是“快照稳定窗口”的位置：现在是先按固定 N 帧生成快照，再比较相邻快照是否相似；更合理的是先用连续稳定帧筛选，再生成快照。ByteTrack 的运行位置不变，始终是每帧运行。


## 一个现实限制

ByteTrack 依靠相邻帧 bbox 的重叠（IoU）维持 `track_id`。手完全遮住物品、物品移动太快或 YOLO 漏检时，`track_id` 可能断掉。

所以不能只相信 `track_id`：

    连续 track_id      → 强证据
    手部代理路径        → 补充强证据
    没有轨迹但前后相似  → 低置信候选，等待确认
    完全没有可靠证据    → 不强行说是整理

这也是“稳定快照负责最终裁决，ByteTrack 只提供过程证据”比“ByteTrack 直接修改库存”更安全的原因。


=====================================================================
代码视角：ByteTrack 的数据怎样进入快照对比（2026-07-25）
=====================================================================

## 先纠正一句话

对于**当前代码**：

    生成 Snapshot 本身，不使用 ByteTrack 的 Track。
    但 ByteTrack 在两份稳定快照之间的每一帧都运行并记录证据。
    到 compare_snapshots() 对比快照时，才读取并使用这些证据。

所以不是“对比时才运行 ByteTrack”，而是：

    ByteTrack 每帧收集过程信息 → OperationContext 保存 → 快照对比时消费。


## 第一步：YOLO 产生 Detection

`main.cc` 中每一帧先把 YOLO 结果转换为：

```cpp
std::vector<fridge::Detection> detections;

Detection {
    BBox  box;       // 当前帧的检测框
    float score;     // YOLO 置信度
    int   cls_id;    // 类别
};
```

这份 `detections` 有两条去向：

```cpp
tracks = tracker.update(detections, g_frame_id);  // 给 ByteTrack
snap_buffer.push(food_dets, g_frame_id, has_hand); // 给快照
```

`food_dets` 是去掉手后的 `Detection` 列表。这里没有把 `tracks` 传给 `SnapshotBuffer`。


## 第二步：ByteTrack 得到东西 A = `std::vector<Track>`

`tracker.update()` 的返回值是：

```cpp
const std::vector<Track>& tracks;

Track {
    int track_id;       // ByteTrack 分配的临时轨迹编号
    int cls_id;
    BBox box;           // 这一帧该轨迹的位置
    float score;
    TrackState state;   // NEW / TRACKED / LOST
    int frames_since_update;
    int hit_count;
    int last_frame_id;
    int start_frame;
};
```

ByteTrack 会将“本帧 detection”和“上一帧已有 Track”按 **类别相同 + bbox IoU 足够高**配对。
配对成功，就用本帧 bbox 更新原来的 Track，因此移动中的苹果会尽量保持同一个 `track_id`。


## 第三步：把 Track 变成可跨快照使用的证据

`main.cc` 紧接着调用：

```cpp
session.update_hand(hand_boxes, tracks, g_frame_id, now_ms);
```

它内部调用 `update_operation_context()`，将 `tracks` 写进当前的：

```cpp
OperationContext operation_context_;
```

`OperationContext` 的生命周期是：

    baseline_snapshot 建立时创建
    ↓
    中间每一帧持续收集 tracks / hands 证据
    ↓
    下一份 stable_snapshot 完成对比后清空并重建

### 3.1 正常物品轨迹：`TrackEvidence`

对每条非手 `Track t`，代码会先找它最可能属于哪个库存物品：

```cpp
item_id = find_inventory_item_for_track(t);
```

找到后，以 `t.track_id` 为 key，在
`operation_context_.active_track_evidences` 中建立或更新：

```cpp
TrackEvidence {
    int track_id;
    int cls_id;
    int associated_item_id; // 关联到库存中的哪个 item_id
    BBox start_box;         // 本轮操作刚开始时的位置
    BBox end_box;           // 当前最新位置
    int start_frame_id;
    int end_frame_id;
    int sample_count;
    bool occluded_by_hand;
    float confidence;
};
```

之后每帧只更新 `end_box`、`end_frame_id`、`sample_count`。
如果 `start_box → end_box` 的移动距离超过物品对角线的一半，代码把该
`track_id` 写入 `moving_tracks`，并把对应 `item_id` 写入 `moved_item_candidates`。

### 3.2 手挡住物品：`HeldProxyEvidence`

代码会比较：

```cpp
hand_box  与  inventory_item.visible_box 的重叠比例
```

手明显盖住可见物品，且物品随后在当前 `tracks` 中消失至少 2 帧时，建立：

```cpp
HeldProxyEvidence {
    int item_id;                  // 被挡住的是哪件库存物品
    int original_object_track_id; // 遮挡前物品的 track_id
    int held_by_hand_track_id;    // 遮挡它的手的 track_id（可能为 -1）
    BBox last_visible_box;        // 物品最后被看见的位置
    BBox hand_bbox_at_hold_start;
    std::vector<BBox> proxy_boxes;// 后续每帧手框的位置序列
    ...
};
```

`proxy_boxes` 不是说“手就是苹果”，而是“苹果失踪期间，手的移动可以暂时作为苹果可能移动路径的证据”。


## 第四步：快照生成仍只产生 `VotingItem`

`SnapshotBuffer` 将 `food_dets` 的 N 帧结果投票成：

```cpp
Snapshot {
    std::vector<VotingItem> items;
};

VotingItem {
    int cls_id;
    BBox box;
    float best_score;
    int count;
};
```

注意：`VotingItem` 里没有 `track_id`。因此快照只能告诉程序：

    旧快照：左边有一个苹果 A
    新快照：左边没有，右边有一个苹果 B


## 第五步：compare_snapshots() 如何实际使用轨迹证据

`compare_snapshots()` 先用两个快照得到三类集合：

```cpp
same_position_pairs  // 原位置仍存在
disappeared_indices  // 旧快照有、新快照原位置没有
appeared_indices     // 新快照新增的物品
```

对于一个消失物品 A 和一个新出现物品 B，代码调用：

```cpp
relocation_match(item_id_A, A, B, reid, second_reid, &evidence_score);
```

它内部的 `relocation_evidence_score(item_id_A, A.box, B.box)` 会读取
`operation_context_`：

1. 若 A 对应的 `TrackEvidence.start_box` 靠近 A 的旧位置，且
   `TrackEvidence.end_box` 靠近 B 的新位置，给很高的移动证据分；
2. 若 A 的 `HeldProxyEvidence.proxy_boxes.back()` 靠近 B，给更高的手代理证据分；
3. 若 A 只是被确认手持、但路径没有到 B 附近，只给较低分；
4. 若完全没有轨迹/手代理证据，不能高置信判定整理。

再结合：类别相同、bbox 面积和宽高比接近、候选是否唯一，最终决定：

```cpp
CONFIRMED      → MOVED，保留 item_id_A，更新它的位置到 B.box
LOW_CONFIDENCE → 写入 PendingRelocation，等下一份稳定快照
NO_RELOCATION  → 后续再按普通 OUT / IN 处理
```


## 最短的数据流图

```text
YOLO Detection
   ├─→ SnapshotBuffer → stable_snapshot（只记录最终看到什么）
   └─→ ByteTrack → Track
                      └─→ OperationContext
                            ├─ TrackEvidence
                            └─ HeldProxyEvidence

baseline_snapshot + stable_snapshot
   └─→ compare_snapshots()
          └─→ 读取 OperationContext 的证据
                 └─→ 判定 MOVED / OUT / IN
```

一句话总结：
    快照不带 track_id；ByteTrack 也不直接改库存。
    ByteTrack 只在两份快照之间积累“过程证据”，
    快照对比时用这些证据决定“消失 A 与出现 B 是否其实是同一次移动”。


=====================================================================
例子：苹果从左边移到右边，轨迹证据如何参与判定（2026-07-25）
=====================================================================

假设当前库存中已有：

```text
item_id = 3
cls_id  = apple
box     = 左边位置 L
status  = VISIBLE
```

此时系统已经得到旧稳定快照 `snap1_`，里面也有左边的苹果 A。
从现在起，到下一份稳定快照到来之前，所有 ByteTrack 证据都会写进同一份
`operation_context_`。


## 情况一：苹果没有被完全挡住

### 移动期间的帧

```text
第 101 帧：苹果在左边 L，ByteTrack 给它 track_id = 12
第 102 帧：苹果被推向中间，track_id 仍是 12
第 103 帧：苹果到右边 R，track_id 仍是 12
```

代码在每帧调用 `update_operation_context()`。第一次看到 `track_id=12` 时，建立：

```cpp
operation_context_.active_track_evidences[12] = {
    .track_id = 12,
    .associated_item_id = 3,
    .start_box = L,
    .end_box = L,
};
```

第 102、103 帧继续用同一个 `track_id=12`，所以只更新：

```cpp
TrackEvidence.end_box = 当前苹果框;
```

最后得到的实际证据是：

```text
TrackEvidence:
    associated_item_id = 3
    start_box = L
    end_box   = R
```

注意：当前代码的 `TrackEvidence` 主要保存起点和最新终点，不保存每一个物品 bbox；
“完整路径”主要由手部代理的 `proxy_boxes` 保存。

### 画面稳定后的快照对比

新稳定快照 `snap2` 中：

```text
左边 L 的苹果 A 消失
右边 R 出现苹果 B
```

`compare_snapshots()` 会把 A 放进 `disappeared_indices`，把 B 放进
`appeared_indices`。然后尝试问：

```text
“item_id=3 的 A，是否整理到了 B？”
```

调用链为：

```cpp
relocation_match(3, A, B, ...)
  └─ relocation_evidence_score(3, A.box, B.box)
```

`relocation_evidence_score()` 找到上面的 `TrackEvidence` 后检查：

```text
start_box 是否靠近 A 的旧位置 L？  是
end_box   是否靠近 B 的新位置 R？  是
```

两项都是“是”，所以给这次 A → B 很高的移动证据分（代码中约 `0.75`；若轨迹曾被手遮挡则约 `0.80`）。

再加上 A、B 都是苹果，框的面积/宽高比接近，而且没有第二个相似候选，
就满足 `CONFIRMED` 条件，结果是：

```cpp
inventory_.update_anchor_item(3, -1, B.box, ...);
// item_id=3 从左边更新到右边
// 产生 EventKind::MOVED
```

不是：

```text
左边 item#3 出库 + 右边新建 item#4 入库
```


## 情况二：苹果被手完全挡住，物品 track 断掉

```text
第 101 帧：苹果在左边 L，track_id = 12
第 102 帧：手盖住苹果，YOLO 只看见手
第 103 帧：手移动到右边 R，苹果仍不可见
第 104 帧：手离开，苹果在右边 R 重新被检测到
```

第 102 帧开始，物品的 `track_id=12` 可能没有新 bbox 了；这时无法再依赖
`TrackEvidence.end_box`。不过代码发现：

```text
手框与 item#3.visible_box 重叠
item#3 随后至少 2 帧没有出现在 tracks 中
```

于是建立：

```cpp
HeldProxyEvidence {
    .item_id = 3,
    .original_object_track_id = 12,
    .last_visible_box = L,
    .proxy_boxes = [手在左边的框, 手在中间的框, 手在右边的框],
};
```

当 `snap2` 中右边出现 B 时，`relocation_evidence_score()` 再检查：

```text
HeldProxyEvidence.proxy_boxes.back()
    是否靠近 B.box（右边 R）？
```

如果接近，就给约 `0.85` 的强证据分。也就是说，代码不是说“手框等于苹果框”，而是说：

```text
苹果在左边被这只手遮住后消失；
这只手最后到了右边；
右边恰好稳定出现了相似苹果。
```

这三件事合起来，才有足够理由将 B 认定为原来的 `item_id=3`。


## 如果没有这些证据会怎样

若 `snap1_` 左边苹果消失、`snap2` 右边出现苹果，但：

```text
没有连续 track_id；
没有手代理路径；
或者手/物品路径终点不在右边；
```

则证据分很低。代码不会直接判定【整理】，而是先进入
`PendingRelocation` 等待一次稳定确认，或最后按普通【取出 + 放入】处理。

这就是 ByteTrack 辅助证据真正带来的差别：

```text
只有快照：     “左边少苹果，右边多苹果。”
加上轨迹证据： “左边这一个苹果，很可能被移动到了右边。”
```


=====================================================================
track_id 是怎么来的？ByteTrack 还有没有必要？（2026-07-25）
=====================================================================

## 1. `track_id=12` 如何认定“还是同一个物体”

`track_id` 不是 YOLO 给的，也不是库存的 `item_id`。它是 ByteTrack 自己在
`tracker.update(detections, frame_id)` 中分配的短期编号。

例如第 101 帧，YOLO 检测到一个苹果：

```text
Detection = { cls_id=apple, box=L, score=0.90 }
```

这是第一次看到它，ByteTrack 创建一条 `Track`：

```text
Track = { track_id=12, cls_id=apple, box=L, state=NEW }
```

第 102 帧又检测到苹果，ByteTrack 会把“新 detection”和“上一帧已有 Track”逐个比较。
当前代码的规则是：

```text
1. 类别必须一样；                 apple 不能匹配到 milk
2. 两个 bbox 的 IoU 必须足够高；  苹果框与旧苹果框有较大重叠
3. 如果有多个可配对，优先选择 IoU 最大的一对。
```

若第 102 帧的苹果框仍与 `track_id=12` 的旧框重叠足够多，代码只更新原 Track 的 `box`：

```text
Track#12.box = 第102帧苹果的新框
Track#12.track_id 仍然是 12
```

因此，ByteTrack 所说的“同一个物体”实际含义是：

> 相邻帧中，类别相同且位置连续、框重叠足够多的目标，暂时认为是同一个。

它**不是绝对身份识别**。移动太快、完全遮挡或两个同类物品挨得很近，都可能断 ID 或换 ID。

当前代码还会把暂时匹配不上的 Track 保留为 `LOST`，最多 60 帧；后面若有同类检测框再次与它重叠足够多，可以重新回到同一个 `track_id`。若一直匹配不上，才销毁该 Track。


## 2. `active_track_evidences[12]` 到底是什么

它是 C++ 的 `map`（可以理解为“按 key 查找的字典”）：

```cpp
std::map<int, TrackEvidence> active_track_evidences;
```

因此：

```cpp
operation_context_.active_track_evidences[12]
```

意思是：

> 取出“ByteTrack 编号为 12 的那条轨迹”的业务证据记录。

这个记录里又保存 `.track_id = 12`：

```cpp
TrackEvidence {
    track_id = 12,
    associated_item_id = 3,
    start_box = L,
    end_box = R,
};
```

这里的 `track_id=12` 和 map 的 key `12` 在当前实现中确实是重复信息。
它不是用来再次确认身份，而是把 key 也写进记录本身，便于日志、复制这条记录或以后改成 vector/上传时仍知道它属于哪条轨迹。

真正重要的是这层关联：

```text
TrackEvidence 的 key=12        → 这是一条临时视觉轨迹
TrackEvidence.associated_item_id=3 → 这条轨迹在本轮开始时属于库存 item#3
```

所以最终不是拿 `track_id=12` 当库存身份，而是借它保存：

```text
库存 item#3 在这次操作中，曾经从 L 移动到 R。
```


## 3. 快照主导后，ByteTrack 是否还有必要？

我的判断：**有必要保留，但必须降级为“可失败的辅助证据”，不能再当主流程。**

快照已经足够处理：

```text
稳定快照前有物品，稳定快照后没有 → 可能取出
稳定快照前没有物品，稳定快照后有 → 可能放入
```

但只靠快照，很难可靠区分：

```text
左苹果消失 + 右苹果出现
    是同一个苹果被整理？
    还是左苹果被取走，右边放进了另一个苹果？
```

这正是 ByteTrack 的价值：它补充“中间过程”的信息。特别是赛题要求排除整理、移动和手部遮挡时，保留它是有意义的。

不过，第一版不应让 ByteTrack 复杂到主导库存。推荐明确分层：

| 层 | 是否必须 | 职责 |
| --- | --- | --- |
| 无手稳定快照 | 必须 | 最终决定库存前后有什么变化。 |
| 库存 `item_id` / `anchor_box` | 必须 | 保存物品的长期身份。 |
| ByteTrack | 建议保留 | 只为“这次变化是否为移动/整理”加分或降分。 |
| HELD 手部代理 | 可第二阶段完善 | 在物品被手完全挡住、普通 track 断掉时补充路径。 |


## 4. 对当前代码最容易理解的使用方式

不要把 ByteTrack 想成“另一个库存系统”。它只做下面三件事：

```text
① 同一个检测目标连续出现时：尽量给它同一个短期编号 track_id。
② 每帧把该编号的起点和最新位置记入 TrackEvidence。
③ 快照发现 A 消失、B 出现时，检查这条轨迹是否从 A 附近走到了 B 附近。
```

判断优先级应始终是：

```text
稳定快照差异        → 提出 A 消失、B 出现
ByteTrack 轨迹证据   → 支持或反对“A 移到了 B”
最终业务规则        → MOVED / OUT + IN / PendingRelocation
```

而不是：

```text
看到 track_id 移动 → 立刻更新库存        （不推荐）
```


## 5. 当前实现的一个需要知道的事实

`InventoryItem` 虽然有 `track_id` 字段，但当前 `session.cc` 在更新库存时大多传入 `-1`。
因此当前代码并没有把“库存 item#3 永久绑定到 Track#12”。它主要是在本轮
`OperationContext` 内，通过位置匹配将 Track 关联到 item_id，再保存成 `TrackEvidence`。

这是合理的方向：

```text
item_id  → 长期库存身份
track_id → 短期视觉轨迹，下一次操作可能已经换号
```

后续若要改进，优先做“本轮开始时把 Track 与 item_id 的关联冻结”，而不要把 track_id 当成长久身份。


一句话总结：

    快照回答“前后发生了什么”；
    ByteTrack 回答“中间是怎样发生的”。
    保留 ByteTrack，是为了更可靠地识别整理；
    但即使 ByteTrack 失败，库存最终仍应以稳定快照为准。


=====================================================================
为什么要区分 item_id 和 track_id？item_id 有什么用？（2026-07-25）
=====================================================================

## 先回答：它们不是同一个东西

你的理解已经接近正确：

```text
item_id  = 库存系统给“这件库存物品”的编号
track_id = ByteTrack 给“视频中一段连续轨迹”的编号
```

它们虽然都是整数，但不是同一个系统产生的。

```text
InventoryDB::add_item()  → 分配 item_id
ByteTrackLite::create_new_track() → 分配 track_id
```

具体到当前代码：

```cpp
// inventory.cc：库存表分配 item_id
it.item_id = next_item_id_++;

// tracker.cc：ByteTrack 分配 track_id
t.track_id = next_track_id();
```


## 两者的区别

| 对比项 | `item_id` | `track_id` |
| --- | --- | --- |
| 谁创建 | 库存 `InventoryDB` | ByteTrack |
| 表示什么 | “库存里的第几件具体物品” | “这几帧画面里连续出现的目标” |
| 何时产生 | 物品确认入库/初始化时 | 目标首次被连续检测到时 |
| 何时失效 | 出库历史过期或库存删除时 | 遮挡、漏检超时后 Track 被移除时 |
| 是否可靠长期存在 | 应该是库存层的长期身份 | 不可靠，只适合短期运动过程 |
| 是否可直接决定库存 | 可以承载库存状态 | 不可以，只是辅助证据 |

举一个最直接的例子：

```text
库存 item#3：左边的苹果
```

用户开门后，摄像头看到了它：

```text
Track#12：第 1~20 帧里连续看到的左边苹果
```

手完全盖住苹果 6 秒：ByteTrack 的 `Track#12` 最终会超时删除；
但库存系统仍必须记得 `item#3` 还在冰箱中、只是 `OCCLUDED`，不能因为一条视觉轨迹消失就删除库存。

等苹果再次露出，ByteTrack 很可能会创建新的 `Track#27`：

```text
同一件库存 item#3
先后对应过 Track#12 和 Track#27
```

这就是两者不能合并的最主要原因。


## 为什么不能只靠 `box + cls_id` 作为身份？

`box + cls_id` 能用来做“这次检测可能对应谁”的匹配，
但不能当成长期身份，因为它们都会变化：

```text
cls_id：同类物品会重复，例如两个苹果都是 apple。
box：物品整理后会移动；被遮挡时会变小；YOLO 也会抖动。
```

### 例子 1：苹果移动后 box 改变

```text
操作前：苹果在 L（左边）
操作后：苹果在 R（右边）
```

若只用 `box + cls_id`，操作后得到的是：

```text
apple + R
```

它已经无法直接等同于原来的：

```text
apple + L
```

这时 `item_id` 的意义就是：

```text
“旧位置 L 的 apple 对应 item#3；
  证据表明它移动到了 R；
  所以更新 item#3 的 box 为 R。”
```

如果没有 `item_id`，本质上只能删除 `apple+L`，再新增 `apple+R`。

### 例子 2：两个相同苹果

```text
item#3：苹果，左边 L
item#4：苹果，右边 R
```

两者 `cls_id` 完全相同。若盒子位置也接近，单靠 `box + cls_id` 可能把两个苹果匹配错。
`item_id` 至少让系统能明确保存两条独立的库存记录、状态和历史：

```text
item#3 是否被手拿过？是否遮挡？何时出库？
item#4 是否被手拿过？是否遮挡？何时出库？
```

注意：`item_id` 不能神奇地解决“两个一模一样苹果”的视觉区分问题；
它解决的是：一旦业务层已经作出关联，系统能稳定地知道应该更新哪一条库存记录。

### 例子 3：遮挡物品根本没有当前 box

苹果被牛奶完全挡住时，YOLO 没有苹果的新检测框。
库存仍需保存：

```text
item#3：apple，status=OCCLUDED，最后 anchor_box=L
```

若没有 `item_id`，这个“当前不可见但仍在库存中”的对象就没有一个可靠的保存位置。


## item_id 在当前代码中具体拿来做什么

当前代码里，`item_id` 是 `InventoryDB.items_` 这张库存表的 key：

```cpp
std::map<int, InventoryItem> items_;
```

它被用来：

```text
1. 找到并更新同一库存物品
   update_item(item_id, ...)          // 只更新 visible_box
   update_anchor_item(item_id, ...)   // 整理/放回后更新主位置

2. 保存状态
   set_status(item_id, VISIBLE / OCCLUDED / OUT)

3. 把短期轨迹接到库存对象
   TrackEvidence.associated_item_id
   HeldProxyEvidence.item_id

4. 保存低置信整理候选
   PendingRelocation.item_id_A

5. 上报事件时说明“哪件库存物品”发生了事件
   InventoryEvent.item_id
```

换句话说，`item_id` 不是“为了更快识别 YOLO 框”而设计的；
它是库存数据库的主键，用来把位置、状态、事件、遮挡历史和轨迹证据绑定在同一件物品上。


## 那么 `InventoryItem.track_id` 字段还要不要？

这个字段的设计意图是保存“该库存物品最近对应哪条视觉轨迹”，便于快速关联。
但当前 `session.cc` 多数更新调用都传入 `-1`，所以它现在没有被充分利用。

这不影响“保留 item_id”的必要性；反而说明应该保持清晰的关系：

```text
item_id 是主角（库存身份）
track_id 是临时助手（本轮过程轨迹）
```

后续可选择：

```text
简单版：不依赖 InventoryItem.track_id，只用位置把 Track 对应到 item_id。
改进版：本轮操作开始时，将可靠 track_id 临时绑定到 item_id；断轨也不删除 item_id。
```

第一版建议采用简单版，避免把 `track_id` 错当成长期身份。


## 一个当前代码的现实说明

当前 `start_new_session()` 会重新创建 `InventoryDB()`；如果后台库存接口尚未接入，
下一次开门会从第一份快照重新初始化，因此 `item_id` 会重新编号。

所以当前的 `item_id` 是“当前开门会话内稳定”的本地库存编号，尚不是跨开门会话永久不变的编号。
等后台库存同步接入后，应该让后端物品 ID 或稳定 UUID 作为跨会话身份；
但在一次开门操作期间，`item_id` 仍然是必要的。


一句话总结：

    `box + cls_id` 用于“猜测这次检测可能是谁”；
    `track_id` 用于“记录它这几帧怎么移动”；
    `item_id` 用于“库存系统确认后，持续管理的是哪一件物品”。
