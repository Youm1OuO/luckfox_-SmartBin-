我现在大概想到了怎么去匹配track了：

注意：
    我们的track是为了处理整理才提出了, 所以我们必然默认是物体本身就是在库存中的 (即：假设没有物品拿进拿出)

如果本帧有识别到手
    看当前帧手附近是否有其他物品A(而不是看库存), 
        > 1. 如果有物品A的话根据这个物品的cls_id先把track中相同cls_id并且不是"被手完全遮挡"的track先找出来, 然后逐个对比这些选出来的track中的last_yolo_box和当前帧的物品A box的是否匹配 (主要看中心点是否够近, 框的长与宽的阈值可以宽松点, 因为这个这一帧可能手有所变化导致识别到的框的大小改变)
            > 1.1 如果匹配的话, 就选上了这个track, 并更新这个track的last_yolo_box
            > 1.2 如果没有匹配的话, 就看看库存中物品A box这个位置是否有这个【可见】的而且相同cls_id的物品(此时由于物品可能被遮挡部分了, 所以这里使用的匹配方法是：交集/面积小的面积, 即：这个框是否被仓库中的这个物品的框所包含)
                > 1.2.1 如果有匹配的话, 为这个物品创建新的track (这种情况很大可能就是手刚进来而已)
                > 1.2.2 如果没有匹配的话, 就【operation_pending = true；清空 no_hand_buffer；本帧不参与快照；InventoryDB 不修改。】(说明有可能是手单纯的进来晃了晃, 也有可能是误检或漏检了, 反正track只是处理"整理"而已, 而且也是别到手了, 反正这次我们就先略过)
            > 2.1 如果匹配的话, 就把物品A的track的last_yolo_box更新为当前帧的手的box (这种情况说明是手把这个物品抓住了，并且移动了)
            > 2.2 如果没有匹配的话, 就看看库存中手 box这个位置是否有这个【可见】的物品
                > 2.2.1 如果有, 为这个物品创建新的track (这种情况很大可能就是手刚进来而已)
            > 2.2.2 如果没有, 就【operation_pending = true；清空 no_hand_buffer；本帧不参与快照；InventoryDB 不修改。】(说明有可能是手单纯的进来晃了晃, 也有可能是误检或漏检了, 反正track只是处理"整理"而已, 而且也是别到手了, 反正这次我们就先略过)


---

## 这个思路的正确部分与需要修正的部分

你的主线是对的：

```text
有手时，先尝试把当前物品 Detection 接到已有 Track；
接不上时，才查询库存中手附近的 VISIBLE 物品并创建新 Track；
接不上库存时，不必为了“整理”强行创建 Track。
```

因为 Track 的唯一业务用途是证明“原有库存物品整理到了新位置”。所以，对没有关联到旧库存物品的 Detection，可以暂时略过，最终由稳定快照与库存判断它是新放入、误检或其他情况。

但有四个关键修正。

### 修正 1：先匹配已有 Track 时，不应只看“手附近”的物品 Detection

有手的本帧，应让 **所有物品 Detection** 都先尝试匹配已有 Track，而不是只看手附近的 Detection。

原因：手可能已经放下苹果、移动去拿右边牛奶；此时苹果 Detection 在中间，已经不在手附近。

```text
若不处理这个离开手的苹果 Detection，
Track 可能继续错误跟随右边的手。
```

因此正确顺序是：

```text
1. 所有当前物品 Detection → 尝试匹配 active Track；
2. 匹配成功的 Detection → 更新对应 Track；
3. 只对“仍未匹配 Track 且位于手附近”的 Detection，
   再查询库存，决定是否创建新 Track；
4. 最后处理“手在但完全没有物品 Detection”的情况。
```

### 修正 2：完全遮挡时，绝不能把手框写进 `last_yolo_box`

你写的：

```text
把物品 A 的 Track 的 last_yolo_box 更新为当前帧手的 box
```

这里不对。

```text
last_yolo_box = 上一次匹配到本 Track 的 YOLO 物品框；
它必须始终是“物品框”，不能变成手框。
```

物品完全被手挡住时，应当这样更新：

```cpp
hand_delta = Center(current_hand_box) - Center(track.last_hand_box);

track.estimated_box = Move(track.estimated_box, hand_delta);
track.last_hand_box = current_hand_box;
track.state = FULL_HAND_OCCLUDED;

// track.last_yolo_box 保持上一次物品 YOLO 框，不更新
```

含义是：

```text
last_yolo_box：保存“上次看到物品时，YOLO 框是什么样”。
estimated_box：保存“即使现在看不见，物品估计移动到哪里”。
last_hand_box：保存“手上次在哪里”，用于计算手位移。
```

### 修正 3：完全遮挡后物品重新出现，要用 `estimated_box` 接回 Track

你在第 1 步中排除了“被手完全遮挡”的 Track，这只对一半。

```text
被手完全遮挡的 Track
不能用 last_yolo_box 与当前 Detection 严格比较；
但当前 Detection 重新出现时，仍然需要有办法接回这条 Track。
```

正确做法：

```text
若 Track 上一帧不是完全遮挡
→ 当前 D 与 Track.last_yolo_box 比较。

若 Track 上一帧是完全遮挡
→ 当前 D 与 Track.estimated_box 比较。
```

第二种比较只看：

```text
cls_id 相同；
当前 D 的中心是否在 estimated_box 内部或附近；
```

当前 D 可能还是局部小框，因此不要求它与完整 `estimated_box` 的宽高相同。

若接回成功：

```cpp
track.last_yolo_box = D.box;
track.state = PARTIAL_HAND_OCCLUDED 或 NORMAL_VISIBLE;

// 本帧不拿 D.box 与很久以前的 last_yolo_box 算位移
// 下一帧才重新使用相邻 YOLO 框的位移
```

### 修正 4：有手时，快照缓冲的处理是固定动作

只要本帧识别到手，就无论是否找到 Track、是否匹配到库存，都要先执行：

```cpp
operation_pending = true;
no_hand_buffer.clear();
// 本帧不参与快照，InventoryDB 不修改
```

所以你在 `1.2.2`、`2.2.2` 写的这段不是“匹配失败后的特殊处理”，而是“本帧有手”一开始就已经做完的固定处理。

匹配失败时只需：

```text
不创建可用于整理证明的 Track；
继续处理下一帧；
```

## 建议采用的完整逻辑

### 情况 A：本帧有手，且有物品 Detection

```text
0. 先固定执行：operation_pending=true；清空 no_hand_buffer。

1. 对所有当前物品 Detection，尝试匹配 active Track：

   a. Track 非完全遮挡：
      D.box 与 Track.last_yolo_box 比较。

   b. Track 完全遮挡：
      D.box 与 Track.estimated_box 的位置比较。

2. 每个 Detection、每条 Track 最多匹配一次。
   多个同类候选时，只接受唯一的最佳匹配；不唯一则不强行绑定。

3. 匹配成功：
   更新已有 Track；不查询库存，不创建新 Track。

4. 对剩余、未匹配 Track 的 Detection：
   只有它在手附近时，才查库存中：
   同 cls_id、VISIBLE、尚未被 Track 绑定、位置也在手附近的物品。

   唯一匹配库存 item_id
   → 创建 bound_item_id=item_id 的新 Track。

   没有唯一库存匹配
   → 不创建“整理用”的正式 Track，先略过。
```

### 情况 B：本帧有手，但没有物品 Detection

```text
0. 先固定执行：operation_pending=true；清空 no_hand_buffer。

1. 对已有 active Track：
   本帧没有 Detection 接上它，且手仍靠近它的 estimated_box
   → 用手位移更新该 Track.estimated_box。

2. 查询库存中手附近的 VISIBLE 物品：
   若某个物品尚未有 active Track，
   → 为它创建候选 Track；
   → start_box/estimated_box 取 anchor_box（无效时取 last_seen_box）；
   → 本帧只记录 last_hand_box，下一帧再开始计算手位移。
```

### 多个附近物品怎么办

```text
当前 Detection 能唯一对应其中一个库存项
→ 只创建该物品的 Track。

当前没有 Detection，手同时盖住多个库存物品
→ 可以给每个附近 VISIBLE 物品建立候选 Track；
  后面只有路径与稳定快照新位置唯一对应时，才确认整理。
```

无法唯一对应时，宁可不确认整理，也不要随便给错误的 `item_id` 建 Track。

## 一个很短的逐帧例子

```text
库存：item_id=1 的苹果在左边 L。

第 10 帧：手出现，YOLO 看到苹果 L_part。
→ 没有旧 Track；
→ L_part 在手附近；
→ 与库存 item_id=1 唯一对应；
→ 创建 Track#1，bound_item_id=1。

第 11 帧：手仍在，YOLO 看到苹果 M_part。
→ M_part 先匹配 Track#1.last_yolo_box；
→ 匹配成功；更新 Track#1；
→ 不再查询库存，不创建第二条苹果 Track。

第 12 帧：手仍在，YOLO 看不到苹果。
→ 没有 Detection 可接 Track#1；
→ 用手位移更新 Track#1.estimated_box；
→ last_yolo_box 保持 M_part，不能写成手框。

第 13 帧：苹果在右边重新出现 R_part。
→ R_part 与 Track#1.estimated_box 的位置匹配；
→ 接回 Track#1；last_yolo_box=R_part；
→ 下一帧再继续使用 YOLO 框位移。
```
