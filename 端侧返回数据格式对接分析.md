# 端侧返回数据格式 — 对接分析报告（已确认）

## 一、接口文档要求（已确认）

### 1. 端侧发送给后台（事件上报）

**格式**：
```json
{
  "device_id": "luckfox-pico-01",
  "timestamp": 1781488800000,
  "event_type": "ITEM_IN",
  "data": [{
    "local_track_id": 101,
    "category": "苹果",
    "confidence": 0.91,
    "bbox": [88, 120, 140, 132],
    "crop_image": "/9j/4AAQSkZJRgABAQ..."
  }]
}
```

**字段说明**：
- `device_id`：设备 ID
- `timestamp`：时间戳（毫秒）
- `event_type`：ITEM_IN / ITEM_OUT / ITEM_MOVED
- `data`：数组，每个元素包含：
  - `local_track_id`：本地追踪 ID
  - `category`：**中文细粒度**（如"苹果"）
  - `confidence`：置信度
  - `bbox`：[x, y, w, h]
  - `crop_image`：base64 编码的截图（ITEM_IN 强烈建议带，ITEM_OUT/MOVED 可选）

### 2. 后台返回给端侧（库存清单）

**格式**：
```json
[{
  "id": "b3f0b4f2-8a4e-4b5e-9e1c-xxxx",
  "device_id": "luckfox-pico-01",
  "category": "苹果",
  "status": "IN_STOCK",
  "remain_ratio": 1.0,
  "bbox": [88, 120, 140, 132],
  "snapshot_path": "uploads/luckfox-pico-01_101_1781488800.jpg",
  "created_at": "2026-06-15T10:00:00"
}]
```

**字段说明**：
- `id`：后台分配的唯一 ID
- `device_id`：设备 ID
- `category`：中文细粒度（如"苹果"）
- `status`：库存状态（如"IN_STOCK"）
- `remain_ratio`：剩余比例
- `bbox`：[x, y, w, h]
- `snapshot_path`：快照路径
- `created_at`：创建时间

---

## 二、当前代码问题分析

### 问题 1：事件上报 `category` 字段格式不正确 ❌

**当前代码**（`main.cc:421`）：
```cpp
job.category = fridge::coarse_category(ev.cls_id);  // 返回 "fruit_veg"
```

**后端期望**：
```json
"category": "苹果"
```

**影响范围**：
- 事件上报（ITEM_IN / ITEM_OUT / ITEM_MOVED）：`cloud_uploader.cc:150`

**解决方案**：
- 把 `coarse_category(ev.cls_id)` 改成中文细粒度类别名
- 需要一个 `cls_id → 中文名` 的映射函数

---

### 问题 2：关门库存快照格式不正确 ❌

**当前代码**（`inventory.cc:142-174`）：
```json
{
  "device_id": "luckfox",
  "timestamp": xxx,
  "session_id": "xxx",
  "event_type": "DOOR_CLOSE",
  "inventory": [{
    "item_id": 1,
    "category": "fruit_veg",      // ❌ 英文粗粒度
    "fine_class": "apple",        // ❌ 多余字段
    "status": "可见",             // ❌ 端侧内部状态，后台不需要
    "bbox": [x1, y1, w, h]
  }]
}
```

**后端期望**：
```json
[{
  "id": "b3f0b4f2-...",
  "device_id": "luckfox-pico-01",
  "category": "苹果",
  "status": "IN_STOCK",
  "remain_ratio": 1.0,
  "bbox": [88, 120, 140, 132],
  "snapshot_path": "uploads/...",
  "created_at": "2026-06-15T10:00:00"
}]
```

**需要修改**：
1. `category` 字段改成中文细粒度（如"苹果"）
2. 删除 `fine_class` 字段（已合并到 category）
3. 删除端侧内部 `status` 字段（后台不需要）
4. 根据后端期望调整格式（可能需要添加 `id`、`remain_ratio`、`snapshot_path`、`created_at` 等字段）

**注意**：
- 后台返回的库存清单中有 `status`（如"IN_STOCK"），但这是**后台的状态**，不是端侧的内部状态
- 端侧的内部状态（VISIBLE/OCCLUDED/OUT）是端侧自己用的，不应该返回给后台
- 如果后端需要知道物品是否在库，应该由后端自己判断，而不是由端侧返回

---

## 三、需要修改的代码

### 修改 1：添加中文类别映射

**位置**：`fridge_config.h`（新增函数）

**内容**：添加 `cls_id → 中文名` 的映射函数
```cpp
inline const char* cls_id_to_chinese(int cls_id) {
    static const char* cn_names[] = {
        "苹果", "香蕉", "橙子", "番茄", "柠檬",
        "梨", "葡萄", "草莓", "西瓜", "哈密瓜",
        "木瓜", "牛油果", "黄瓜", "胡萝卜", "土豆",
        "洋葱", "甜椒", "叶菜", "鸡蛋", "包装肉",
        "包装鱼", "盒装牛奶", "瓶装牛奶", "酸奶杯",
        "果汁瓶", "苏打罐", "水瓶", "袋装食品",
        "盒装食品", "罐头", "玻璃罐", "保鲜膜",
        "保鲜盒", "手", "蘑菇", "南瓜", "大蒜",
        "姜", "萝卜", "红薯", "核桃", "香菜",
        "秋葵", "白菜", "豆角", "生菜", "卷心菜", "苦瓜"
    };
    if (cls_id >= 0 && cls_id < 48) return cn_names[cls_id];
    return "未知";
}
```

### 修改 2：事件上报 category 字段

**位置**：`main.cc:421`

**当前代码**：
```cpp
job.category = fridge::coarse_category(ev.cls_id);
```

**修改为**：
```cpp
job.category = fridge::cls_id_to_chinese(ev.cls_id);
```

### 修改 3：关门库存快照格式

**位置**：`inventory.cc:142-174`

**当前代码**：
```json
{
  "device_id": "luckfox",
  "timestamp": xxx,
  "session_id": "xxx",
  "event_type": "DOOR_CLOSE",
  "inventory": [{
    "item_id": 1,
    "category": "fruit_veg",
    "fine_class": "apple",
    "status": "可见",
    "bbox": [x1, y1, w, h]
  }]
}
```

**修改为**（按后端期望格式）：
```json
[{
  "id": "item_1",
  "device_id": "luckfox-pico-01",
  "category": "苹果",
  "status": "IN_STOCK",
  "remain_ratio": 1.0,
  "bbox": [88, 120, 140, 132],
  "snapshot_path": "",
  "created_at": "2026-06-15T10:00:00"
}]
```

**注意**：
- 删除端侧内部 `status` 字段
- 删除 `fine_class` 字段
- `category` 改成中文细粒度
- 根据后端期望添加其他字段（如 `id`、`remain_ratio`、`snapshot_path`、`created_at`）
- 如果后端不需要某些字段，可以省略或设为空值

---

## 四、对主程序的影响分析

### ✅ 不影响主程序的部分

1. **业务逻辑**（session.cc）：
   - 快照差异判断、整理/拿走/放入逻辑、stable_frames 机制
   - 这些都不依赖 category 字段格式

2. **跟踪器**（tracker.cc）：
   - ByteTrack 跟踪逻辑不受影响

3. **YOLO 推理**（postprocess.cc）：
   - 模型输出 cls_id 不变
   - `coco_cls_to_name()` 仍然用于日志打印和 UI 显示

4. **快照机制**（snapshot.cc）：
   - 快照缓冲区逻辑不受影响

### ⚠️ 需要注意的部分

1. **日志打印**：
   - 当前代码用 `coco_cls_to_name()` 打印英文类别名（如 "apple"）
   - 修改后事件上报用中文，但日志仍用英文
   - 这是**可接受的**，因为日志是给人看的，英文更清晰

2. **UI 显示**：
   - 当前 OSD 画面用 `coco_cls_to_name()` 显示类别名
   - 如果也想改成中文，需要同步修改 `main.cc` 中的绘制部分
   - 但这是**可选的**，不影响功能

### ❌ 不需要修改的部分

1. `labels_list.txt` — 保持英文，用于模型推理和日志
2. `postprocess.cc` — `coco_cls_to_name()` 保持不变
3. `session.cc` — 业务逻辑不变
4. `tracker.cc` — 跟踪逻辑不变
5. `snapshot.cc` — 快照机制不变
6. `cloud_uploader.cc` — 不变
7. `cloud_uploader.h` — 不变
8. `inventory.h` — 不变

---

## 五、修改总结

### 需要修改的文件（3个）

| 文件 | 修改内容 | 影响范围 |
|------|---------|---------|
| `fridge_config.h` | 添加 `cls_id_to_chinese()` 函数 | 无影响，纯新增 |
| `main.cc:421` | `coarse_category()` → `cls_id_to_chinese()` | 仅影响事件上报 |
| `inventory.cc:142-174` | 关门库存快照格式调整 | 仅影响关门快照 |

### 不需要修改的文件

- `labels_list.txt` — 保持英文
- `postprocess.cc` — 保持英文
- `session.cc` — 业务逻辑不变
- `tracker.cc` — 跟踪逻辑不变
- `snapshot.cc` — 快照机制不变
- `cloud_uploader.cc` — 不变
- `cloud_uploader.h` — 不变
- `inventory.h` — 不变

### 结论

**只改上报格式，不影响主程序业务逻辑。**

修改范围：
1. `fridge_config.h` — 新增中文映射函数（纯新增，不影响现有代码）
2. `main.cc` — 改一行（事件上报时的 category）
3. `inventory.cc` — 改几行（关门快照格式）

主程序的业务逻辑（快照判断、整理/拿走/放入、stable_frames 机制）完全不受影响。

**关键点**：
- 端侧内部状态（VISIBLE/OCCLUDED/OUT）是端侧自己用的，不应该返回给后台
- 后台返回的库存清单中有 `status`（如"IN_STOCK"），但这是后台的状态，不是端侧的
- 我们只需要按照接口文档的格式来实现输入输出，内部怎么处理是我们自己的事
