# 标签与物品的配对机制

标签本身没有位置信息，位置是通过 YOLO 在物品入库那一刻给的 bbox 提供的。后端用"时间窗自动配对"机制把这两件事缝合到一起。

## 完整流程

```
用户动作                端侧上报                              后端处理
────────────────────────────────────────────────────────────────────
1. 扫标签朝镜头 ──→  POST /events/label_scan              写入 pending_labels
                    { label_image, ttl_seconds: 300 }     （5 分钟内有效）

2. 用户把物品  ──→  POST /events/item                      ① 找最近 5 分钟内
   放进冰箱                                                  未消费的 pending_label
                    {                                     ② 把 label_data 写入
                      event_type: "ITEM_IN",                 inventory.label_data
                      data: [{                            ③ inventory.bbox 来自
                        bbox: [x,y,w,h],  ← 位置在这           本次上报的 bbox
                        crop_image: "..."
                      }]
                    }
```

关键：标签的"位置"和物品的"位置"是同一个东西，都是 ITEM_IN 时 YOLO 算出来的 bbox。后端不要求标签自己带位置。

## 为什么这样设计

LuckFox 上 OCR 跑不动，所以标签照片得发到云端识别。但用户扫标签时，物品还没放进冰箱，端侧不知道它最终会落在哪。等用户真把物品放进去，YOLO 才检测出 bbox。

后端的工作就是把"5 分钟前刚扫过的那张标签" 自动挂到"刚放进来的那件物品"上——两者绑定靠的是同一台设备 + 时间窗，不是位置。

## 端侧需要做的

只有一件事：保证用户扫标签后 5 分钟内必须发 ITEM_IN。具体步骤可以引导用户：

1. 屏幕提示"请把标签朝向摄像头"
2. 端侧拍标签 → 发 /events/label_scan
3. 屏幕提示"标签已识别，请把商品放入冰箱"
4. YOLO 检测到新物体 → 算出 bbox → 发 /events/item ITEM_IN
5. 后端自动配对，inventory 里既有标签信息又有位置信息

不需要端侧自己关联。

## 位置怎么显示

后端 inventory 表里每条记录都有：

```
bbox: [x, y, w, h]                      ← 来自 YOLO
label_data: {brand, expire_date, ...}   ← 来自标签 OCR
category                                ← 来自 YOLO 分类
```

一个 ITEM_IN 之后两边都齐了

前端 /admin/fridge-map 和 /user/fridge-map 直接读 inventory.bbox 在画布上画框，点击该框就能看到对应的标签信息（品牌、保质期等）。

## 5 分钟内没放怎么办

pending_label 自动过期，标签变成"未关联"状态留在 pending_labels 表里。如果用户后来扫了一次新标签，会再产生一条新的 pending_label，旧的等 sweeper 懒清理。这个不会影响 inventory，只是这次没标签信息。

## 一个细节

如果用户连续扫了 3 个不同标签，再连续放入 3 件物品，怎么知道哪个标签对应哪个物品？

后端目前是按时间倒序匹配：每次 ITEM_IN 找"最近的、未消费的、未过期的"那一条，配对成功就把那条标记成 consumed。所以操作顺序就是配对顺序——只要用户按 扫A → 放A → 扫B → 放B 这样交替操作，就能正确对齐。如果是 扫A → 扫B → 扫C → 放X → 放Y → 放Z，则 X 配 C、Y 配 B、Z 配 A（后进先出）。

实际场景中建议引导用户一件一件来，扫完立刻放，避免乱序。
