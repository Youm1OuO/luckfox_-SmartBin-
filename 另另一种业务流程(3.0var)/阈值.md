RECLS_EGG_TO_ORANGE_AREA_RATIO = 0.020f;  // egg 框比这大 → 改 orange
RECLS_ORANGE_TO_EGG_AREA_RATIO = 0.015f;  // orange 框比这小 → 改 egg
我特意让两个阈值留了一段缓冲区（0.015~0.020 之间谁都不改），防止物体在临界点上一帧 egg 一帧 orange 地抖。你调的时候保持“EGG_TO_ORANGE
≥ ORANGE_TO_EGG”就行。0.020 大约是画面的 2%。

apple ↔ onion（按框内颜色，HSV，H:0~179、V:0~255）：
RECLS_DARK_V_MAX     = 90.0f;    // 平均亮度低于此算“暗”
RECLS_BRIGHT_V_MIN   = 110.0f;   // 平均亮度高于此算“亮”
RECLS_PURPLE_H_MIN/MAX = 130/170 // 紫/品红色相区间
RECLS_RED_H_LOW_MAX  = 12.0f;    // 红色相（红在色环两端）
RECLS_RED_H_HIGH_MIN = 168.0f;
RECLS_COLOR_INSET_RATIO = 0.20f; // 采样时框各边向内缩 20%，避开背景
判据：apple 若偏暗或偏紫 → 改 onion；onion 若偏亮且偏红 → 改 apple。你说这个不严重，主要靠调 DARK_V_MAX/BRIGHT_V_MIN 就能控松紧。

bitter_gourd → cabbage（单向，按框长宽比）：
RECLS_BITTER_GOURD_TO_CABBAGE_ENABLED = true;  // 开/关
RECLS_SQUARE_ASPECT_MIN = 0.75f;   // 短边/长边 >= 此值算“接近正方形”→ 判为圆的 cabbage
  逻辑：苦瓜长条(长宽比偏离1:1)、卷心菜圆球(接近1:1)。被识别成 bitter_gourd 的框若接近正方形，
        改成 cabbage。单向、不反向。调大=更严(只非常方才转)，调小=更宽松(略长也转,可能误伤)。
相关类别 ID：CLS_CABBAGE=46(卷心菜)、CLS_BITTER_GOURD=47(苦瓜)、CLS_HAIRY_GOURD=48(节瓜)。
（注：lettuce↔cabbage 的区分暂不做——两者都圆团、长宽比都接近1:1，后处理无可靠特征可分；
  需精确区分应补数据重训模型。）

假 orange 过滤（两个 egg 拼成的长框会被误判成 orange，分数多在 60 左右）：
RECLS_ORANGE_FILTER_ENABLED   = true;   // 开/关这个过滤
RECLS_ORANGE_FILTER_SCORE_MAX = 0.63f;  // 分数 < 此值 才可能被过滤(高分真橙子保留)
RECLS_ORANGE_H_MIN = 8.0f;   // 橙色色相下界(HSV,0~179)
RECLS_ORANGE_H_MAX = 25.0f;  // 橙色色相上界；H 落在[MIN,MAX]=橙(真橙子,保留)，区间外=不橙
  判据(三条同时满足才删框，宁可漏过不可误删真 orange)：cls_id==orange + 分数<0.63 + 颜色不橙。
  顺序：此过滤放在【所有逐框转换之后】(含 egg->orange)，因此对“最终是 orange 的框”都生效——
        无论 YOLO 原生 orange，还是两个 egg 拼成大框被面积规则转出来的假 orange，都能删掉。
        （之前放在转换之前、只拦原生 orange，导致 egg 转来的假 orange 漏网，现已修正。）
  真橙子(颜色橙/高分)会被保住不删。egg<->orange 转换本身仍保持纯面积、不加颜色。
  想调松紧：调 SCORE_MAX(调小=更少框被过滤/更保守)；橙色区间 H_MIN/H_MAX 按实测的真橙子色相微调。

总开关：RECLASSIFY_ENABLED = true，排查问题时置 false 就完全回到 YOLO 原始输出。


2. 决定“哪些框进业务”的阈值在 fridge_config.h:45：
YOLO_OBJECT_SCORE_THRESH = 0.40f;   // 物品的通用阈值
YOLO_HAND_SCORE_THRESH   = 0.30f;   // 手

2b. 单类特殊阈值（比通用物品阈值更高，压掉特定误判）：
MILK_BOX_CLS_ID       = 21;      // milk_box 的 cls_id（与训练 label 对齐）
MILK_BOX_SCORE_THRESH = 0.77f;   // milk_box 专用入库门槛（通用是 0.40）
  // 原因：空透明鸡蛋盒会被误判成 milk_box，误判分数多在 60~76；真 milk_box 正立摆放通常 80+。
  //       订 0.77 把空盒误判(≤0.76)挡在业务层外。代价：真 milk_box 躺放分数可能跌到 60 而被挡，
  //       所以演示时正立摆放 milk_box。想取消这个特殊处理，把 0.77 改回 0.40 即可。
  // 生效点：postprocess.cc 里 yolo_output_score_threshold(cls_id) 逐框过滤，仅影响 milk_box。


3. 类别合并表（可自由增删，只改这张表、不用改代码），在 fridge_config.h：
   RECLS_CLASS_MERGES[] = {
       { CLS_CHINESE_CABBAGE, CLS_LETTUCE },   // 白菜 → 生菜（from 被统一记成 to）
       // 想加就加一行 { CLS_XXX, CLS_YYY }; 想停用就把那行注释掉
   };
   // 填表规则：from≠to；同一个 from 只写一行；不要写链式(别同时 A→B 和 B→C)。
   // 合并只做一轮不连锁。（旧的 RECLS_MERGE_FROM/TO/LETTUCE_CABBAGE 三个常量已被这张表取代）


4. 统一去重（过滤+转换之后的最后一步，去重完交业务）
RECLS_DEDUP_ENABLED     = true;    // 是否启用统一去重
RECLS_OVERLAP_DEDUP_IOU = 0.90f;   // 任意两框(不分类别/不分是否改过) IoU >= 此值 → 留高分删低分
  逻辑：只杀“几乎完全重合、大小也相近”的真重复框（如同位置 apple+onion 双标签，IoU≈0.95）。
  用 IoU(交/并)而非 IoM：一个小物品挡在大物品前面时 IoU 低，不会被误删（IoM 会误删）；
        挨着但不重合的相邻同类物体 IoU 达不到 0.90，也安全。
  调节：调低=更激进(更多框被合并,可能误伤相邻物体)；调高=更保守。默认 0.90 起步，不行再手调。
  （旧的“只对改过类别的框做同类去重(做法A)”已废弃，统一成这一条。）
RECLS_DEDUP_IOM_THRESH  = 0.75f;   // 【注意】不再用于 reclassify 去重；仅被 session_settlement 的
                                   //   补 IN 候选“与已登记框重叠”判据复用，故保留，勿删。


5. 无手期后手矫正（补登记）—— 细节31（都在 fridge_config.h）
  > 一次操作后，把有手期没能定论的入库/出库在无手期补回来。核心钥匙：候选必须落在
  > 本轮“手实际活动过的区域”内（手没去过的地方一律不补）。整盒鸡蛋放入/取走靠它。
FLOW3_NOHAND_CORRECTION_ENABLED = true;  // 总开关；置 false 立即回到"无手不补 IN/OUT"
FLOW3_NOHAND_CORRECT_FRAMES = 3;         // 补 IN(连续稳定)/补 OUT(连续缺失) 需要的帧数; 调小=更快但更容易误补；调大=更稳但更慢
FLOW3_NOHAND_CORRECT_HAND_MARGIN_RATIO = 0.20f;  // 手框外扩比例(各边0.20≈宽高放大40%); 调大=更容易覆盖盒边物品，但纳入更多背景
FLOW3_NOHAND_CORRECT_REGION_IOM        = 0.50f;  // 候选框与"手活动区域"的重叠(IoM)达标线; 调小=更宽松(更容易补)，调大=更严
FLOW3_NOHAND_POSTPROCESS_MAX_FRAMES    = 3;      // 手离开后无手后处理的封顶帧数(v3闸门二,防0/1震荡死循环); 超过此帧数仍未提交就强制让无手纠正接管。调大=给正常收敛更多余地；调小=更快接管但可能打断需多帧的正常场景
  // 说明(v3接管机制): 有手阶段处理不了、卡住的未决状态,不再无限等待。两道闸门任一触发即接管:
  //   闸门一(无进展检测): 一整帧相比上帧毫无推进 → 静止死等 → 立即接管(最快次帧)。
  //   闸门二(上面这个封顶帧数): 兜底"0/1反复横跳"的震荡死循环。
  // 接管后由无手纠正(快照↔库存双向匹配消解)把残余就地了结: 该IN的IN、该OUT的OUT、被别的框
  // 压着(遮挡)的保留不动。