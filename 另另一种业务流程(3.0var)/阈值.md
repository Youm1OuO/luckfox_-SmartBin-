RECLS_EGG_TO_ORANGE_AREA_RATIO = 0.020f;  // egg 框比这大 → 改 orange
RECLS_ORANGE_TO_EGG_AREA_RATIO = 0.015f;  // orange 框比这小 → 改 egg
我特意让两个阈值留了一段缓冲区（0.015~0.020 之间谁都不改），防止物体在临界点上一帧 egg 一帧 orange 地抖。你调的时候保持“EGG_TO_ORANGE
≥ ORANGE_TO_EGG”就行。0.020 大约是画面的 2%。

apple ↔ onion（按框内颜色，HSV，H:0~179、V:0~255）：
RECLS_DARK_V_MAX     = 90.0f;    // 平均亮度低于此算“暗”
RECLS_BRIGHT_V_MIN   = 110.0f;   // 平均亮度高于此算“亮”
RECLS_PURPLE_H_MIN/MAX = 130/170 // 紫/品红色相区间
RECLS_RED_H_LOW_MAX  = 12.0f;    // 红色相（红在色环两端）
RECLS_RED_H_HIGH_MIN = 168.0f;
RECLS_COLOR_INSET_RATIO = 0.20f; // 采样时框各边向内缩 20%，避开背景
判据：apple 若偏暗或偏紫 → 改 onion；onion 若偏亮且偏红 → 改 apple。你说这个不严重，主要靠调 DARK_V_MAX/BRIGHT_V_MIN 就能控松紧。

总开关：RECLASSIFY_ENABLED = true，排查问题时置 false 就完全回到 YOLO 原始输出。


2. 决定“哪些框进业务”的阈值在 fridge_config.h:45：
YOLO_OBJECT_SCORE_THRESH = 0.25f;   // 物品
  YOLO_HAND_SCORE_THRESH = 0.30f;   // 手

3. 白菜/生菜合并：按你要求做成可自由配置，在 fridge_config.h：
RECLS_MERGE_LETTUCE_CABBAGE = true;         // 开/关这个合并
RECLS_MERGE_FROM = CLS_LETTUCE;             // 被替换的类
RECLS_MERGE_TO   = CLS_CHINESE_CABBAGE;     // 替换成的类

4. 纠正后的过滤去重
RECLS_DEDUP_ENABLED    = true;     // 是否启用纠正后同类去重
RECLS_DEDUP_IOM_THRESH = 0.75f;    // IoM 超过此值且同类，去重(保留高分框)