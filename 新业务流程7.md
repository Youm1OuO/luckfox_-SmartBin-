新业务流程7
=====================================================================


一、核心结论
---------------------------------------------------------------------

这个版本是在新业务流程6的基础上，专门针对“整理/移动容易被误判成放入同类新物品”的问题做一次流程重排。

新业务流程6 的核心问题不是“稳定快照对比”本身错误，而是整理事件的处理优先级不够合理：
    1. 先用 strict_origin_match 把 stable_snapshot 中原位置仍然匹配到的物品锚定为“没动”
    2. 再只从 disappeared_objects 中寻找整理源
    3. 如果移动过程中旧位置存在 YOLO 残影、局部误检、同类重叠框，
       物品就不会进入 disappeared_objects
    4. 新位置的同类物品会落入 appeared_objects
    5. 最终容易被误判成“旧物还在 + 新物放入”

新业务流程7 的核心原则：
    【整理】是过程事件，不能只依赖两份稳定快照的“结果差异”来反推。

    稳定快照仍然重要，但它的职责调整为：
        1. 确认最终画面稳定
        2. 确认物品最终落点
        3. 处理普通拿走、放入、遮挡、露出
        4. 作为 ByteTrack / HELD 轨迹的最终校验

    ByteTrack / HELD / 手轨迹的职责调整为：
        1. 先找出本轮被操作过、可能移动过的库存 item
        2. 先为这些 item 做 relocation settlement
        3. 防止它们被 strict_origin_match 过早锚定在旧位置
        4. 防止它们的新位置被误判成新放入


新业务流程7 的一句话总结：
    对被手碰过、HELD 过、或 ByteTrack 明确移动过的 item，
    先执行“整理结算”，再执行普通快照差异。


整体优先级：
    1. 开门初始化库存
    2. 每帧记录 ByteTrack / 手 / HELD / 轨迹证据
    3. 等待下一份无手稳定快照
    4. 先处理 pending_relocation
    5. 先结算本轮被操作过的 item
    6. 再处理普通 stable_snapshot diff
    7. 最后处理遮挡 / 露出 / 清理


重要边界：
    本版本不是纯 ByteTrack 流程。

    原因：
        冰箱场景中，物品被手拿起后经常被遮挡，YOLO 和 ByteTrack 都可能断轨。
        如果只依赖 ByteTrack，整理会漏判很多。

    本版本也不是纯快照流程。

    原因：
        整理是过程事件，稳定快照只看到结果。
        如果旧位置残留同类框，纯快照很容易把移动拆成“旧物还在 + 新物放入”。

    所以本版本采用：
        ByteTrack / HELD 先建立移动候选
        stable_snapshot 确认最终落点
        普通快照 diff 处理剩余未操作物品


继续保留的重要业务前提：
    【遮挡】不能直接变成【出库】。

也就是说：
    一个物品要出库，通常必须先处于【可见】状态。
    状态路径应该是：
        【遮挡】 -> 【可见】 -> 【出库】

这个规则仍然用于避免 YOLO 漏检、遮挡变化、光照变化导致库存错误减少。




二、状态定义
=====================================================================

物品状态仍然分为三类：
    【可见】
        当前可以被 YOLO 稳定识别，业务上认为物品在冰箱中。

    【遮挡】
        当前看不到或不稳定可见，但业务上认为物品仍在冰箱中。

    【出库】
        物品已经离开冰箱。


注意：
    【可见】和【遮挡】都算在库存中。
    【出库】不算在当前库存中，但保留一段时间的历史信息，用于误判恢复和放回匹配。


状态转换规则：
    【可见】 -> 【遮挡】
        物品被其他物品挡住，或者被移动物品压住。

    【遮挡】 -> 【可见】
        原本被挡住的物品重新露出来，并被 YOLO 稳定识别。

    【可见】 -> 【出库】
        原本可见的物品消失，并且没有遮挡证据、没有整理证据、没有 pending 证据。

    【出库】 -> 【可见】
        出库历史中的物品又被检测到，说明它被放回，或者之前是误出库。


禁止状态转换：
    【遮挡】 -> 【出库】

原因：
    遮挡物品本来就不可见。
    如果允许它直接出库，YOLO 漏检、遮挡变化、反光、光照变化都可能导致错误删除库存。


新增概念：操作锁定状态 operation_locked
---------------------------------------------------------------------

operation_locked 不是库存状态。
它只是本轮 diff 过程中的临时标记。

如果一个 item 满足以下任意条件，就可以进入 operation_locked：
    1. 被手 bbox 明显覆盖过
    2. 进入 candidate_held_items
    3. 进入 confirmed_held_items
    4. ByteTrack 显示它发生明显位移
    5. 它关联的 track 与手发生遮挡后断开
    6. 它存在 pending_relocation

operation_locked 的作用：
    1. 不允许 strict_origin_match 过早把它判定为“没动”
    2. 不允许它的新位置直接进入【放入】
    3. 先进入 relocation settlement
    4. relocation settlement 结束后，再决定它是否仍然可见、整理、遮挡或出库

注意：
    operation_locked 只在本轮 baseline_snapshot -> stable_snapshot 的裁决中有效。
    本轮裁决结束后清空。




三、事件定义
=====================================================================

物品状态是库存结果。
事件是用户操作过程。

本流程主要识别以下事件：
    【拿走】
        原本可见的库存物品不再出现在冰箱中。

    【放入】
        一个稳定出现的新物品，无法匹配当前库存、出库历史、整理候选和 pending 候选。

    【整理】
        一个库存物品从位置 X 移动到位置 Y，仍然在冰箱中。

    【遮挡】
        一个原本可见的物品，因为另一个物品出现或移动到它上方，变成遮挡。

    【露出】
        一个原本遮挡的物品，因为上方物品被拿走或移开，重新变成可见。

    【忽略抖动】
        检测变化不满足稳定条件，或者与库存状态冲突，暂不修改库存。

    【待确认整理】
        已经有整理迹象，但证据不足，不立即提交整理，也不拆成出库 + 放入。


事件和状态的关系：
    【拿走】
        【可见】 -> 【出库】

    【放入】
        新建库存物品，状态为【可见】

    【整理】
        item_id 不变，状态保持【可见】，更新 bbox / track_id / appearance_feature

    【遮挡】
        【可见】 -> 【遮挡】

    【露出】
        【遮挡】 -> 【可见】


重要约束：
    对同一个真实物品，一次操作中不能同时提交：
        【整理】和【放入】
        【整理】和【出库】
        【遮挡】和【放入同类新物品】

如果出现这些冲突，应优先降级为 pending 或忽略抖动，而不是直接修改库存数量。


3.1 全局交接规则
---------------------------------------------------------------------

本流程最容易出错的地方，不是单个判断条件，而是各步骤之间的交接。

因此规定以下全局规则：

    1. 每个 stable_snapshot object 在一轮 diff 中最多只能被一个事件消费。

    2. 每个库存 item 在一轮 diff 中最多只能产生一个主事件：
        整理 / 拿走 / 遮挡 / 露出 / 忽略抖动 只能选一个主结果。

    3. 如果某个 item 已经 confirmed_relocation：
        它不能再进入普通 disappeared_objects。
        它的新位置 B 不能再进入 appeared_objects。

    4. 如果某个 item 进入 pending_relocation：
        A 和 B 都必须冻结。
        A 不能判出库。
        B 不能判放入。

    5. 如果某个 stable object 可以解释为“旧物整理落点”：
        即使证据暂时不够强，也不能立刻判为【放入】。
        应优先 pending。

    6. 如果某个旧位置框可能是移动残影：
        它不能直接创建新库存。
        必须经过连续稳定确认，或者能匹配另一个真实库存 item。

    7. 如果多个判断分支给出冲突结果：
        优先级为：
            confirmed_relocation
            pending_relocation
            occluded / revealed
            out / in
            ignore_jitter

        也就是说，能解释成整理的，先不要解释成放入。


本轮 diff 中建议维护以下临时集合：
    consumed_snapshot_object_ids
        已经被事件消费的 stable_snapshot 物品。

    settled_item_ids
        已经完成本轮结算的库存 item。

    pending_reserved_item_ids
        被 pending_relocation 冻结的库存 item。

    pending_reserved_snapshot_object_ids
        被 pending_relocation 冻结的 stable_snapshot 物品。

    residual_snapshot_object_ids
        暂时认为可能是残影、重复框、局部误检的 stable_snapshot 物品。

这些集合只在本轮 diff 内有效。
本轮 diff 结束后清空，不能写入最终库存状态。




四、身份匹配与事件匹配标准
=====================================================================

本版本继续使用分层匹配，但调整使用顺序。

四类匹配：
    1. snapshot_cluster_match
        用于多帧快照内部聚合。

    2. strict_origin_match
        用于判断未被操作物品是否仍在原位置。

    3. reid_match
        用于判断两个不同位置物品外观是否相似。

    4. relocation_match
        用于判断一个库存 item 是否完成整理。


最重要的变化：
    strict_origin_match 不再无条件优先于整理判断。

    对 operation_locked item：
        不先执行 strict_origin_match 锚定。
        先进入 relocation settlement。

    对普通未操作 item：
        仍然可以先执行 strict_origin_match。



4.1 快照内聚合匹配 snapshot_cluster_match
---------------------------------------------------------------------

用途：
    多帧生成一个稳定快照时，把 N 帧里的同一个物品合并到一起。

要求：
    不能太严格。
    因为 YOLO 框每帧都会抖动，移动过程中 bbox 变化更大。

建议条件：
    1. 类别一致
    2. 中心点距离小于较宽阈值
    3. bbox 面积比例相近
    4. IoU 或 overlap 有一定重叠
    5. 同一帧内同一个 cluster 最多接收一个 detection


风险：
    移动物品时，快照窗口如果混入移动过程帧，
    同一个真实物品可能形成两个 cluster：
        旧位置残影 cluster
        新位置真实 cluster

因此：
    snapshot_cluster_match 不能单独决定库存增减。
    它只是生成稳定快照对象。



4.2 原位严格匹配 strict_origin_match
---------------------------------------------------------------------

用途：
    判断某个未被操作的库存物品是否仍在原位置。

适用场景：
    1. 初始化时，后台库存与第一份稳定快照对齐
    2. 普通未操作物品的原位更新
    3. 判断遮挡物品是否在原位置露出
    4. 判断 YOLO 抖动是否可以忽略

建议条件：
    1. 类别一致
    2. 中心距离足够近
    3. 面积差异足够小
    4. IoU 足够高
    5. 外观差异在允许范围内


新业务流程7 的重要限制：
    如果 item 是 operation_locked，
    strict_origin_match 不能直接把它判定为“没动”。

原因：
    移动过程中旧位置可能存在：
        1. YOLO 残影
        2. 局部误检
        3. 被遮挡后的相似框
        4. 同类物品重叠
        5. snapshot_cluster 聚合残留

这些都可能让旧位置看起来“还在”，但真实物品已经移动。



4.3 外观重识别匹配 reid_match
---------------------------------------------------------------------

用途：
    判断两个不同位置的物品看起来是否像同一个物品。

注意：
    reid_match 只能回答：
        “A 和 B 看起来像不像？”

    不能单独回答：
        “A 是否被整理到了 B？”

原因：
    同类物品往往外观接近。
    尤其是 apple / orange / soda_can 这类类别，只有 bbox、面积、分数是不够的。


建议第一版 reid_score 包含：
    1. 类别一致
    2. bbox 面积比例
    3. bbox 宽高比
    4. YOLO score 接近程度
    5. 简单颜色 / patch 特征

如果后续条件允许，应逐步加入：
    1. HSV 直方图
    2. 分块颜色特征
    3. 轻量 ReID embedding
    4. 物品历史 appearance_feature 平滑更新



4.4 整理判定 relocation_match
---------------------------------------------------------------------

relocation_match 是事件级判断，不是普通身份匹配。

用途：
    判断库存 item A 是否从旧位置移动到了 stable_snapshot 中的候选位置 B。

输入：
    A                         # 库存 item
    old_bbox                  # A 在 baseline_snapshot / last_visible_bbox 中的位置
    B                         # stable_snapshot 中的候选物品
    operation_context         # 本轮操作证据
    candidate_set             # 同类候选集合


基础条件：
    1. A 和 B 类别一致
    2. B 未被其他已确认事件消费
    3. B 与 A 的外观 reid_score 足够高
    4. A 有操作证据，或者 A 确实从原位置消失
    5. 候选唯一性足够好


强证据：
    1. 同一个 ByteTrack track_id 从 A 附近连续移动到 B 附近
    2. TrackEvidence 起点靠近 A，终点靠近 B
    3. 轨迹曾被手遮挡，但断点前后方向和位置能接上
    4. HeldProxyEvidence 的手代理轨迹从 A 附近移动到 B 附近
    5. A 是 confirmed_held，并且手释放点靠近 B


中等证据：
    1. A 是 candidate_held
    2. A 附近曾出现手，且 stable_snapshot 中出现同类新位置
    3. A 的物体 track 明显移动，但没有完整到达 B
    4. A 的 track 断开，B 附近出现同类新 track
    5. A 与 B 外观强相似，但轨迹不完整


负证据：
    1. A 的 track 明确持续停留在旧位置
    2. A 的 track 明确移动到了 B 以外的位置
    3. B 在 A 被操作前就已经稳定存在
    4. B 已被另一个高置信 item 占用
    5. 多个同类 item 都能同等匹配 B，候选不唯一


判定结果：
    confirmed_relocation：
        外观分数高
        有强移动 / HELD / 手代理证据
        候选唯一
        无明显负证据

    low_confidence_relocation：
        外观分数高
        有中等证据
        但轨迹不完整或候选不够唯一

    no_relocation：
        只有外观相似，没有移动证据
        或存在强负证据


执行原则：
    confirmed_relocation：
        立即提交【整理】，更新旧 item 到 B 位置。

    low_confidence_relocation：
        进入 PendingRelocation。
        不提交整理。
        不提交出库。
        不提交放入。

    no_relocation：
        交给后续普通 diff 逻辑处理。



五、“附近”的量化方法
=====================================================================

继续使用自适应归一化中心距离。

公式：
    normalized_distance = center_distance(A, B) / min(diagonal(A), diagonal(B))

其中：
    center_distance(A, B) = 两个 bbox 中心点距离
    diagonal(A) = sqrt(width^2 + height^2)


建议含义：
    < 0.5
        几乎在同一位置。

    0.5 ~ 1.0
        紧挨着，仍然可以认为有关联。

    > 1.0
        空间关系较弱。


不同用途使用不同阈值：
    strict_origin_match：
        应该严格。

    遮挡判断：
        可以使用 overlap_ratio_of_smaller 或 IoU。

    relocation_match：
        不要求新旧位置附近。
        因为整理可能移动很远。

    held_proxy / track endpoint 判断：
        需要判断轨迹终点是否靠近候选 B。




六、数据结构建议
=====================================================================


6.1 库存物品 InventoryItem
---------------------------------------------------------------------

每个库存物品至少保存：
    item_id
    class_id
    class_name
    status                    # visible / occluded / out
    bbox                      # 当前最新可靠 bbox
    last_visible_bbox         # 最近一次可见 bbox
    appearance_feature        # 颜色 / patch / 直方图 / ReID 特征
    track_id                  # 最近关联的 ByteTrack ID
    score
    created_frame_id
    updated_frame_id
    last_seen_snapshot_id
    last_seen_frame_id
    occluded_by_item_id       # 可选
    operation_locked          # 本轮临时标记，不持久化


注意：
    operation_locked 不属于最终库存状态。
    它只影响本轮 diff 的处理优先级。



6.2 快照物品 SnapshotObject
---------------------------------------------------------------------

每个快照物品至少保存：
    snapshot_object_id
    class_id
    class_name
    bbox
    appearance_feature
    score
    count_in_window
    source_frame_ids
    track_id                  # 如果可关联
    cluster_motion_hint       # 可选，快照窗口内聚合是否发生明显移动


建议：
    如果一个 snapshot object 来自明显移动的 cluster，
    应记录 first_bbox / last_bbox。
    这样后续可以判断它是旧位置残影，还是最终落点。



6.3 快照 Snapshot
---------------------------------------------------------------------

每个快照至少保存：
    snapshot_id
    frame_range
    objects
    hand_present
    created_at



6.4 操作上下文 OperationContext
---------------------------------------------------------------------

OperationContext 的职责：
    记录 baseline_snapshot 到下一份 stable_snapshot 之间发生过的操作证据。

它不直接修改库存。
它不直接提交事件。
它为 relocation settlement 提供证据。


每次 baseline_snapshot 更新后，创建或重置一份 OperationContext：
    context_id
    baseline_snapshot_id
    hand_seen
    hand_frame_count
    hand_track_ids
    hand_long_present

    touched_item_ids              # 被手碰过/覆盖过的 item
    candidate_held_items
    confirmed_held_items
    held_proxy_evidences

    active_track_evidences
    moving_tracks
    moved_item_candidates

    operation_locked_items        # 本轮应优先做整理结算的 item
    negative_track_evidences      # 明确反证

    unstable_frame_count
    created_frame_id
    updated_frame_id


operation_locked_items 的来源：
    1. confirmed_held_items
    2. candidate_held_items
    3. moved_item_candidates
    4. 被手 bbox 覆盖过的 visible item
    5. 有 track 起点靠近库存 item 且发生明显位移
    6. pending_relocation 中的 item


重要约束：
    OperationContext 的生命周期是：
        baseline_snapshot -> 下一份 stable_snapshot

    不是：
        手进入 -> 手离开

原因：
    手可能多次进出。
    物品可能在多次短操作中完成一次最终整理。
    最终仍然需要用下一份无手稳定快照确认落点。



6.5 RelocationCandidate
---------------------------------------------------------------------

RelocationCandidate 是本轮整理结算的内部候选。

每条候选至少保存：
    item_id_A
    class_id
    old_bbox
    candidate_snapshot_object_B
    candidate_new_bbox
    reid_score
    track_score
    held_score
    hand_score
    negative_score
    total_score
    candidate_rank
    decision                  # confirmed / low_confidence / no


来源：
    对每个 operation_locked item A：
        从 stable_snapshot 中找所有同类候选 B。

候选 B 可以是：
    1. A 原位置附近的快照物品
    2. A 新位置附近的快照物品
    3. 远距离但被 track / HELD 指向的快照物品


注意：
    对 operation_locked item，不能只找 appeared_objects。
    因为 old position 可能存在残影，导致传统 appeared/disappeared 分组失效。



6.6 PendingRelocation
---------------------------------------------------------------------

PendingRelocation 用来保存低置信整理候选。

它必须跨 stable_snapshot 保留，不能只放在 OperationContext 中。

每条 PendingRelocation 至少保存：
    pending_id
    item_id_A
    class_id
    old_bbox
    candidate_new_bbox
    snapshot_object_B
    reid_score
    evidence_summary
    created_snapshot_id
    last_checked_snapshot_id
    stable_count
    expire_after_stable_count
    status                    # pending / confirmed / cancelled / expired


pending 期间：
    A 不判出库。
    B 不判放入。
    A / B 都标记为 pending_reserved。


下一份 stable_snapshot 到来后：
    如果 B 仍稳定存在，且 A 没有高置信回到旧位置：
        可以确认整理。

    如果 A 明确回到旧位置，且 B 消失：
        取消 pending。

    如果 A 和 B 都存在，但证据冲突：
        继续 pending 或取消，不能直接新增库存。

    如果超过确认窗口：
        取消 pending，回退到最新快照重新判断。



6.7 HeldProxyEvidence
---------------------------------------------------------------------

HELD 的核心目的：
    不是直接判断拿走。
    而是在物品被手遮挡后，用手轨迹临时代理物品轨迹。


每条 HeldProxyEvidence 至少保存：
    item_id
    original_object_track_id
    held_by_hand_track_id
    last_visible_bbox
    hand_bbox_at_hold_start
    object_to_hand_offset
    proxy_trajectory_points
    held_start_frame_id
    held_end_frame_id
    release_candidate_bbox
    confidence


生成条件：
    1. 手 bbox 与可见物品 A 明显重叠
    2. A 后续持续不可见，或 A 的物体 track 断开
    3. 手 track 后续仍然存在并移动


使用方式：
    如果 stable_snapshot 中有同类候选 B，
    且 B 与 A 外观相似，
    且 A 的 proxy_trajectory 终点靠近 B，
    则强烈提高 relocation_match(A, B) 的置信度。


注意：
    不要把物品的 track_id 改成手 track_id。
    手轨迹只是代理证据，不是物品身份。



6.8 TrackEvidence
---------------------------------------------------------------------

ByteTrack 的 track_id 是临时轨迹 ID，不是库存 item_id。

每条 TrackEvidence 至少保存：
    track_id
    class_id
    associated_item_id
    start_bbox
    end_bbox
    trajectory_points
    start_frame_id
    end_frame_id
    lost_frame_count
    occluded_by_hand
    confidence


使用原则：
    track 连续从 A 到 B：
        强证据。

    track 中途断开：
        不否定整理。
        可作为中等证据。

    track_id 不一致：
        不否定整理。
        只说明没有强连续证据。

    track 明确停在旧位置或移动到别处：
        负证据。



6.9 DiffWorkingSet
---------------------------------------------------------------------

DiffWorkingSet 是 diff_snapshots_v7 内部使用的一轮临时工作集。

它不是持久化数据。
它只负责让各个步骤之间交接清楚，避免同一个 item 或 snapshot object 被重复处理。


建议字段：
    consumed_snapshot_object_ids
        已经被 confirmed_relocation、露出、放回、放入等事件消费的 stable_snapshot object。

    settled_item_ids
        已经完成本轮结算的库存 item。

    operation_locked_item_ids
        本轮需要优先进行整理结算的库存 item。

    pending_reserved_item_ids
        进入 pending_relocation 的库存 item。

    pending_reserved_snapshot_object_ids
        进入 pending_relocation 的 stable_snapshot object。

    residual_snapshot_object_ids
        疑似残影、重复框、局部误检，不允许本轮直接入库。

    same_position_pairs
        普通未操作 item 的原位匹配结果。

    disappeared_objects
        普通未操作 item 中，旧快照有、新快照原位没有的对象。

    appeared_objects
        stable_snapshot 中剩余未消费、未冻结、未匹配的对象。

    confirmed_relocations
        本轮已经确认的整理事件。

    low_confidence_conflicts
        同类冲突、残影冲突、候选不唯一等低置信冲突记录。


使用原则：
    1. 任何步骤消费 stable_snapshot object 后，必须写入 consumed_snapshot_object_ids。

    2. 任何步骤完成库存 item 的主结果后，必须写入 settled_item_ids。

    3. 后续步骤必须跳过 consumed / settled / pending_reserved 对象。

    4. residual_snapshot_object_ids 不能在本轮进入【放入】。
       它只能等待下一份 stable_snapshot 继续确认。

    5. DiffWorkingSet 是实现新业务流程7的关键。
       如果没有这个工作集，流程很容易退化成“整理分支处理完，后面放入分支又重复处理”的旧问题。




七、初始化流程（开门时）
=====================================================================

开门时：
    1. 向后台请求上一轮库存
    2. 等待第一份无手稳定快照
    3. 如果后台库存可信，用后台库存与快照对齐
    4. 如果后台库存不可信，用第一份稳定快照初始化
    5. 设置 baseline_snapshot
    6. 创建 OperationContext


初始化匹配规则：
    如果后台库存有可靠 bbox：
        当前快照能 strict_origin_match：
            状态设为【可见】
            更新 bbox / score / appearance_feature

        当前快照不能匹配：
            状态设为【遮挡】
            保留后台 bbox 和 appearance_feature


如果后台库存为空但不权威：
    不要认为冰箱一定为空。
    等第一份无手稳定快照初始化。


如果后台明确权威为空：
    可以用空库存初始化。
    但第一份稳定快照中出现的物品仍应进入待确认新物品流程。




八、快照生成流程
=====================================================================

快照仍然由 N 帧投票生成，不使用单帧。

流程：
    1. 连续采集 N 帧 YOLO 检测
    2. 每帧送入 ByteTrack
    3. 用 snapshot_cluster_match 聚合 N 帧中的物品
    4. count >= N * s 的 cluster 保留
    5. 生成 Snapshot


手的规则：
    如果快照窗口中有足够稳定的手检测，
    该 Snapshot 标记 hand_present = true。

hand_present = true 的快照：
    1. 不参与库存 diff
    2. 不更新 baseline_snapshot
    3. 不更新 contrast_snapshot
    4. 只让 OperationContext 记录手和轨迹证据


重要风险：
    移动过程中如果手漏检，移动帧可能进入无手快照。
    这会导致同一个真实物品在 stable_snapshot 中形成两个同类对象。

因此后续 diff 不能只靠 appeared/disappeared。
对 operation_locked item 必须先做 relocation settlement。




九、稳定快照选择
=====================================================================

变量：
    baseline_snapshot
        上一次已经完成库存裁决的无手稳定快照。

    contrast_snapshot
        用来判断画面是否稳定的无手候选快照。

    current_snapshot
        当前刚生成的快照。

    stable_snapshot
        新确认的无手稳定快照。

    operation_context
        baseline_snapshot 到 stable_snapshot 之间的操作证据。


流程：
    1. 初始化后：
        baseline_snapshot = 初始稳定快照
        contrast_snapshot = 初始稳定快照
        operation_context = new OperationContext(baseline_snapshot)

    2. 每生成一份 current_snapshot：
        如果 current_snapshot.hand_present = true：
            丢弃该快照，不参与库存 diff。
            baseline_snapshot 不变。
            contrast_snapshot 不变。
            OperationContext 继续收集证据。

    3. 如果 current_snapshot 与 contrast_snapshot 差异较大：
            说明画面仍在变化。
            contrast_snapshot = current_snapshot。
            暂不裁决库存。

    4. 如果 current_snapshot 与 contrast_snapshot 差异较小：
            说明画面稳定。
            stable_snapshot = current_snapshot。
            执行 diff_snapshots_v7(
                baseline_snapshot,
                stable_snapshot,
                operation_context
            )

    5. 裁决完成后：
            baseline_snapshot = stable_snapshot
            contrast_snapshot = stable_snapshot
            清空并重建 OperationContext


注意：
    稳定快照仍然是最终库存裁决的时间点。
    但整理相关 item 在 diff 内部必须优先结算。




十、快照差异判断 v7
=====================================================================

统一入口：
    events = diff_snapshots_v7(
        baseline_snapshot,
        stable_snapshot,
        operation_context
    )


新业务流程7 与新业务流程6 的关键差异：
    新业务流程6：
        先做 strict_origin_match
        得到 same / disappeared / appeared
        只从 disappeared 中找整理

    新业务流程7：
        先找 operation_locked item
        先为这些 item 做 relocation settlement
        再对剩余未操作物品做 strict_origin_match 和普通 diff


处理步骤：
    第零步：创建 DiffWorkingSet
    第一步：处理历史 pending_relocation
    第二步：建立 operation_locked_items
    第三步：优先结算 operation_locked_items 的整理
    第四步：对剩余物品执行普通快照 diff
    第五步：批量处理遮挡 / 露出
    第六步：处理剩余新出现物品
    第七步：例行清理



10.0 第零步：创建 DiffWorkingSet
---------------------------------------------------------------------

进入 diff_snapshots_v7 后，先创建一份新的 DiffWorkingSet。

初始化内容：
    1. consumed_snapshot_object_ids = empty
    2. settled_item_ids = empty
    3. operation_locked_item_ids = empty
    4. pending_reserved_item_ids = pending_relocation 中仍有效的 item
    5. pending_reserved_snapshot_object_ids = empty
    6. residual_snapshot_object_ids = empty
    7. confirmed_relocations = empty
    8. low_confidence_conflicts = empty


同时建立基础索引：
    1. baseline_snapshot_object -> inventory_item
    2. stable_snapshot_object -> track_id
    3. track_id -> TrackEvidence
    4. item_id -> OperationContext 证据
    5. class_id -> stable_snapshot objects


注意：
    这一步不修改库存。
    这一步只是为了让后续各阶段能共享同一套 consumed / settled / pending 状态。



10.1 第一步：处理历史 pending_relocation
---------------------------------------------------------------------

对每个 pending_relocation：
    1. 在 stable_snapshot 中寻找 candidate_new_bbox 附近的同类物品 B
    2. 检查旧位置 old_bbox 是否高置信回到原位
    3. 检查 B 是否仍稳定存在


如果 B 稳定存在，且 A 没有明确回到旧位置：
    确认【整理】
    更新 item_id_A 到 B 的位置
    B 标记为 consumed
    A 标记为 settled


如果 A 明确回到旧位置，且 B 不稳定：
    取消 pending
    不提交事件


如果 A 和 B 都存在：
    说明可能存在残影、同类重叠或误检。
    不要直接放入 B。
    根据轨迹证据决定：
        证据强 -> 确认整理到 B，旧位置对象视为残影或其他库存物品
        证据弱 -> 继续 pending 或记录低置信冲突


如果超过确认窗口：
    取消 pending。
    释放候选，交给本轮最新 diff 重新判断。



10.2 第二步：建立 operation_locked_items
---------------------------------------------------------------------

operation_locked_items 来源：
    1. operation_context.confirmed_held_items
    2. operation_context.candidate_held_items
    3. operation_context.moved_item_candidates
    4. touched_item_ids
    5. pending_relocation 中尚未处理完成的 item
    6. TrackEvidence 起点靠近某个库存 item 且发生明显位移


对 operation_locked_items 执行以下规则：
    1. 不先进入 same_position_pairs
    2. 不先被 strict_origin_match 锚定为“没动”
    3. stable_snapshot 中的同类对象都可作为候选落点
    4. 候选可以包括旧位置对象和新位置对象


原因：
    被操作过的物品旧位置可能存在残影。
    如果先把旧位置残影当成原物品，就会导致新位置被误判为放入。



10.3 第三步：优先结算 operation_locked_items 的整理
---------------------------------------------------------------------

对每个 operation_locked item A：
    1. 找 A 的 old_bbox
        优先使用 baseline_snapshot 中的 bbox
        其次使用 InventoryItem.last_visible_bbox

    2. 从 stable_snapshot 中找所有同类候选 B
        不要求 B 必须是 appeared_object
        不要求 A 原位置必须已经消失

    3. 对每个 B 计算：
        reid_score(A, B)
        track_score(A -> B)
        held_score(A -> B)
        hand_score(A -> B)
        negative_score(A -> B)
        total_score(A -> B)

    4. 选择最佳候选 best_B
        同时计算 second_best

    5. 执行 relocation_match(A, best_B)


判定：
    confirmed_relocation：
        更新 A 到 best_B 的位置。
        best_B 标记 consumed。
        A 标记 settled。
        记录【整理】事件。

    low_confidence_relocation：
        创建 PendingRelocation。
        A 和 best_B 标记 pending_reserved。
        本轮不判 A 出库。
        本轮不判 best_B 放入。

    no_relocation：
        A 暂不结算。
        后续进入普通 diff。


关于旧位置仍有同类框：
    如果 best_B 是新位置，
    但 stable_snapshot 中旧位置也有一个同类框：
        旧位置框不能直接绑定 A。
        需要判断它是：
            1. YOLO 残影
            2. 被遮挡后的局部误检
            3. 另一个真实同类物品

    判断依据：
        1. 旧位置框是否有稳定 track
        2. 旧位置框是否在 A 被操作期间一直存在
        3. 旧位置框与其他库存 item 是否匹配
        4. A 的 track / HELD 终点是否明确指向 best_B

    如果无法确认旧位置框是另一个真实物品：
        不新增库存。
        记录低置信残影或忽略抖动。


关于多个 item 同时整理：
    必须先为所有 operation_locked item 建立候选关系，
    再统一求解匹配。

    不要在确认第一个整理后，马上把它覆盖到的另一个 operation_locked item 判成遮挡。

    正确处理顺序：
        1. 批量建立所有移动候选
        2. 批量确认高置信整理
        3. 标记 consumed / settled
        4. 再处理剩余未 settled 物品的遮挡和露出

原因：
    同时整理 apple 和 orange 时，
    apple 的新位置可能覆盖 orange 的旧位置。
    如果先处理 apple，再马上把 orange 判遮挡，
    orange 自己的整理就会被打断。


批量匹配建议：
    对所有 operation_locked item A 和所有同类候选 B 建立候选边：
        edge(A, B)

    每条边计算：
        total_score =
            reid_score * W_reid +
            track_score * W_track +
            held_score * W_held +
            hand_score * W_hand -
            negative_score * W_negative

    然后按以下规则求解：
        1. 每个 A 最多匹配一个 B
        2. 每个 B 最多匹配一个 A
        3. confirmed 边优先于 low_confidence 边
        4. total_score 最高的唯一候选优先
        5. 如果两个 A 抢同一个 B，且分差不够大：
            不确认，进入 pending 或 low_confidence_conflicts
        6. 如果一个 A 有多个 B，且 best 与 second_best 分差不够大：
            不确认，进入 pending


第一版实现可以不用复杂算法。
可以使用排序贪心：
    1. 把所有候选边按 total_score 从高到低排序
    2. 依次取边
    3. 如果 A 和 B 都未被占用，且满足 confirmed 条件，则确认
    4. 如果候选冲突或分差不足，则 pending

后续如果同类物品很多，再考虑二分图匹配。


不允许的做法：
    不能遍历到第一个 A 就立刻修改库存，
    然后用修改后的库存去判断第二个 A。

原因：
    这样会让处理顺序影响结果。
    同时整理多个物品时，会出现先处理者把后处理者判成遮挡的问题。



10.4 第四步：对剩余未操作物品执行普通快照 diff
---------------------------------------------------------------------

在 operation_locked_items 结算完成后，
对剩余未 consumed 的 baseline objects 和 stable objects 执行普通 diff。


建立三类集合：
    same_position_objects
        未操作物品在原位置 strict_origin_match 成功。

    disappeared_objects
        未操作物品在 stable_snapshot 中找不到原位匹配。

    appeared_objects
        stable_snapshot 中未被 consumed、未被 pending_reserved、未匹配库存的物品。


规则：
    1. same_position_objects
        更新库存 bbox / score / appearance_feature。

    2. disappeared_objects
        进入拿走 / 遮挡判断。

    3. appeared_objects
        进入露出 / 放回 / 放入判断。


注意：
    operation_locked item 如果已经 settled 或 pending_reserved，
    不再进入普通 disappeared / appeared 处理。



10.5 第五步：处理遮挡 / 露出
---------------------------------------------------------------------

遮挡 / 露出应该在整理候选结算之后处理。

原因：
    移动物品的新位置可能覆盖另一个物品旧位置。
    但被覆盖的物品也可能正在被同时整理。
    如果过早判遮挡，会打断它自己的整理判断。


遮挡判断：
    对剩余 disappeared_objects 中的物品 C：
        如果 C 是 operation_locked 且仍有未决移动候选：
            不判遮挡。
            继续 pending 或等待下一份 stable_snapshot。

        如果 C 不是 operation_locked，
        且 stable_snapshot 中有其他已确认可见物品覆盖 C 的旧位置：
            C 状态设为【遮挡】。
            记录【遮挡】事件。


露出判断：
    如果某个原本【遮挡】物品 D 在 stable_snapshot 中出现，
    并且 D 与库存中遮挡物品匹配：
        D 状态设为【可见】。
        更新 bbox / appearance_feature。
        记录【露出】事件。


整理导致的露出：
    如果 item A 从旧位置离开，
    旧位置附近出现了原本遮挡的 D：
        D 设为【可见】。


整理导致的遮挡：
    如果 item A 的新位置覆盖了未操作物品 C：
        C 设为【遮挡】。

    但如果 C 也是 operation_locked：
        不立即判遮挡。
        先看 C 是否有自己的整理落点。



10.6 第六步：处理剩余新出现物品
---------------------------------------------------------------------

对剩余 appeared_objects 中的物品 B：
    1. 如果 B 被 pending_relocation 保留：
        跳过，不放入。

    2. 如果 B 能匹配本地【遮挡】物品：
        认为旧物露出。
        状态改为【可见】。

    3. 如果 B 能匹配 out_stock_archive：
        认为放回或误出库恢复。
        状态改为【可见】。

    4. 如果 B 与某个 operation_locked item 有较强关系：
        不直接放入。
        创建 pending_relocation 或记录低置信冲突。

    5. 如果以上都不满足：
        才新建库存。
        记录【放入】事件。


新增库存必须满足：
    1. B 未被任何整理候选消费
    2. B 未被 pending 保留
    3. B 不能匹配当前库存
    4. B 不能匹配出库历史
    5. B 不能解释为 operation_locked item 的新落点


提交【放入】前必须通过以下检查：
    1. 同类别 operation_locked item 数量为 0；
       或者这些 operation_locked item 都已经 settled，且没有把 B 作为候选。

    2. 同类别 pending_relocation 数量为 0；
       或者 B 不在任何 pending 的候选区域内。

    3. B 附近不存在 residual_snapshot_object 关系。

    4. B 不是某个 confirmed_relocation 的旧位置残影。

    5. B 连续稳定存在达到新物品确认条件。


建议：
    真正新增库存可以设计为 PendingNewItem。

PendingNewItem 的意义：
    当 B 看起来像新物品，但同类操作风险较高时，
    先暂存一轮，不立即入库。

确认条件：
    1. 下一份 stable_snapshot 中 B 仍然稳定存在
    2. 没有 operation_locked item 可以解释 B
    3. 没有 pending_relocation 可以解释 B
    4. B 不匹配遮挡物品露出
    5. B 不匹配出库物品放回

如果满足这些条件，再提交【放入】。

这样可以避免：
    移动物品时，旧位置残影和新位置检测同时存在，
    导致系统立刻新增同类库存。


这条规则是为了避免：
    移动一个 orange，却新增一个 orange。



10.7 第七步：例行清理
---------------------------------------------------------------------

每轮 diff 结束后执行清理：
    1. 清理冗余 OUT 记录
    2. 合并重复 OCCLUDED / VISIBLE 记录
    3. 检查同类重复 item
    4. 清理过期 pending_relocation
    5. 清空本轮 operation_locked 标记


同类重复检查：
    如果同一类别多个 item 空间高度重叠，
    且其中一个刚由放入产生，
    另一个有 HELD / 移动证据，
    应优先合并或回滚新 item，
    不要轻易增加库存数量。


10.8 事件提交边界
---------------------------------------------------------------------

建议 diff_snapshots_v7 使用两阶段提交。

第一阶段：只计算，不修改库存。
    1. 生成 RelocationCandidate
    2. 生成 confirmed_relocation
    3. 生成 pending_relocation
    4. 生成 occlusion / reveal 候选
    5. 生成 out / in 候选
    6. 检查冲突


第二阶段：按优先级统一应用。
    1. 应用 confirmed_relocation
    2. 创建或更新 pending_relocation
    3. 应用 reveal
    4. 应用 occlusion
    5. 应用 out
    6. 应用 confirmed_in（确认放入）
    7. 清理冗余记录


为什么要两阶段提交：
    如果边遍历边修改库存，
    后面的判断会读到已经被前面分支改过的状态。

    这会导致处理顺序影响结果。
    例如：
        先处理 apple 整理，
        apple 覆盖 orange 旧位置，
        orange 被立即改成遮挡，
        orange 自己的整理候选被破坏。


两阶段提交的约束：
    1. 第一阶段不能改 InventoryItem.status。
    2. 第一阶段不能 add_item。
    3. 第一阶段只能写 DiffWorkingSet 和候选事件列表。
    4. 第二阶段应用事件时，必须再次检查 consumed / settled / pending_reserved。
    5. 如果应用前发现冲突，事件降级为 pending 或 ignore_jitter。




十一、OperationContext 与 ByteTrack 辅助逻辑
=====================================================================

原则：
    ByteTrack 每帧都运行。
    OperationContext 每帧更新。
    库存事件只在无手稳定 stable_snapshot 到来后提交。


与新业务流程6的区别：
    新业务流程6：
        OperationContext 主要用于 diff 之后提高 relocation_match 置信度。

    新业务流程7：
        OperationContext 还用于决定哪些 item 不能被 strict_origin_match 先锚定。



11.1 OperationContext 生命周期
---------------------------------------------------------------------

生命周期：
    1. baseline_snapshot 已确定
    2. 创建 OperationContext
    3. 每帧运行 YOLO + ByteTrack
    4. 记录手、物体轨迹、HELD、移动候选
    5. 等待下一份无手稳定 stable_snapshot
    6. 先执行 relocation settlement
    7. 再执行普通 diff
    8. 提交事件
    9. baseline_snapshot = stable_snapshot
    10. 重建 OperationContext


注意：
    OperationContext 不按“手进来 / 手出去”重置。
    它按“稳定快照裁决周期”重置。



11.2 手出现时怎么处理
---------------------------------------------------------------------

每帧检测到手：
    1. operation_context.hand_seen = true
    2. hand_frame_count++
    3. 记录 hand_track_ids
    4. 检查手 bbox 覆盖哪些可见库存 item
    5. 被覆盖 item 加入 touched_item_ids / candidate_held_items


如果手持续出现超过阈值：
    operation_context.hand_long_present = true


手出现不直接产生库存事件。
它只增加操作证据。



11.3 HELD 代理轨迹
---------------------------------------------------------------------

HELD 的用途：
    当物品被手遮挡，YOLO / ByteTrack 看不到物品时，
    用手轨迹作为临时代理轨迹。


流程：
    1. 手覆盖 item A
    2. A 后续持续不可见或 track 断开
    3. A 进入 confirmed_held_items
    4. 建立 HeldProxyEvidence
    5. 手轨迹继续移动时，更新 proxy_trajectory
    6. stable_snapshot 中出现同类候选 B
    7. 如果 proxy_trajectory 终点靠近 B，提高 A -> B 整理置信度


注意：
    HELD 不直接等于拿走。
    HELD 也不直接等于整理。
    HELD 是移动证据。



11.4 ByteTrack 轨迹
---------------------------------------------------------------------

ByteTrack 能连续跟到物品时：
    是整理的强证据。

例如：
    track 起点靠近 item A 的旧位置
    track 终点靠近 stable_snapshot 中的候选 B
    中间移动距离超过阈值
    则 A -> B 可以高置信整理。


轨迹与库存 item 的交接：
    1. track 第一次出现时，先检查它的 bbox 是否靠近某个可见库存 item。
        如果靠近，记录：
            track_id -> associated_item_id

    2. 如果 track 起点靠近 item A 的 baseline bbox，
       并且后续发生明显移动：
            A 加入 moved_item_candidates
            A 加入 operation_locked_items

    3. 如果 track 终点靠近 stable_snapshot 中的同类候选 B：
            建立 RelocationCandidate(A, B)

    4. 如果 track 中途被手遮挡后断开：
            记录 lost_frame_count
            记录 occluded_by_hand = true
            交给 HELD / proxy_trajectory 继续补证据

    5. 如果 track 起点无法绑定库存 item：
            这条 track 只能作为弱证据。
            不能单独触发整理。


轨迹终点的使用：
    track 的 end_bbox 不一定等于最终物品 bbox。
    因为 ByteTrack 可能在物品放下前断掉，或者在手遮挡时漂移。

因此：
    track endpoint 只用于指向候选区域。
    最终 bbox 仍然使用 stable_snapshot 中的 B。


轨迹方向的使用：
    如果 track 从 A 旧位置向 B 方向移动：
        加分。

    如果 track 远离 B：
        降分。

    如果 track 一直停在 A：
        作为负证据，说明 A 可能没有被移动。


ByteTrack 断轨时：
    不直接否定整理。

原因：
    手遮挡、物品重叠、YOLO 漏检都会导致断轨。


ByteTrack 明确反证时：
    要降低整理置信度。

反证例子：
    1. A 的 track 一直停在旧位置
    2. A 的 track 明确移动到其他位置
    3. B 的 track 在 A 被操作前就已经存在
    4. 同一个 track 被多个 item 抢占



11.5 被操作 item 的原位匹配规则
---------------------------------------------------------------------

这是本版本最关键的规则。

如果 item A 是 operation_locked：
    stable_snapshot 中即使存在 A 旧位置附近的同类框，
    也不能立即认定 A 没动。

必须先判断：
    1. A 是否有 track / HELD 指向其他候选位置
    2. 旧位置框是否可能是残影或局部误检
    3. 新位置框是否与 A 外观相似
    4. 新位置框是否靠近手释放点或 track 终点
    5. 旧位置框是否能匹配另一个真实库存物品


如果 A 有强证据移动到 B：
    A 更新到 B。
    旧位置框不应导致新增库存。

如果 A 没有移动证据，且旧位置框稳定：
    A 保持原位。
    新位置同类框继续走普通新出现判断。

如果两边证据都不够：
    进入 pending_relocation。
    不新增库存。




十二、容易出错的场景与处理方式
=====================================================================


12.1 移动一个物品，却多出同类物品
---------------------------------------------------------------------

典型现象：
    baseline:
        item#5 orange 在 A

    stable:
        A 附近仍有 orange 框
        B 位置又出现 orange 框

错误处理：
    A 被 strict_origin_match 锚定为“没动”
    B 被当成新出现
    最终新增 item#6 orange


正确处理：
    如果 item#5 有 HELD / track / touched 证据：
        item#5 进入 operation_locked
        先对 item#5 执行 relocation settlement
        如果证据指向 B：
            item#5 更新到 B
            A 附近框视为残影、抖动或其他待确认对象
            不新增 item#6



12.2 同时整理两个物品
---------------------------------------------------------------------

典型现象：
    apple 移到 orange 旧位置附近
    orange 同时移到另一个位置

错误处理：
    先确认 apple 整理
    apple 新位置覆盖 orange 旧位置
    orange 被立即判成遮挡
    orange 新位置被判放入


正确处理：
    apple 和 orange 都进入 operation_locked
    先批量建立它们各自的候选落点
    先确认各自整理关系
    再处理遮挡 / 露出



12.3 YOLO 残影和局部误检
---------------------------------------------------------------------

现象：
    物品已经移动走，但旧位置仍有同类框。

处理：
    如果旧位置框没有稳定 track，
    且被操作 item 的轨迹/HELD 明确指向新位置，
    则旧位置框不能直接产生库存。

建议：
    记录 residual_detection 日志。
    必要时要求旧位置框连续多个 stable_snapshot 独立存在，才允许作为新物品。



12.4 同类相似物品
---------------------------------------------------------------------

问题：
    多个 apple / orange 外观很像。
    reid_score 容易混淆。

处理：
    1. 不靠 reid_score 单独确认整理
    2. 候选唯一性不足时进入 pending
    3. 有轨迹/HELD强证据时才确认
    4. 宁可 pending，也不要新增错误库存



12.5 没有识别到手的整理
---------------------------------------------------------------------

如果没识别到手，但 ByteTrack 记录到物体移动：
    可以确认整理。

如果没识别到手，ByteTrack 也没记录到移动：
    只能依赖普通快照 diff。

这种情况下：
    如果旧位置消失，新位置出现，外观相似：
        可以 low_confidence_relocation 或 pending。

    如果旧位置没消失，新位置又出现：
        不应直接新增库存。
        应记录同类冲突，等待下一份 stable_snapshot。



12.6 放入真新物品
---------------------------------------------------------------------

真放入的判断应该更保守：
    只有当新出现物品无法解释为：
        1. 当前库存 item 的整理落点
        2. pending_relocation 的候选落点
        3. 遮挡物品露出
        4. 出库物品放回
        5. YOLO 残影或重复框
    才能判定【放入】。



12.7 推荐调试日志
---------------------------------------------------------------------

为了定位误判，建议每次准备提交【放入】前打印：
    1. stable_snapshot 中所有同类物品 bbox / score / count
    2. 当前库存中所有同类物品 bbox / status
    3. 是否有 operation_locked item
    4. 每个同类 item 的 reid_score
    5. 每个同类 item 的 track_score / held_score
    6. 是否存在 pending_relocation
    7. 为什么最终仍然判定为新放入


建议日志格式：
    [DIAG] new_candidate cls=orange box=(...)
    [DIAG] same-class inventory item#5 status=VISIBLE locked=1 reid=0.93 track=0.80 held=0.55
    [DIAG] decision: merge_to_item#5 / pending / real_new


只有把这些中间信息打印出来，才能区分：
    1. 快照中真的出现了两个真实同类物品
    2. YOLO 在旧位置产生残影
    3. strict_origin_match 过早锚定
    4. track / HELD 没有正确关联到 item
    5. reid 分数把同类物品混淆


本文暂不继续展开“十三、主循环伪代码”及后续章节。
