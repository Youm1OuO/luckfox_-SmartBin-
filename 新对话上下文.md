# 新对话上下文

## 任务与优先阅读

这是一个基于摄像头与 YOLOv5n 的冰箱库存识别项目。目标是在用户开关门、拿取、放入、
整理后的视觉序列中，正确维护库存，并处理遮挡、检测漏失和同类物品。

新对话开始时，请先阅读：

```text
另另一种业务流程(3.0var)/细节1-21总结.md  业务规则、术语、不可退化边界的精简版
另另一种业务流程(3.0var)/细节22.md        最新遮挡生命周期设计与实施背景
另另一种业务流程(3.0var)/超参数位置与调节说明.md
讨论用/测试后的结果与发现的问题*.md       如本轮讨论实机问题，再读对应日志
```

摘要优先于逐份阅读旧指导文件；需要追溯具体案例、阈值理由或测试断言时，再查
`细节1.md` 至 `细节21.md` 原文。

## 当前状态

当前构建标识为 `3.0-r21`。`r19` 完成了模块职责拆分；其后已实现《细节22.md》提出的
统一遮挡生命周期：身份结算在前，blocker 关系和遮挡/露出计划在中，普通 OUT 与原子提交在后。

当前新增或重点保留的能力：

```text
src/session_occlusion.cc     遮挡几何、blocker 关系、proof 和生命周期计划
InventoryItem::block_ids     已确认的目标侧前景关系，不是逐帧交叠缓存
OcclusionProof               严格覆盖、边缘残余、连续消失支持等正式遮挡依据
FLOW3_CONFIRMED_OCCLUSION_DISAPPEARANCE_MIN_COVER_RATIO = 0.85
```

上轮主机回放及 ASAN/UBSAN 回放已通过。后续只要改动状态机、身份、遮挡或参数，就必须补充
针对性回放并重跑完整回归；实机问题应以日志和帧序列核实，不能假定某个历史修复已覆盖所有情况。

## 工程位置

正式工程：

`tools/luckfox_pico_rkmpi_example/example/luckfox_pico_rtsp_yolov5n`

主要接口与配置：

```text
include/inventory.h          正式库存项、身份与可见性数据
include/session.h            SessionManager 对外契约和运行时类型
include/session_internal.h   模块共用内部声明
include/geometry.h           基础几何类型
include/fridge_config.h      业务超参数和版本标识
```

会话实现按职责拆分：

```text
src/session.cc               生命周期与 process_frame() 总编排
src/session_geometry.cc      纯几何、严格/局部/轨迹匹配
src/session_ownership.cc     C/B/D 同类所有权与仲裁
src/session_hand.cc          HAND、CONTACT、D、C-D alias
src/session_no_hand.cc       无手观察、灰区、reappear、OUT 证据
src/session_occlusion.cc     遮挡 proof、blocker 因果与状态计划
src/session_settlement.cc    正式结算、事件排序、原子提交
```

主机回放位于 `tests/session3_replay_*.cc`，项目部署目标为 Luckfox Pico，正式实现语言为 C++。

## 工作约束与沟通

```text
1. 先读当前代码、配置、相关回放和实机日志，再判断根因；不要把 detection 直接当库存身份。
2. 保护 inventory_/working_inventory_/operation_start_inventory_ 的事务隔离与 replace_all() 提交边界。
3. 不能为单一案例全局放宽匹配、YOLO/NMS、D/MOVED/OUT 确认条件，或让未确认 D/手框成为 blocker。
4. 修改前先说明目标、范围、因果顺序与验证方式；不要覆盖或回退无关的未提交工作区改动。
5. 代码按职责放入相应文件，不把所有新逻辑塞进一个大函数；改后做相称的编译和回放验证。
```

请用中文沟通，说明简洁、分支清楚、可结合具体帧和物品举例。用户希望先理解方案，再决定是否实施。
