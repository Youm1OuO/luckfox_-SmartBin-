# 新对话上下文

## 任务目标

这是一个基于摄像头与 YOLOv5n 的冰箱物品识别项目。

新对话的任务是：独立评估并完善“用户开关冰箱、拿取或摆放物品后，如何根据视觉结果维护库存”的业务实现。

期望系统能够处理或合理取舍：

```text
放入、取出、整理、遮挡、检测漏失、同类物品同时出现。
```

## 工程位置

正式工程：

`tools/luckfox_pico_rkmpi_example/example/luckfox_pico_rtsp_yolov5n`

当前已验证版本：3.0-r19。

`r19` 只完成了模块化拆分；没有修改业务阈值、匹配优先级、状态机条件、库存提交时机
或事件语义。拆分后的 55 个主机回放场景均通过，输出与 r18 基线一致。

主要接口与配置：

```text
include/inventory.h          库存数据
include/session.h            SessionManager 对外契约和运行时类型
include/session_internal.h   Session 实现模块共用的内部声明
include/geometry.h           基础几何类型
include/fridge_config.h      全部业务超参数和开关
```

主要实现：

```text
src/session.cc               会话生命周期和 process_frame() 总编排
src/session_geometry.cc      几何、严格/局部/轨迹匹配
src/session_ownership.cc     同类所有权和匹配仲裁
src/session_hand.cc          HAND_*、CONTACT_*、D、C-D alias
src/session_no_hand.cc       无手恢复、灰区、reappear
src/session_settlement.cc    数量结算、遮挡/揭示、事件提交

src/main.cc                  硬件资源、门状态 owner、推理和顶层帧循环
src/app_preprocess.cc        letterbox 和坐标映射
src/app_door_state.cc        门状态图像测量
src/app_display.cc           OSD 和事件打印

src/postprocess.cc           NMS 与最终检测结果整理
src/postprocess_decode.cc    i8/RV1106/fp32 解码
src/postprocess_labels.cc    标签生命周期
src/cloud_uploader.cc        云端队列、worker、重试和心跳
src/cloud_json.cc            JSON/base64
src/cloud_http.cc            HTTP socket
```

主机回放测试位于 `tests/session3_replay_*.cc`：共享辅助、基础场景、HAND/CONTACT、
同类所有权、无手恢复和唯一 runner 已拆分为独立文件。

项目部署目标为 Luckfox Pico，正式实现语言为 C++。

## 新对话的工作方式

```text
1. 先自行阅读当前代码、配置和测试方式。
2. 不假定任何已有文档、状态设计、匹配规则或临时轨迹逻辑一定正确。
3. 先从实际输入、库存需要保证的结果和可接受的误判代价出发，独立判断方案。
4. 若要改代码，先说明目标、改动范围和验证方式；避免覆盖工作区中无关的未提交修改。
5. 修改后应做与改动相称的编译或场景验证。
```

## 沟通偏好

```text
用中文；说明要简洁、分支清楚、可结合具体例子。
用户希望先理解方案，再决定是否实施。
```
