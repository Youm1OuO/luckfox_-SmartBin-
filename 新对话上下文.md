# 给新对话的项目上下文

请先看这份文件，再开始讨论。

## 1. 项目在做什么

这是一个基于摄像头与 YOLOv5n 的冰箱物品识别项目。

目标是：用户开冰箱、拿取或摆放物品后，程序根据画面中的识别结果维护库存，并尽量区分：

```text
放入
取出
整理（同一物品换位置）
遮挡 / 露出
```

实际部署端是 Luckfox Pico，正式代码是 C++。

## 2. 重要文件与代码位置

项目要求说明：

```text
任务说明.md
```

正式工程：

```text
tools/luckfox_pico_rkmpi_example/example/luckfox_pico_rtsp_yolov5n
```

当前主要相关代码：

```text
include/inventory.h      库存物品数据结构
include/session.h        会话与业务层接口
src/session.cc           每帧处理、库存结算、临时移动判断
src/snapshot.cc          连续无手帧的快照生成
include/fridge_config.h  阈值配置
src/main.cc              摄像头 / YOLO / 业务层主循环
```

## 3. 当前状态

目前已经有一套“YOLO 每帧输入 → 连续稳定画面形成快照 → 更新库存”的实现，也已经能在板端运行。

但当前方案仍存在整理操作被误判的问题，尤其是多个同类物品同时存在、物品移动、手部遮挡时，可能把整理错误识别为遮挡或新放入。

用户准备重新思考、尝试新的方案。

## 4. 对新对话最重要的要求

不要把现有的 `Track`、快照结算规则、旧文档中的流程当成必须保留的正确答案。

它们只是此前的尝试，可以参考代码了解现状，但应该从项目目标与真实输入输出重新分析；如果新思路更简单、更可靠，可以推翻或重写旧实现。

讨论时优先回答这些根本问题：

```text
1. 库存真正需要保存哪些信息？
2. 每一帧 YOLO 结果应该如何使用？
3. 什么条件下才允许修改正式库存？
4. 如何在“整理”和“取出 + 放入”之间做可靠、可接受的取舍？
5. 手部遮挡和 YOLO 漏检时，系统应保守到什么程度？
```

## 5. 用户偏好

用户更熟悉 Python，不太熟悉 C++；但正式改动仍需落到 C++ 工程中。

解释设计时请尽量：

```text
简单、分支清楚、结合具体物品和帧举例；
不要一开始就给出很复杂的大段代码；
不要假设旧实现一定正确。
```

若准备修改代码，应先说明新思路与旧方案相比具体改变了什么，再实施并做编译或模拟验证。

