# 端侧对接接口文档

## 基本信息

- **后端地址：** `http://192.168.5.6:8000`
- **鉴权方式：** 所有接口均需 `Authorization: Bearer <admin_token>` 请求头
- **Content-Type：** `application/json`

---

## 目录

1. [获取 Token（登录）](#1-获取-token登录)
2. [设备心跳](#2-设备心跳)
3. [食材事件上报（入库/出库/整理）](#3-食材事件上报入库出库整理)
4. [事件响应格式](#4-事件响应格式)
5. [字段别名兼容](#5-字段别名兼容)
6. [标签扫描（可选）](#6-标签扫描可选)
7. [curl 完整示例](#7-curl-完整示例)

---

## 1. 获取 Token（登录）

```
POST /api/v1/admin/auth/login
```

**请求：**

```json
{
  "username": "admin",
  "password": "admin123"
}
```

**响应：**

```json
{
  "token": "eyJhbGciOiJIUzI1NiIs...",
  "admin_id": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
}
```

---

## 2. 设备心跳

设备每 **30 秒**调用一次。首次心跳会自动将设备注册到 `devices` 表。

```
POST /api/v1/admin/events/heartbeat
```

**请求：**

```json
{
  "device_id": "luckfox-001",
  "event": "heartbeat",
  "payload": null
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `device_id` | string | ✅ | 设备唯一标识 |
| `event` | string | ❌ | 事件类型，默认 `"heartbeat"` |
| `payload` | object | ❌ | 附加数据，可为 null |

**响应：**

```json
{
  "device_id": "luckfox-001",
  "name": "luckfox-001",
  "status": "online",
  "last_seen_at": "2026-06-16T15:00:00",
  "auto_registered": true
}
```

---

## 3. 食材事件上报（入库/出库/整理）

这是端侧的**主接口**，所有食材操作均通过此接口上报：

```
POST /api/v1/admin/device-ingest
```

### 3.1 通用字段说明

**外层请求体：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `device_id` | string | ✅ | 设备唯一标识，如 `"luckfox-001"` |
| `event_type` | string | ✅ | 事件类型：`ITEM_IN` / `ITEM_OUT` / `ITEM_MOVED` |
| `timestamp` | int | ❌ | 毫秒级时间戳，不传则服务端自动填充当前时间 |
| `data` | array | ✅ | 食材列表，至少包含 1 个元素 |

**`data` 数组中每个食材对象：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `local_track_id` | int | ✅ | 端侧本地追踪 ID |
| `category` | string | ✅ | 食材分类名称，如 `"苹果"`、`"牛奶"` |
| `confidence` | float | ✅ | 识别置信度，范围 `0.0 ~ 1.0` |
| `bbox` | array[int] | ✅ | 检测框坐标 `[x1, y1, x2, y2]`，像素值 |
| `crop_image` | string | ❌ | 裁剪图 base64（不含 `data:...;base64,` 前缀） |

---

### 3.2 场景 A：入库（ITEM_IN）

将食材放入冰箱。服务端会自动执行 AI 视觉识别、新鲜度评估、保质期推断、向量去重等流程。

**支持的 `event_type` 写法：** `ITEM_IN`、`入库`、`放入`、`in`、`item_in`

```json
{
  "device_id": "luckfox-001",
  "event_type": "ITEM_IN",
  "timestamp": 1750083600000,
  "data": [
    {
      "local_track_id": 1,
      "category": "苹果",
      "confidence": 0.92,
      "bbox": [100, 120, 300, 350],
      "crop_image": "/9j/4AAQSkZJRgABAQAAAQ..."
    },
    {
      "local_track_id": 2,
      "category": "牛奶",
      "confidence": 0.87,
      "bbox": [400, 130, 550, 380],
      "crop_image": "/9j/4AAQSkZJRgABAQAAAQ..."
    }
  ]
}
```

---

### 3.3 场景 B：出库（ITEM_OUT）

将食材从冰箱取出。服务端会将对应库存记录标记为 `OUT_PENDING` 状态。

**支持的 `event_type` 写法：** `ITEM_OUT`、`出库`、`取出`、`out`、`item_out`

```json
{
  "device_id": "luckfox-001",
  "event_type": "ITEM_OUT",
  "timestamp": 1750087200000,
  "data": [
    {
      "local_track_id": 1,
      "category": "苹果",
      "confidence": 0.95,
      "bbox": [110, 125, 295, 345],
      "crop_image": "/9j/4AAQSkZJRgABAQAAAQ..."
    }
  ]
}
```

---

### 3.4 场景 C：整理（ITEM_MOVED）

在冰箱内移动食材位置，更新检测框坐标。此场景可以不传 `crop_image`。

**支持的 `event_type` 写法：** `ITEM_MOVED`、`整理`、`移动`、`move`、`moved`、`item_moved`

```json
{
  "device_id": "luckfox-001",
  "event_type": "ITEM_MOVED",
  "timestamp": 1750089000000,
  "data": [
    {
      "local_track_id": 2,
      "category": "牛奶",
      "confidence": 0.88,
      "bbox": [200, 100, 350, 350]
    }
  ]
}
```

---

## 4. 事件响应格式

入库、出库、整理三种事件统一返回以下格式：

```json
{
  "id": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
  "device_id": "luckfox-001",
  "event_type": "ITEM_IN",
  "raw_payload": { ... },
  "normalized_payload": { ... },
  "status": "success",
  "error_message": null,
  "related_inventory_ids": ["uuid-1", "uuid-2"],
  "trace_id": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
  "created_at": "2026-06-16T15:00:00",
  "processed_at": "2026-06-16T15:00:02"
}
```

**`status` 状态说明：**

| 状态值 | 含义 |
|--------|------|
| `received` | 已接收，排队等待处理 |
| `processing` | 处理中 |
| `success` | 处理成功 |
| `failed` | 处理失败，查看 `error_message` 获取错误详情 |

**关键字段说明：**

| 字段 | 说明 |
|------|------|
| `related_inventory_ids` | 本次事件关联的库存记录 UUID 列表 |
| `trace_id` | AI Agent 执行追踪 ID，可在管理后台日志页面查看详情 |
| `error_message` | 仅 `status=failed` 时有值，描述失败原因 |

---

## 5. 字段别名兼容

后端自动归一化端侧上报的字段名，以下别名均可被正确识别：

**外层字段：**

| 标准字段 | 可接受的别名 |
|----------|-------------|
| `device_id` | `deviceId`、`device`、`deviceID` |
| `event_type` | `event`、`type`、`action`、`business_type` |
| `data` | `items` |

**`data` 数组内字段：**

| 标准字段 | 可接受的别名 |
|----------|-------------|
| `local_track_id` | `track_id`、`trackId`、`localTrackId`、`id` |
| `category` | `label`、`name`、`class`、`class_name` |
| `confidence` | `conf`、`score` |
| `bbox` | `box`、`rect` |

**`event_type` 中文映射：**

| 中文写法 | 映射到 |
|----------|--------|
| `入库`、`放入` | `ITEM_IN` |
| `出库`、`取出` | `ITEM_OUT` |
| `整理`、`移动` | `ITEM_MOVED` |

---

## 6. 标签扫描（可选）

端侧扫描食品标签后可先上传标签图，服务端会做 OCR 解析并缓存，后续同一设备的 `ITEM_IN` 事件会自动关联标签数据。

```
POST /api/v1/admin/events/label_scan
```

**请求：**

```json
{
  "device_id": "luckfox-001",
  "label_image": "/9j/4AAQSkZJRgABAQAAAQ...",
  "ttl_seconds": 300
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `device_id` | string | ✅ | 设备唯一标识 |
| `label_image` | string | ✅ | 标签裁剪图 base64（不含 `data:` 前缀） |
| `ttl_seconds` | int | ❌ | 缓存存活时间（秒），默认 300，范围 10~3600 |

**响应：**

```json
{
  "pending_label_id": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx",
  "label_text": "伊利纯牛奶 250ml",
  "label_data": {
    "brand": "伊利",
    "product": "纯牛奶",
    "volume": "250ml",
    "expire_at": "2026-07-15"
  },
  "expires_at": "2026-06-16T15:05:00"
}
```

---

## 7. curl 完整示例

```bash
# ========== 1. 登录获取 Token ==========
TOKEN=$(curl -s http://192.168.5.6:8000/api/v1/admin/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' \
  | python -c "import sys,json; print(json.load(sys.stdin)['token'])")

# ========== 2. 设备心跳 ==========
curl -X POST http://192.168.5.6:8000/api/v1/admin/events/heartbeat \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"device_id":"luckfox-001"}'

# ========== 3. 入库 ==========
curl -X POST http://192.168.5.6:8000/api/v1/admin/device-ingest \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "device_id": "luckfox-001",
    "event_type": "ITEM_IN",
    "data": [
      {
        "local_track_id": 1,
        "category": "苹果",
        "confidence": 0.92,
        "bbox": [100, 120, 300, 350]
      }
    ]
  }'

# ========== 4. 出库 ==========
curl -X POST http://192.168.5.6:8000/api/v1/admin/device-ingest \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "device_id": "luckfox-001",
    "event_type": "ITEM_OUT",
    "data": [
      {
        "local_track_id": 1,
        "category": "苹果",
        "confidence": 0.95,
        "bbox": [110, 125, 295, 345]
      }
    ]
  }'

# ========== 5. 整理 ==========
curl -X POST http://192.168.5.6:8000/api/v1/admin/device-ingest \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "device_id": "luckfox-001",
    "event_type": "ITEM_MOVED",
    "data": [
      {
        "local_track_id": 2,
        "category": "牛奶",
        "confidence": 0.88,
        "bbox": [200, 100, 350, 350]
      }
    ]
  }'

# ========== 6. 标签扫描（可选） ==========
curl -X POST http://192.168.5.6:8000/api/v1/admin/events/label_scan \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "device_id": "luckfox-001",
    "label_image": "/9j/4AAQSkZJRgABAQAAAQ..."
  }'
```

---

## 附录：管理员后台查看端侧数据

| 页面 | 功能 | 入口 |
|------|------|------|
| **设备管理** | 设备注册状态、在线/离线、心跳频率图表 | `GET /api/v1/admin/devices` |
| **端侧联调** | 原始事件收件箱，可查看每条事件的处理状态和详情，支持重处理 | `GET /api/v1/admin/device-raw-events` |
| **库存管理** | AI 处理后的食材入库结果 | `GET /api/v1/admin/inventory` |
| **系统日志** | Agent 执行追踪，每步工具调用详情 | `GET /api/v1/admin/logs` |
