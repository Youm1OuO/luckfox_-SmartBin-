

for A in 库存中的【可见】物体：
    for B in 快照：
        if 严格匹配(A, B):
            给 A 打算[以匹配]标记, 表示库存中的 A 已经与快照中的物体 B 匹配了
            给 B 打算[以匹配]标记, 表示快照中的 B 已经与库存中的物体 A 匹配了
            B.item_id = A.item_id
        
        elif 局部匹配(A, B):
            '''
            原先是完全看得见的, 现在变得部分可见, 必然有东西挡住了部分或者是 Yolo模型误判(这种情况不是这里处理的, 忽略)
            1. 有可能是库存中的原有物体 C 移动过来遮挡了 A
            2. 也可能是新物品进来遮挡了 A
            无论如果都是有东西遮挡了 A, 只要后续我们能确认A附近是有"新物品拿进"或者"旧物品移动"的话, 就坐实了是被部分遮挡了
            但是其实单凭 A 附近有一个非【可见】的物品就可以直接认为被遮挡了, 其实不需要这些标记？
            '''

            if 【在快照中, 物体 B 的附近有其他物品 C】and 【快照物品 C 与 库存物体 A 附近的物体都不能严格匹配】：
                """说明库存物体 A 被遮挡了"""
                A.current_box = B.box
                给 A 打算[以匹配]标记, 表示库存中的 A 已经与快照中的物体 B 匹配了
                给 B 打算[以匹配]标记, 表示快照中的 B 已经与库存中的物体 A 匹配了
                B.item_id = A.item_id
            else:
                """
                正常来说不会进入到这个分支的, 但是进入到这个分支的话就说明不正常, 保险起见我们直接不处理本次的操作
                """
                跳过本次库存的修改

        eles：
            """
            说明现在 A 直接在快照中看不见了, 有 4 种可能:
            1. Yolo误检了 (这种情况不由我们的逻辑来处理, 忽略掉)
            2. 库存中的物品 A 被整理(移动)到了其他地方
            3. 有新物品被拿进来, 把 A 完全挡住了
            4. 库存中的物品 A 被拿走了
            """
            if 库存物体 A 有属于他的 track:
                if 这个track的路径上可以在快照中找到