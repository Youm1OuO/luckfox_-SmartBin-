思考问题：
    单个摄像头使用yolo很难做到连同被堆叠（被压住）的物体一起都识别到
但是：
    我们的任务并不是让yolo识别被压住的物体，而是当有东西拿走时我们要知道是原本的上面物体被拿走；当有东西放进来时我们要知道他是上面类别并且放在那里；当整理物品时我们要知道原来的某个位置的物品后面被移动到什么位置
也就是说：
    即便yolo无法识别被压住的物体，但是只要yolo能很好的该识别到的物体都准确识别就行了
    我们真正要做的是要想办法把【拿走】【放下】【整理】这几个操作正确判断出来
    （其实不用特别去管遮挡与否，只要把【放下】【拿走】【整理】都处理好，然后其实不管yolo识没识别到，只要再库存中并且不是被拿走的，就算是在库中）





一、对比标准
======================================================================================================

身份匹配（严格）：用于判断"这个新检测到的东西，是不是库存里那个旧东西"
5个条件全部满足才算"是同一个物品"：
1. 类别要一样
2. 中心位置：center_distance(A, B) < ɛ1     (ɛ1是一个阈值, 越小越严格)
3. 框的面积：area_distance(A, B) < ɛ2       (ɛ2是一个阈值, 越小越严格)
4. 框的大小：IoU(A, B) > ɛ3                 (ɛ3是一个阈值, 越大越严格)
5. 截图的像素颜色：两个 bbox 裁剪图 resize 到 16x16 后，计算平均 RGB 差异
    每通道差 < 20（0~255 范围），三通道取平均
    (为提高性能，可以对裁剪图进行切块patch，再对各个切块patch各自单独计算像素颜色差异)

要点：
    我的这次的思路主要基于最最原始的那一版的思路, 即：主要是以快照的形式就行对比判断，因此在身份匹配时必须要严格



身份匹配（宽松）
1. 类别相同
2. center_distance(A, B) < ɛ1_loose  (ɛ1_loose 与 ɛ1 不是同一个)


说明：
    在涉及状态变化的操作中（入库、拿走、遮挡恢复等），必须使用严格匹配
  - 状态变更（入库、拿走、遮挡恢复）一旦判错影响很大 → 必须严格
  - 方法B只是判断"场景有没有变"，漂移不是真正的变化 → 宽松即可，避免误触发


二、状态定义
======================================================================================================

    【可见】  — 物品在冰箱中（含新放入的、遮挡的、以及从遮挡/被拿走恢复回来的）
    【遮挡】  — 被手遮挡/拿着
    【拿走】— 确认被取出（临时保留，关门时不上传后台）

注意：
    无论是【可见】还是【遮挡】都被认为是在冰箱库存中





三、初始化（开门时）
======================================================================================================

开门的时候我们向后台申请要一份冰箱中的库存清单（假设这个库存中的物品全都是存在的）
并且在打开冰箱门时（在手还没进来时）的第一帧，检测识别一下这些物品：
    - YOLO 识别到的物品 与 后台库存清单做身份匹配（严格对比）
    - 匹配上 -> 【可见】
    - 没匹配上但在库存清单中 -> 【遮挡】（YOLO 没识别到, 大概率是被压住了, 小概率是 yolo 误判）




四、HELD 状态机（手在时的主逻辑）
======================================================================================================

当手进来的时候（yolo 识别到）：
    手进来，当手部遮住某个物品时，我们就假设这个物品是被手抓住了（只是假设）
    然后后续去一直检查这个物品是否仍然被手抓住了
    如果这个物品只是凑巧被手挡住的话，他肯定迟早会被再次识别到，如果这个物品被手挡之前与之后的位置没有变的话，就说明这个物品并没有被手抓住，只是凑巧挡住而已

简单来说：
    手进来 -> 物品消失 -> 先标 HELD（不判拿走）
    手离开画面 -> 物品依然没出现 -> 判【拿走】
    手离开画面 -> 物品重新出现 -> 取消 HELD，没被拿走




五、具体操作判断
======================================================================================================

首先我们先来分析一下：
    识别是否增多其实很简单，多了就是多了，没有别的情况; 
    但是识别少了是比较麻烦的, 因为少了的话有可能是：1. yolo漏检; 2. 物品确实是被拿走了; 3. 物品被遮挡了
对应方法：
    1) 我们尽量把yolo模型训练的越准越好
    2) 不要轻易把物品规定为【拿走】
    3) 物品【拿走】后不马上就丢弃, 而是一直保存他的信息, 后续有新物品进来的时候也进行匹配, 如果匹配上就把物品从【拿走】改回【可见】



划分：
快照划分：
    用 N 帧来的最终结果作为一个快照 (N必须为奇数, 并且N最好不要过于大, 例如 3或5)
    这 N 帧内物品必须出现超过 N*s 次(s是百分比，例如 60%), 才能被识别为快照物品（这样做的目的是为了避免yolo抖动误判）
我想到的具体实现方法是：
    在开始创建一个物体识别表（这个表每N帧重置一次, 即这个表只用于这一个快照的判断）
    每yolo识别到一个物品后, 给每一个物品一个计数器 count (初始值为1)
    每一帧识别后的结果与物体识别表的结果进行【身份匹配(宽松)】, 如果匹配上, 将该物品的 count 加1, 如果没有匹配上, 则将 count不动
    N帧结束后这个物体识别表中count数大于 N*s 次(s是百分比，例如 60%) 的物体被认为是正确被是被到的




时间段区分：
    时间段的划分通过两种方法来区分 (方法A or 方法B, 只要有一个通过就行, 这两种方法是一起使用的)

方法A (依靠手来划分是时间段)：
1. 识别到手之前的时间段（没有手的那一段连续时间内，或4~5个快照）
    把这一段连续的【快照】都看作是【时间段t】
    这个【时间段t】内只要有一帧中识别到某个物体, 那么这个物体就要被标为【可见】
    （这是为了防止yolo误判, 因为yolo可能识别的不是十分快、十分准, 如果用单帧来判断的话有可能漏判, 因此在没有的这一段连续时间内, 如果识别到物体, 那么就认为物体是可见的; 即用连续多帧来共同判断物体是否可见）

2. 识别到手的时候：
    这时按照之前的【整理】的步骤就行
    之前的【整理】操作我觉得挺好的

3. 识别到手之后的时间段（没有手的那一段连续时间内，或4~5个快照）
    把这一段连续的【快照】都看作是【时间段t+1】
    这个【时间段t+1】内只要有一个【快照】中识别到某个物体, 那么这个物体就要被标为【可见】
    （这是为了防止yolo误判, 因为yolo可能识别的不是十分快、十分准, 如果用单帧来判断的话有可能漏判, 因此在没有的这一段连续时间内, 如果识别到物体, 那么就认为物体是可见的; 即用连续多帧来共同判断物体是否可见）


注意：
    理想情况下是没有手的那一整段连续时间内的共同识别结果作为时间段t的结果
    但是如果这样的话识别起来就太慢了（主要是测试时显示效果时太慢了, 所以这里可以选择不是整段没有手的连续时间内，而是10~20帧内, 这样就能提前显示结果了）
其实：
    我们实际使用的时候不需要使用这个 4~5个快照内的规定, 直接用没有手的那一整段连续时间内的共同识别结果作为时间段t的结果就行
    比较实际使用的时候要的是我们上传的相信足够准确就行了, 慢几秒钟无所谓的
    但是测试的时候, 我们为了测试效果, 所以建议启用这个 4~5个快照内的设置





方法B (依靠快照对比来划分时间段)：
    对比前后两个【快照】的物品数量, 如果物品的数量一样、而且这些物品经过【身份匹配(宽松)】后也是一样的话, 就当作是同一个时间段
    对比前后两个【快照】的物品数量, 如果物品的数量不一样、或者这些物品经过【身份匹配(宽松)】后不一样的话, 就当作是不同一个时间段, 前面的是【时间段t】, 后面是【时间段t+1】
注意：
    方法B触发【时间段】结束时，将引起数量变化的那一帧纳入下一个时间段的快照计算范围
而且
    方法B的主要用途是在手无法被识别到时作为时间段划分的兜底手段



-----------------------------------------------------------------------------------------------------------------------


【拿走】与【放下】都通过快照来实现：
    时间段t看作一张"快照", 时间段t+1看作一张"快照", 如此类推...
    使用时间段来替代单帧作为一个快照能更稳定yolo的结果, 所以我们用时间段t时的库存与库存清单来比较



使用【时间段t+1】与【时间段t】来判断
    如果当前【时间段t+1】中yolo识别到的物品与【时间段t】中的物品全都一致, 就没事发生 (注意：是使用身份匹配(严格)来判断的, 即：5个条件全部满足才算"是同一个物品")
    如果当前【时间段t+1】中yolo识别到的物品与【时间段t】中的物品不完全一样的话, 就需要进行判断


处理顺序：
    "先处理减少再处理增加"

具体步骤：
    第一步：找出 t+1 比 t 少了的物品（候选"被拿走"）
      -> 对每个消失的物品 A：
        检查 A 的原位置附近有没有新出现的物品 C
          -> 不存在物品 C (什么都没有) -> A 被拿走, A 状态从【可见】变成【拿走】
          -> 存在物品 C, 且 C 在库存中且是【遮挡】状态 -> 说明 C 是旧物品露出来, A 确实被拿走; A 状态从【可见】变成【拿走】, C 状态从【遮挡】变成【可见】
          -> 存在物品 C, 但 C 不在库存中 -> C 是新物品压住了 A; A 状态从【可见】变成【遮挡】
    
    第二步：t+1 中所有识别到的物品与库存做严格身份匹配
      -> 先跟【拿走】状态的物品进行严格身份匹配
        -> 匹配上 -> 说明这个物品是重新被识别的, 将其状态【拿走】改为【可见】
        -> 没匹配上 -> 不做改变
      -> 然后跟库存做严格身份匹配
        -> 匹配上, 但这个物品原先是【遮挡】状态 -> 说明这个物品原先就已经被记录在库存中了, 不需要重新记录, 但需要将【遮挡】状态改为【可见】
        -> 匹配上, 但这个物品已经是【可见】状态 -> 说明这个物品原先就已经被记录在库存中了, 不需要重新记录
        -> 没匹配上 -> 新物品入库, 状态改为【可见】

    第三步：例行检查
      -> 将所有【拿走】状态的物品与库存中的物品（即：遮挡、可见）进行严格身份匹配
        -> 匹配上 -> 说明这个物品被误认是【拿走】并且已恢复到库中, 忘了修改。直接把这个物品删除就行（因为库存中已经有了）
        -> 没匹配上 -> 不做改变
      -> 将所有【遮挡】状态的物品与库存中的【可见】物品进行严格身份匹配
        -> 匹配上 -> 说明这个物品被误认是【遮挡】并且已恢复到【可见】, 忘了修改。直接把这个物品删除就行（因为库存中已经【可见】）
        -> 没匹配上 -> 不做改变


关于"附近"的量化
    A 的原位置 bbox 与 C 的 current_bbox 的 overlap_ratio > 阈值（如0.1~0.2）
    或：C 的中心点落在 A 的原 bbox 内（可适当扩大 bbox 范围作为容忍区域）


注意：
    上面这三步由于涉及状态的变化，因此这三步中在进行身份匹配时全都是使用【身份匹配(严格)】来进行的




【整理】（保留原有代码逻辑）
------------------------------------------------------------------------------------------------------
    整理操作保留原有的 HELD + 平滑移动检测逻辑

【整理】时处理【被挡住】的问题：
    整理 = 放入 + 拿走同时发生，遮挡处理按【放入】和【拿走】的遮挡逻辑同时执行

    即：物品 A 从位置 X 被移到位置 Y
      ->
    检查位置 Y：有没有其他物品的 last_bbox 与 A 的 new_bbox 有空间重叠（overlap_ratio > 0.3），
              且该物品现在消失了？
      -> 有 = A 挡住了它 -> 被挡物品变成【被遮挡】
      ->
    检查位置 X：有没有之前【被遮挡】状态的物品，现在重新出现了？
      -> 有 = A 挪走后它露出来了 -> 解除【被遮挡】






======================================================================================================
补充建议（by Claude）
======================================================================================================


# 状态转换流程图
                                                   ──────────────────────────────────────────────
                                                  │                                              │
                                                  ↓                                      快照对比：t+1有, 且原本
                快照对比：t+1有, 且库中没有             快照对比：t有 t+1无 且附近无新物体出现        │
     不在库   ───────────────────────────────→ 【可见】───────────────────────────────────────→ 【拿走】
                             ↑                    │                                            
                             │      快照对比：t有 t+1无 且附近有新物体出现
                             │                    ↓ 
                             │                 【遮挡】
                             │                    │            
                             │           遮挡物体恢复出现 (回【可见】) 
                             │                    │
                             │       快照对比：t+1有, 且库中原本是【遮挡】
                             └────────────────────┘



### 日志帧索引精度

用视频流的绝对帧号（从摄像头/视频开始计数的序号）：

  时间段结束时，记录：
  - 该时间段的起始帧号（绝对）
  - 该时间段的结束帧号（绝对）

