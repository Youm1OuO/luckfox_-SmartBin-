每一帧都处理：
    YOLO 检测
    判断有无手
    有手时更新 Track、判断物品如何移动


但不在每一帧修改 InventoryDB；库存仍只在稳定快照对比后修改。


所以新物品放入的正确时序应是：

第 1 帧：手拿着新牛奶，只看到一部分
    → 这是临时 Detection
    → 有手，清空快照缓冲
    → 不写正式 anchor_box

手还在的后续帧：
    → 继续逐帧处理
    → Track 处理原有库存物品的移动证据
    → 快照始终不生成

手离开：
    → 开始收集连续 N 帧无手稳定 Detection
    → 此时物品不会再移动
    → N 帧合成 candidate_snapshot

candidate_snapshot 中出现新牛奶：
    → 才真正新增 InventoryItem