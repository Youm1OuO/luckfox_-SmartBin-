1. 包装、透明袋这些要想办法实现
    > 包装的思路：用OCR来识别图片的文字内容, 然后根据图片的文字来分析是什么物品？
        > 但是这个为什么不交给后台来实现？在我们这里实现有什么优势呢？

    > 透明袋的思路：通过数据集来实现？更改曝光度？（曝光度的方法是否可以通过魔改 Yolov5n 模型来实现？）


2. 业务流程要优化完善
    > 似乎有些时候还是有 bug
    > 尝试快一点、多一点物体看会不会有问题


3. 打开冰箱门的检测
    > 是否要实现先暂定, 因为这个的话要把摄像头放在冰箱里面, 这可能比较麻烦


4. 部分取出
    > 如果只是鸡蛋的部分取出的话, 其实通过 Yolo 模型来实现（数据集）
    > 但如果是物体被拿走一半的话(例如：西瓜被拿走一半), 这些可能就要另外想办法了
    > 获取其实不需要做这个