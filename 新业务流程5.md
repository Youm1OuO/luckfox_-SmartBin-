思考问题：
    单个摄像头使用yolo很难做到连同被堆叠（被压住）的物体一起都识别到
但是：
    我们的任务并不是让yolo识别被压住的物体，而是当有东西拿走时我们要知道是原本的上面物体被拿走；
    当有东西放进来时我们要知道他是什么类别并且放在那里；
    当整理物品时我们要知道原来的某个位置的物品后面被移动到什么位置
也就是说：
    即便yolo无法识别被压住的物体，但是只要yolo能很好的该识别到的物体都准确识别就行了
    我们真正要做的是要想办法把【拿走】【放下】【整理】这几个操作正确判断出来
    （其实不用特别去管遮挡与否，只要把【放下】【拿走】【整理】都处理好，然后不管yolo识没识别到，只要在库存中并且不是被拿走的，就算是在库中）




一、对比标准
======================================================================================================

身份匹配（要严格）：用于判断"这个新检测到的东西，是不是库存里那个旧东西"
5个条件全部满足才算"是同一个物品"：
    1. 类别要一样
    2. 中心位置：center_distance(A, B) < ɛ1
    3. 框的面积：area_distance(A, B) < ɛ2       例如： |area_A - area_B| < ε2
    4. 框的大小：IoU(A, B) > ɛ3
    5. 截图的像素颜色：两个 bbox 裁剪图 resize 到 16x16 后，计算平均 RGB 差异
       每通道差 < 20（0~255 范围），三通道取平均


# 其他物品 B 覆盖了 A 的面积
coverage_ratio(A, B) = area(A ∩ B) / area(A)
# 其他物品 B 的中心离 A 的中心
center_distance(A, B)



二、状态定义
======================================================================================================

    【在库】  — 物品在冰箱中（含新放入的、遮挡的、以及从遮挡/被拿走恢复回来的）
    【遮挡】  — 被遮挡（不会直接转为【拿走】，需等 YOLO 重新检测到后恢复为【在库】）
    【拿走】  — 确认被取出（临时保留，关门时不上传后台）



三、初始化（开门时）
======================================================================================================

    申请后台的库存清单(如果库存为空, 就说明冰箱没东西; 如果无响应超时的话, 发出提示并且当作冰箱没东西)
    开机后立刻开始收集检测结果（不管有没有手）
    手第一次出现时，用之前收集到的检测结果做基线初始化
    这样不管手什么时候进来，系统都能正确初始化




四、具体操作判断
======================================================================================================

【拿走】与【放下】都通过快照来实现：
    帧t看作一张"快照", 帧t+1看作一张"快照"
    使用帧t+1与帧t来判断（先减后加）

    第一步（减少）：找出 t+1 比 t 少了的物品（候选"被拿走"）
      对每个消失的物品 A：
        检查 A 的原位置的附近是否yolo识别到存在其他物品（coverage_ratio(A, B) > 0.6 或 center_distance(A, B) < 40）
          -> yolo识别到存在其他物品 → A 可能被遮挡，留在库存中, 标上【遮挡】状态
          -> yolo没识别到存在其他物品 → A 可能被拿走, 标上【拿走】状态

    第二步（增加）：找出 t+1 比 t 多了的物品（候选"新放入"）
      多出来的物品跟库存以及【拿走】状态的物品做严格身份匹配
        -> 先比较：是否跟【拿走】状态匹配上 → 说明这是之前yolo漏检(误判), 恢复【在库】状态
        -> 然后比较：是否跟库存清单没匹配上 → 说明这是新物品, 标上【在库】状态

    第三步：恢复检查
      新快照中的物品跟 【拿走】状态的物品做严格匹配
        -> 跟【拿走】状态匹配上 → 说明这是之前yolo漏检(误判), 恢复【在库】状态


注意：
    要先处理减少，再处理增加
    因为放入新物品后可能导致旧物品被遮挡，先处理减少才不会误判
    【拿走】需要连续 N 帧（建议 3 帧）都检测不到才确认，避免 YOLO 单帧波动误判
    【放下】需要连续 N 帧都检测到才确认新物品
而且：
    【拿走】状态的恢复检查是十分十分重要的
    因为识别过程中可能会有波动、误判、甚至手的短暂遮挡，这些都可能导致一些物品被误判为【拿走】
    因此【拿走】状态的恢复检查是十分十分重要的



遮挡恢复检查
    每次 yolo 识别到物品时，检查（身份匹配（要严格））该被识别到的物品是否【遮挡】状态的，如果是的话就改成【在库】状态



【整理】（两种检测方式并行）
------------------------------------------------------------------------------------------------------

整理有两种触发方式，互相补充：

方式一：HELD 机制（手被识别到时）
    手进来 -> 物品消失 -> 先标 HELD（不判拿走）
    手移动 -> 物品依然没出现 -> 判物品被手握住 -> 物品的轨迹与手的轨迹一致 -> 整理
    手移动 -> 物品重新出现 -> 取消 HELD，物品没被手握住


方式二：平滑移动检测（手没被识别到时）
    不依赖手识别，通过跟踪物品位置变化来检测整理。
    具体逻辑：
      1. 每个物品有一个"锚点"（item_anchor），记录它稳定时的位置
      2. 每帧检测物品位置是否有变化（跟上一帧比，位移 > 阈值 = 在移动）
      3. 如果物品移动了，记录它最后的位置
      4. 物品停下来后（连续几帧位移很小 = 稳定了），检查：
         - 停下来的位置离锚点很远（> 60 像素）→ 整理事件
         - 停下来的位置离锚点很近 → 没有整理，忽略
      5. 整理确认后，更新锚点到新位置


    这样即使手没被识别到，只要物品自己从 X 移到了 Y，也能检测到整理。


两种方式的关系：
    - 有手时：HELD 优先处理，平滑移动检测作为补充
    - 没手时：只有平滑移动检测
    - 两者不会冲突：HELD 处理过的物品会更新锚点，不会重复报整理


【整理】时处理【被挡住】的问题：
    物品 A 从位置 X 被移到位置 Y
      ->
    检查位置 Y：有没有其他物品覆盖住了 → 有则标【遮挡】
      ->
    检查位置 X：有没有之前【遮挡】的物品露出来 → 有则解除【遮挡】



======================================================================================================
附录：代码实现中的匹配策略（与第一章对比标准的关系）
======================================================================================================

第一章定义的 5 条件严格身份匹配在实际实现中发现了一个问题：
    物品在移动过程中（例如手放入冰箱），相邻帧的位置变化可能超过严格阈值，
    导致同一物品在不同帧被误判为"不同物品"，重复添加到库存中。

因此代码中采用了三层匹配策略，以 ByteTrack 的 track_id 为核心：


第一层：track_id 匹配（首选，最可靠）
    ByteTrack 跟踪器已经为每个物体分配了唯一的 track_id，
    同一个 track_id 在时序上保证是同一个物理物品。
    只要 track_id 还在当前帧的食物列表中，就直接认为是同一个物品。
    这一层解决了物品移动过程中位置变化导致的误判问题。

第二层：宽松几何匹配（track_id 变化时的兜底）
    当 track_id 匹配不上时（例如物品被短暂遮挡后 ByteTrack 分配了新 id），
    使用放宽的几何匹配条件：
        1. 类别要一样
        2. 中心位置：center_distance(A, B) < 30px
    不要求 IoU、面积比、颜色匹配。
    适用于：物品移动、track_id 短暂变化、YOLO 检测框抖动等场景。

第三层：严格身份匹配（仅用于 TAKEN 恢复等高置信场景）
    就是第一章定义的 5 个条件，全部满足才算"是同一个物品"：
        1. 类别要一样
        2. 中心位置：center_distance(A, B) < 15px
        3. 框的面积：|area_A - area_B| / area ∈ [0.8, 1.25]
        4. 框的大小：IoU(A, B) > 0.7
        5. 截图颜色：resize 到 16x16 后平均 RGB 差异 < 20
    仅用于：判断"新出现的物品是不是之前被拿走又放回来的"。


各步骤使用的匹配方式：
    第一步（减少 - 判断物品是否还在）：
        → 第一层：track_id 匹配
        → 第二层：宽松几何匹配（兜底）
        → 都匹配不上 → 消失（标【遮挡】或【拿走】）

    第二步（增加 - 新出现的物品）：
        → 第一层：track_id 匹配库存（更新位置）
        → 第三层：严格匹配 TAKEN 物品（高置信恢复）
        → 第二层：宽松匹配 TAKEN 物品（移动物品恢复，5帧窗口内）
        → 第二层：宽松匹配现有库存（防止重复添加）
        → 都匹配不上 → 新物品入库

    第三步（TAKEN 恢复检查）：
        → 第二层：宽松匹配（5帧窗口内）

    遮挡恢复检查：
        → 第一层：track_id 匹配
        → 第二层：宽松几何匹配


TAKEN 恢复窗口：
    物品被标为【拿走】后，5帧内仍允许通过宽松匹配恢复为【在库】。
    这覆盖了 track_id 短暂变化后又匹配回来的情况。