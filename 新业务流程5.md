思考问题：
    单个摄像头使用yolo很难做到连同被堆叠（被压住）的物体一起都识别到
但是：
    我们的任务并不是让yolo识别被压住的物体，而是当有东西拿走时我们要知道是原本的上面物体被拿走；当有东西放进来时我们要知道他是上面类别并且放在那里；当整理物品时我们要知道原来的某个位置的物品后面被移动到什么位置
也就是说：
    即便yolo无法识别被压住的物体，但是只要yolo能很好的该识别到的物体都准确识别就行了
    我们真正要做的是要想办法把【拿走】【放下】【整理】这几个操作正确判断出来
    （其实不用特别去管遮挡与否，只要把【放下  
  】【拿走】【整理】都处理好，然后其实不管yolo识没识别到，只要再库存中并且不是被拿走的，就算是在库中）





所以我现在的思路是：


一、对比标准
======================================================================================================

身份匹配（要严格）：用于判断"这个新检测到的东西，是不是库存里那个旧东西"
    5个条件全部满足才算"是同一个物品"：
    1. 类别要一样
    2. 中心位置：center_distance(A, B) < ɛ1     (ɛ1是一个阈值, 越小越严格)
    3. 框的面积：area_distance(A, B) < ɛ2       (ɛ2是一个阈值, 越小越严格)
    4. 框的大小：IoU(A, B) > ɛ3                 (ɛ3是一个阈值, 越大越严格)
    5. 截图的像素颜色：两个 bbox 裁剪图 resize 到 16x16 后，计算平均 RGB 差异
       每通道差 < 20（0~255 范围），三通道取平均
       (为提高性能，可以对裁剪图进行切块patch，再对各个切块patch各自单独计算像素颜色差异)


要点：
    我的这次的思路主要基于最最原始的那一版的思路, 即：主要是以快照的形式就行对比判断，因此在身份匹配时必须要严格





二、物体的状态定义
======================================================================================================

    【可见】  — 物体可被yolo识别（物品在冰箱中）
    【遮挡】  — 被其他东西遮挡/被手拿着（也算在冰箱中）
    【出库】  — 物品不在冰箱中



注意：
    【可见】与【遮挡】都算是物品在冰箱中
而且：
    每当有新物品进到冰箱时, 都要将其添加到【库存清单】中
    每当有新物品离开冰箱时, 都要将其从【库存清单】中删除



说明：
    【遮挡】实际上包括了两种不同的情况：
        完全被遮挡住, 这时 yolo 是必然无法识别到的 (人眼也看不到, 因为是完全被挡住了)
        不完全被遮挡住, 这时 yolo 是有可能识别到的, 但是由于yolo版本的问题，有可能有使用有抖动（一时看得到一时看不到）
    我们这里对【遮挡】进行一个规定：不能直接从【遮挡】变成【出库】（只能从【可见】变成【出库】）


注意：
    我们之所以需要保留【出库】物品的信息, 是为避免yolo抖动误判以为这个误判消失了, 保留【出库】信息能将他们的信息续回来, 主要是把 item_id 续回来




说明一下：
    物体的状态是分为了【遮挡】、【可见】、【出库】
    但是手的动作（event）是分为了【拿走】、【放下】、【整理】
而且：
    在测试时, 我打算在每次有物品的状态改变时, 都打印一下这些物体的信息（库存清单以及那些已经【出库】了的物品）




三、初始化（开门时）
======================================================================================================

开门的时候我们向后台申请要一份冰箱中的库存清单（我们假设这个库存清单中的物品全都是存在的）
并且在打开冰箱门时（在手还没进来时）我们先拍一张快照检测识别一下这些物品：
    - YOLO 识别到的物品 与 后台库存清单 做身份匹配（严格对比）
    - 匹配上 -> 【可见】
    - 没匹配上但在库存清单中 -> 【遮挡】（YOLO 没识别到, 大概率是被压住了, 小概率是 yolo 误判）





四、具体操作判断
======================================================================================================

首先我们先来分析一下：
    识别是否增多其实很简单，多了就是多了，没有别的情况; 
    但是识别少了是比较麻烦的, 因为少了的话有可能是：1. yolo漏检; 2. 物品确实是被拿走了; 3. 物品被遮挡了
对应：
    1) 我们尽量把yolo模型训练的越准越好
    2) 不要轻易把物品规定为【出库】
    3) 物品【出库】后不马上就丢弃, 而是一直保存他的信息, 后续有新物品进来的时候也进行匹配, 如果匹配上就把物品从【出库】改回【可见】




快照的定义：
    由于我们使用的yolo的版本并不是非常顶级, 如果我们只用单帧作为快照进行对比的话, 很容易被 yolo 的抖动误判所影响到
    因此我们不使用单帧作为快照, 而是使用多帧进行快照
即：
    用 N 帧来的最终结果作为一个快照 (N必须为奇数, 并且N最好不要过于大, 例如 3)
    这 N 帧内，同一个物品必须出现超过 N*s 次(s是百分比，例如 60%), 才会被识别为稳定快照物品（这样做的目的是为了避免yolo抖动误判）

我想到的具体实现方法是：
    在开始创建一个【物体识别表】（这个表每N帧有单独的一个表, 即这个表其实就是这个快照的最终信息结果）
    每yolo识别到一个物品后, 与【物体识别表】中的物体进行【身份匹配(严格)】
       -> 如果没有匹配上的话，就把这个物品的信息（例如：类别，位置，框的大小）增加到物体识别表中，并且给该物品设置一个计数器 count (初始值为1)
       -> 如果有匹配上的话，就给物品的计数器 count 加 1
    N帧结束后这个【物体识别表】中count数大于 N*s 次(s是百分比, 例如 60%) 的物体被认为是正确被是被到的, 那些不符合条件的（count数不够N*s 次的）物品的信息被剔除
特殊点：
    手这个类在我们这个业务中是十分特殊的类别, 因为有手在的话很可能是不稳定的, 所以我们给手一个特殊的处理：这N帧内, 但凡有一帧出现了手的话, 这一个快照就需要把手的信息保留下来
    简单的说就是：在N帧后, 【物体识别表】中其他物品都是按照count来决定十分剔除, 但手必然不提出（如果检查到的话）
    或者这样来实现：其他物品count初始化为1, 手初始化为N
    最后, 这行【物体识别表】中的信息就表示当前快照中所有物品的识别信息（而且只要有一个帧识别到手, 不管真假都要把手保留下来）





上面介绍了每一个快照的识别信息是如果实现的，下面介绍这些快照如何使用
    由于算力有限，我们无法做到实时识别追踪（帧率不够）, 所以我们以快照为主要的对比方法
    但是我们并不是用每一个前后快照进行对比，因为如果直接使用前后相邻的快照进行对比的话, 在【拿走】【放下】【整理】等动作的实现过程中
    有可能会将一个动作错误的划分到了不同的快照中（动作被截断），这样就会导致错误的判断
    因此我们只会在物体稳定后才进行快照的对比
我想到的具体实现方法是：

1. 设置四个变量并初始化为NULL, 例如：snapshoot_1=Null, contrast=Null, current=Null, snapshoot_2=Null

2. 在一开始我们先尝试将第一个【快照】保存到 snapshoot_1 与 contrast 中 (即：snapshoot_1=快照1; contrast=快照1; current=Null, snapshoot_2=Null)

3. 然后获取到下一个【快照】，保存到 current 中 (即：snapshoot_1=快照1; contrast=快照1; current=快照2, snapshoot_2=Null)

4. 然后 current 与 contrast 进行【身份匹配(严格)】
    -> 如果他们差距不大，就说明现在已经稳定了, 当前的快照能用来作为对比， (即：snapshoot_1=快照1; contrast=快照1; current=快照2, snapshoot_2=快照2)
    -> 如果他们差距比较大，就说明现在还没有稳定, 当前的快照不能用来作为对比，要重复上述操作，继续对下一个快照继续进行比较 (即：snapshoot_1=快照1; contrast=快照2; current=快照3, snapshoot_2=Null)...以此类推直到稳定为止（直到snapshoot_1=快照1; contrast=快照x; current=快照x+1, snapshoot_2=快照x+1）

5. 当 snapshoot_1 与 snapshoot_2 都不为 Null 时，就说明已经找到了两个用于对比的快照了
   现在就用 snapshoot_1 与 snapshoot_2 进行对比，判断物品的变动

注意：
    由于我们的快照的思路是用稳定的快照进行对比的, 如果快照中存在手的话, 说明现在必然是动荡期（不够稳定的）
    所以对比的时候如果我们发现 snapshoot_1 或 snapshoot_2 中只要有一个里面存在手的信息, 我们就直接开始下一轮的操作（即：本轮对比当作无事发生）
    (即：直接 snapshoot_1=snapshoot_2; contrast=Null; current=Null, snapshoot_2=Null)


6. 判断的具体步骤：
    第一步：找出 snapshoot_2 比 snapshoot_1 少了的物品（处理"被拿走"或"遮挡"）
    -> 对每个消失的物品 A：
        -> 先检查 A 是否已经在库中 (跟【库存清单】的物品进行严格身份匹配)
            -> 匹配不到, 无事发生
            -> 匹配到, 并且 A 原本是【遮挡】状态 -> A 原先是【遮挡】然后直接看不见, 我们认为这是 yolo 的抖动, 此时我们不去处理
            -> 匹配到, 并且 A 原本是【可见】状态 -> 检查 A 的原位置附近有没有新出现的物品 C
                                                    -> 不存在物品 C (什么都没有) -> A 被拿走, A 状态从【可见】变成【出库】, 更新【库存清单】(即：从【库存清单】中删除 A)
                                                    -> 存在物品 C, 且 C 在库存中且是【遮挡】状态 -> 说明 C 是旧物品露出来, A 确实被拿走; A 状态从【可见】变成【出库】, C 状态从【遮挡】变成【可见】; 更新【库存清单】(即：从【库存清单】中删除 A)
                                                    -> 存在物品 C, 且 C 在库存中且是【可见】状态 -> 此时可能时误判, 无事发生
                                                    -> 存在物品 C, 但 C 不在库存中 -> C 是新物品压住了 A; A 状态从【可见】变成【遮挡】

        （注意：既然现在看得到之前【遮挡】状态的物体C的话，现在虽然能识别到C的框的，但是这个框可能与原来的大小可能不一样，说以这个时候我们只需要对比类别与中心点的位置就行; 这时C需要将状态从【遮挡】变成【可见】,状态变【可见】的同时，用当前快照的 bbox、score 等信息全部更新。 track_id / 类别 / 库存记录 ID 这些标识不变） 

    
    第二步：snapshoot_2 中所有识别到的物品与库存做严格身份匹配（处理"拿进来"）
    -> 然后跟【库存清单】中物品做严格身份匹配
        -> 匹配上, 但这个物品原先是【遮挡】状态 -> 说明这个物品原先就已经被记录在库存中了, 不需要重新记录, 但需要将【遮挡】状态改为【可见】
        -> 匹配上, 但这个物品已经是【可见】状态 -> 说明这个物品原先就已经被记录在库存中了, 不需要重新记录
        -> 没匹配上 -> 这时需要与【出库】物品进行匹配
                        -> 如果这个物品与【出库】物品匹配上, 说明这个物品是刚刚被拿出去又拿回来的 (或者是yolo抖动误以为消失的) —> 那么这个物品的状态从【出库】变回【可见】; 更新【库存清单】(即：从【库存清单】中添加 A)
                        -> 如果这个物品与【出库】物品不匹配, 说明这个物品是全新的 —> 这个物品的状态设置为【可见】; 更新【库存清单】(即：从【库存清单】中添加 A)

        (注意：我们之所以需要保留【出库】物品的信息, 是为避免yolo抖动误判以为这个误判消失了, 保留【出库】信息能将他们的信息续回来, 主要是把 item_id 续回来)


    第三步：例行检查
    -> 将所有【出库】状态的物品与库存中的物品（即：【遮挡】、【可见】的物品）进行严格身份匹配
        -> 匹配上 -> 说明这个【出库】物品已恢复到库中, 但忘了修改。直接把这个【出库】物品删除就行（因为库存中已经有了）
        -> 没匹配上 -> 不做改变
    -> 将所有【遮挡】状态的物品与库存中的【可见】物品进行严格身份匹配
        -> 匹配上 -> 说明这个物品被误认是【遮挡】并且已恢复到【可见】, 但忘了修改。直接把这个物品删除就行（因为库存中已经【可见】）
        -> 没匹配上 -> 不做改变

关于"附近"的量化
    核心公式：
        normalized_distance = center_distance(A, C) / min(A的对角线, C的对角线)
    其中：
        center_distance(A, C) = 两点中心点的欧氏距离
        A的对角线 = sqrt(A.width² + A.height²)
        C的对角线 = sqrt(C.width² + C.height²)
        min() 选较小的那个做分母

    判定阈值：
        < 0.5  →  C 就在 A 的位置上（几乎重叠）   → 判定"附近" ✓
        0.5~1.0 → C 紧挨着 A（在旁边）            → 判定"附近" ✓
        > 1.0  →  C 离 A 比较远（不太可能是同一位置） → 判定"不附近" ✗
    即：
        normalized_distance 这个值越大, 说明越远;
        normalized_distance 这个值越小, 说明越近

注意：
    上面这三步由于涉及状态的变化，因此这三步中在进行身份匹配时全都是使用【身份匹配(严格)】来进行的
    而且三步的执行顺序不能混
        第一步：处理消失的物品（标记【出库】或【遮挡】）
        第二步：处理新出现的物品（入库或更新状态）
        第三步：例行检查（清理冗余状态）
    必须先跑完第一步再跑第二步，不能交叉执行。
    原因：第一步可能把某个物品标记为【遮挡】，第二步才处理新物品入库。
         如果混在一起，可能出现"同一个物品既被标为【遮挡】又被标为新入库"的矛盾状态。

    伪代码示意：
        // 第一步：消失的物品
        for (A in snap1 但不在 snap2) {
            C = 找 snap2 中 A 附近的物品
            if (C 不存在) → A 标【出库】
            if (C 在库存中且【遮挡】) → A 标【出库】, C 标【可见】
            if (C 不在库存中) → A 标【遮挡】(被 C 挡住)
        }
        // 第二步：新出现的物品（必须在第一步完成后）
        for (C in snap2) {
            严格匹配库存 → 匹配上就更新状态，匹配不上就入库
        }
        // 第三步：例行清理（必须在第二步完成后）
        清理冗余的【出库】和【遮挡】记录

7. 交换 snapshoot_1 与 snapshoot_2, 并设置 snapshoot_2 为 NULL 
    (即：snapshoot_1=snapshoot_2; contrast=Null; current=Null, snapshoot_2=Null)
    然后重复上述处理

注意：
    由于我们的快照的思路是用稳定的快照进行对比的, 如果快照中存在手的话, 说明现在必然是动荡期（不够稳定的）
    所以对比的时候如果我们发现 snapshoot_1 或 snapshoot_2 中只要有一个里面存在手的信息, 我们就直接开始下一轮的操作（即：本轮对比当作无事发生）
    (即：直接 snapshoot_1=snapshoot_2; contrast=Null; current=Null, snapshoot_2=Null)
    (这个在之前就已经提过了, 这里再提醒一下)


而且说明一下：
    虽然我们快照对比时是不使用带有手的信息的, 但是这并不表示我们整个业务流程不使用手的信息
    手的信息我们会通过 bytetrack 来使用, 并且这个过程中也会出现物体的变动（状态的改变）
因此：
    每当 bytetrack 操作完后, 修改好物品的状态后, 这部分的事件完全执行完毕后, 我们依旧会交换 snapshoot_1 与 snapshoot_2, 并设置 snapshoot_2 为 NULL 
    (即：snapshoot_1=snapshoot_2; contrast=Null; current=Null, snapshoot_2=Null)





-------------------------------------------------------------------------------------------------------------------------
我们比较快照的时候是不使用带有手的快照的，所以我们这里需要专门处理一下手的情况：
    当我们识别到手的时候, 我们就是用ByteTrack 的方法进行轨迹的跟踪, 希望能从中来判断【整理】动作
    
    【整理】操作保留原有的 HELD + 平滑移动检测逻辑
即：
当手进来的时候（yolo 识别到）：
    手进来，当手部遮住某个物品时，我们就假设这个物品是被手抓住了（只是假设）
    然后后续去一直检查这个物品是否仍然被手抓住了
    如果这个物品只是凑巧被手挡住的话，他肯定迟早会被再次识别到，如果这个物品被手挡之前与之后的位置没有变的话，就说明这个物品并没有被手抓住，只是凑巧挡住而已

简单来说：
    手进来 -> 物品消失 -> 先标 HELD（不判拿走）
    手离开画面 -> 物品依然没出现 -> 判【拿走】
    手离开画面 -> 物品重新出现 -> 取消 HELD，没被拿走



而且还有一个情况 ———— 没识别到手的【整理】：
    在【整理】的过程中可能有时候没识别到手但识别到了物体在移动, 因此我们需要正确的处理这个情况
    我想到的解决方法：我们会对每一帧持续进行 bytetrack 来追踪物品的移动
    （其实识别手的哪个【整理】操作相当于是这个操作的一个带有手的版本）




【整理】时处理【遮挡】的问题：
    整理 = 放入 + 拿走同时发生，遮挡处理按【放入】和【拿走】的遮挡逻辑同时执行

    即：物品 A 从位置 X 被移到位置 Y
      ->
    检查位置 Y：有没有其他物品的 last_bbox 与 A 的 new_bbox 有空间重叠（overlap_ratio > 0.3），
              且该物品现在消失了？
      -> 有 = A 挡住了它 -> 被挡物品变成【遮挡】
      ->
    检查位置 X：有没有之前【被遮挡】状态的物品，现在重新出现了？
      -> 有 = A 挪走后它露出来了 -> 解除【遮挡】




ByteTrack + 快照对比的协同实现方案
------------------------------------------------------------------------------------------------------

    核心原则：
        ByteTrack = 实时连续跟踪，提供"谁在哪、在移动吗"的连续信息
        快照对比 = 稳态时刻的精确对比，提供"哪些物品变了"的最终裁决
        两者不是替代关系，而是互补关系。

    具体分工：

    （A）ByteTrack 持续运行（每一帧都跑）：
        - 维护每个物品的 track_id，记录它的 bbox 轨迹
        - 输出两个信息给上层：
            ① 每个活跃 track 的当前位置、类别、track_id
            ② 每个 track 的"是否在移动"标志（最近 N 帧中心点位移 > 阈值）

    （B）快照对比只在稳态时触发：
        - 触发条件：画面无手 + 3帧投票快照生成完毕 + 与上一份稳态快照有差异
        - 触发后，对比两份快照，输出变动事件（放入/拿走/整理）

    （C）两者结果冲突时的裁决规则：
        优先级：快照对比 > ByteTrack 候选
        原因：快照是多帧投票后的稳定结果，比单帧 ByteTrack 输出更可靠。
        ByteTrack 的作用是"提前预警"和"补充信息"，不是最终判定。

    实现伪代码：

        // 主循环每帧：
        tracks = bytetrack.update(detections)   // ByteTrack 每帧都跑

        if (hand_present) {
            // 手在画面 → 走 ByteTrack 辅助手部逻辑（见第九点）
            // 不触发快照对比
        } else {
            // 手不在 → 推入快照缓冲区
            snapshot_buffer.push(tracks)
            if (snapshot_buffer.full()) {       // 3帧收齐
                snap = make_snapshot(snapshot_buffer)   // 投票过滤
                if (snap != last_stable_snapshot) {     // 与上一份稳态不同
                    events = diff_snapshots(last_stable_snapshot, snap)
                    apply_events(events)                // 执行出入库逻辑
                    last_stable_snapshot = snap
                }
            }
        }

    重要：ByteTrack 在手存在期间仍然运行，它的 track 信息在手离开后，
    可以用来辅助判断"手部遮挡期间到底发生了什么"（见第九点）。

注意注意：
    这些所有的操作都会实时更新进（本地）库存清单中, 就行身份匹配时也是用这个清单进行匹配的
    （我怕你们不清楚说明时候就行更新，以及不知道用什么进行身份匹配，所以这里特别强调一下）




手存在期间 ByteTrack 的具体作用
------------------------------------------------------------------------------------------------------

    手在画面里时，快照对比暂停，但 ByteTrack 继续工作。
    这段时间 ByteTrack 承担两个职责：

    职责1：跟踪手的移动轨迹（判断手是否在操作物品）
        - ByteTrack 给手分配一个 track_id（假设手的类别是 'hand' 或 'a'）
        - 手的 bbox 移动 → 手在动（可能在操作物品）
        - 手的 bbox 静止 → 手静止（可能只是遮挡）

    职责2：记录手部遮挡期间物品的消失/出现（HELD 判断）
        具体流程：
            ① 手进入画面前，记录当前所有可见物品的位置（snapshot_before_hand）
            ② 手在画面期间，持续检查：
               - 哪些物品的 bbox 与手的 bbox 有重叠（IoU > 阈值）→ 标记为 CANDIDATE_HELD
               - CANDIDATE_HELD 的物品如果在后续帧中消失 → 确认 HELD
               - CANDIDATE_HELD 的物品如果持续可见 → 取消 CANDIDATE_HELD（只是挡住了，没抓住）
            ③ 手离开画面后：
            首先，立即重新检查所有"因手而变成【遮挡】"的物品（不检查原本就被其他物品压住的【遮挡】物品）：
                - 用当前帧的 YOLO 检测结果判断该物品是否还在
                - 物品还在（重新被识别到）→ 恢复为【可见】
                - 物品不在（没被识别到）→ 确认被拿走，标记【出库】

            然后再进行后续的快照对比逻辑：
                - 等待 3 帧稳定（让摄像头画面稳定下来）
                - 然后拍一份新快照（snapshot_after_hand）
                - 对比 snapshot_before_hand vs snapshot_after_hand：
                 · 物品 A 在 before 有，在 after 没有 → 检查 A 是否被标记为 HELD
                   · 是 HELD → A 被拿走，标记【出库】
                   · 不是 HELD → A 可能被遮挡了（不是被手遮挡），标记【遮挡】
                 · 物品 B 在 before 没有，在 after 有 → 新物品放入，标记【可见】
                 · 物品 C 在 before 和 after 都有，但位置变了 → 检查 C 是否被标记为 HELD
                   · 是 HELD → C 被整理（移动了），更新位置
                   · 不是 HELD → C 可能只是被推动了，也更新位置

    关键细节：
        - 手离开画面的判断：连续 N 帧（如 5 帧）没有检测到 hand 类别 → 认定手离开
        - 等待稳定：手离开后再等 3 帧再拍快照，避免手刚离开时画面抖动
        - snapshot_before_hand 什么时候更新：
          每次手离开并完成对比后，用 snapshot_after_hand 更新它，作为下次手进来时的基准




而且说明一下：
    虽然我们快照对比时是不使用带有手的信息的, 但是这并不表示我们整个业务流程不使用手的信息
    手的信息我们会通过 bytetrack 来使用, 并且这个过程中也会出现物体的变动（状态的改变）
因此：
    每当 bytetrack 操作完后, 修改好物品的状态后, 这部分的事件完全执行完毕后, 我们依旧会交换 snapshoot_1 与 snapshoot_2, 并设置 snapshoot_2 为 NULL 
    (即：snapshoot_1=snapshoot_2; contrast=Null; current=Null, snapshoot_2=Null)


注意注意：
    这些所有的操作都会实时更新进（本地）库存清单中, 就行身份匹配时也是用这个清单进行匹配的
    （我怕你们不清楚说明时候就行更新，以及不知道用什么进行身份匹配，所以这里特别强调一下）


======================================================================================================
补充建议（by Claude）
======================================================================================================



五、"附近"量化建议（自适应方案）
------------------------------------------------------------------------------------------------------

    "附近"要回答的核心问题：
        C 是否出现在了 A 原来占据的那个物理空间？
        （不管 A 大还是 C 大，不管位置偏了多少，核心就这一个问题）

    ---- 原方案的问题 ----

    文档原文的两种方法：
        方法1：overlap_ratio(A.bbox, C.bbox) > 0.1~0.2
        方法2：C 的中心点落在 A 的 bbox 内

    都有大小物品适配问题（详见下方附录），不再使用。

    ---- 推荐方案：自适应归一化中心距离 ----

    核心公式：
        normalized_distance = center_distance(A, C) / min(A的对角线, C的对角线)
    其中：
        center_distance(A, C) = 两点中心点的欧氏距离
        A的对角线 = sqrt(A.width² + A.height²)
        C的对角线 = sqrt(C.width² + C.height²)
        min() 选较小的那个做分母

    判定阈值：
        < 0.5  →  C 就在 A 的位置上（几乎重叠）   → 判定"附近" ✓
        0.5~1.0 → C 紧挨着 A（在旁边）            → 判定"附近" ✓
        > 1.0  →  C 离 A 比较远（不太可能是同一位置） → 判定"不附近" ✗
    即：
        normalized_distance 这个值越大, 说明越远;
        normalized_distance 这个值越小, 说明越近


    ---- 为什么用 min(对角线) 而不是 max ----

    场景1：草莓(40px)叠在西瓜(200px)上
        → 用草莓的对角线(≈56px)做分母
        → 两者中心距离 5像素 → 5/56 = 0.09 → 判定"附近" ✓
        → 如果用西瓜的对角线(≈283px)做分母 → 5/283 = 0.018 → 太宽松，西瓜旁边的任何东西都会被判"附近"

    场景2：西瓜(200px)旁边放苹果(50px)，距离 10像素
        → 用苹果的对角线(≈71px)做分母 → 10/71 = 0.14 → 判定"附近" ✓
        → 合理：苹果确实在西瓜旁边

    场景3：草莓(40px)旁边放蓝莓(30px)，距离 60像素
        → 用蓝莓的对角线(≈42px)做分母 → 60/42 = 1.43 → 判定"不附近" ✓
        → 合理：60像素对小物品来说已经很远了

    ---- 用 min 的本质意义 ----

    min(对角线) 选出的是"更小的那个物品"。
    用更小物品的尺寸来衡量距离，意味着：
    "对于这个小物品来说，这段距离算不算近？"
    这天然适配了大小物品混合的场景。

    ---- 快速参考 ----

    如果实际测试发现阈值 1.0 太松或太紧，可以调整：
        更严格 → 改为 0.7（两个物品必须很近才算"附近"）
        更宽松 → 改为 1.5（允许一定距离也算"附近"）
    建议先用 1.0，实际跑几组数据后再微调。