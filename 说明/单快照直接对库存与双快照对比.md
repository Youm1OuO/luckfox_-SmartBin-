# 单快照直接对库存 vs 两份快照对比

## 结论

对于当前项目，更建议使用：

```text
一份新的稳定快照 + 已确认的 InventoryDB
```

不必长期保存 `baseline_snapshot` 再和 `candidate_snapshot` 比较。

前提是：库存表必须是上一次稳定结果确认后保存下来的完整状态，并至少保留：

```cpp
item_id
cls_id
status                 // VISIBLE / OCCLUDED / OUT
anchor_box
anchor_valid
last_seen_box
遮挡计数器
```

在这个前提下，库存表就是“已经带有 item_id、状态和历史信息的上一份快照”。

## 1. 两份快照对比法

```text
旧稳定快照 A  vs  新稳定快照 B
```

主要判断：

```text
旧快照有、新快照没有 → 消失
旧快照没有、新快照有 → 出现
旧新都有且位置相同 → 未变化
旧 A 消失、新 B 出现，再看 Track → 可能是整理
```

优点：

- 很直观：直接看“前后画面有什么不同”；
- 开始设计时简单，不必先设计完整库存状态；
- 适合只关心画面变化、不需要长期保存物品身份的场景。

缺点：

- 旧快照中的物品一开始没有正式 `item_id`，还需要额外把快照物品和库存物品关联；
- `OCCLUDED`、`OUT` 等不在旧画面中的库存物品，仍要回头查询库存表；
- 同时维护 `baseline_snapshot`、`candidate_snapshot`、库存表，容易出现“哪一份才是正式状态”的疑问；
- 每次提交后还要更新旧快照，代码和状态更多。

## 2. 单快照直接对库存法

```text
InventoryDB（上次确认的正式状态）  vs  current_stable_snapshot（这次新快照）
```

新的稳定快照刚生成时，里面的物品只是：

```cpp
SnapshotItem {
    cls_id;
    box;
    score;
    // 还没有自己的 item_id
}
```

然后直接与库存表比较，给快照物品建立“引用哪个库存物品”的关系。

```text
库存 item_id=1 的苹果在左边
新快照看到左边苹果
→ 新快照这个苹果临时关联 inventory_item_id=1
→ 表示未变化
```

若新快照里没有对应物品：

```text
库存 item_id=1 原来是 VISIBLE
新快照中没有它
→ 它可能被拿出，或被遮挡
→ 再检查新快照附近物品、遮挡规则和 Track 证据
```

若新快照多出一个物品：

```text
先尝试关联 Track 已绑定的库存物品（可能是整理）
再尝试关联库存中 OCCLUDED 的同类物品（可能重新可见）
都不是
→ 确认是新放入，才分配新的 item_id
```

## 3. 为什么它符合“马尔可夫性”

做当前判断真正需要的是：

```text
上一次已经确认的库存状态 + 当前稳定观察结果 + 本次手部 Track
```

更早的快照不应再直接参与判断；它们已经通过上一次提交，浓缩成库存表中的：

```text
item_id、位置框、状态、计数器
```

因此，只要库存表这些字段正确，上一张原始快照不会提供额外的必要信息。

这不是“丢弃历史”，而是把有用历史保存为库存状态：

```text
旧快照中的苹果
→ 提交后变成 InventoryItem(item_id=1, anchor_box, last_seen_box, status...)
```

## 4. 两种方法的关键区别

| 问题 | 两份快照 | 单快照 + 库存 |
| --- | --- | --- |
| 上一次状态存在哪里 | `baseline_snapshot` | `InventoryDB` |
| 正式身份 `item_id` | 需额外关联 | 天然存在于库存项 |
| 遮挡/已拿出物品 | 还要查库存 | 库存直接保留 |
| 需要维护的主要状态 | 两份快照 + 库存 | 一份快照 + 库存 |
| 代码理解难度 | 较高 | 较低 |
| 对库存正确性的依赖 | 有 | 更直接、更明确 |


## 5. 当前项目推荐的实际流程

```text
1. 每一帧：YOLO、手检测、Track 更新。
   手在画面中：只更新 ProcessStore / Track，不修改正式库存。

2. 连续 N 帧无手且稳定：生成一份 current_stable_snapshot。

3. 将这份快照直接与 InventoryDB 比较：
   a. 先匹配仍在原位置的 VISIBLE 库存物品；
   b. 再用 Track 判断“旧库存物品是否整理到新位置”；
   c. 处理重新可见、遮挡、拿出、新放入；
   d. 得到 planned_changes。

4. 一次性提交 planned_changes 到 InventoryDB，并同步后台。

5. 清空本次 Track 和临时快照，等待下一次操作。
```

这里的 `current_stable_snapshot` 只是本次观察结果；库存表才是唯一正式基准。

## 6. 必须守住的条件

单快照法能成立，依赖以下规则：

- 只有稳定快照对比完成后，才能修改 InventoryDB；
- 手在画面中时，不能把 Track 中的临时估计框写进 InventoryDB；
- `anchor_box`、`anchor_valid`、`last_seen_box`、状态和计数器必须随库存一起保存；
- 同一轮比较先生成 `planned_changes`，最后统一提交，不能比较到一半就改库存。

只要守住这些规则，单快照直接对库存比“双快照 + 库存”更符合当前项目，也更容易实现和维护。


>
>
>


## 7. 从开门第一帧开始的完整运行过程

这一节说明一次开门会话中，每一帧做什么、Track 如何工作、稳定快照如何和库存比较。

### 7.1 开门：初始化本次临时状态

冰箱门打开时，先从后台读取已经确认的库存表：

```text
InventoryDB：唯一正式状态
item_id=1：苹果，VISIBLE，anchor_box=L，last_seen_box=L
item_id=2：牛奶，VISIBLE，anchor_box=R，last_seen_box=R
item_id=3：饮料，OCCLUDED，anchor_box=M，last_seen_box=M
```

然后只清空本次开门的临时状态：

```cpp
ProcessStore {
    no_hand_buffer = 空;
    current_stable_snapshot = 空;
    tracks = 空;
    operation_pending = false;   // 用于判断是否要处理【遮挡】物体的计数器
}
```

此时不会复制或修改库存。库存表就是本次比较的旧基准。

### 7.2 所有帧都先做的事情

无论是否有手，每一帧都执行：

```text
1. 摄像头取一帧图像；
2. 输入 YOLO，得到本帧 Detection 列表；
3. 得到手框 hand_box（若有手）；
4. 对 Detection 做类别筛选、框坐标统一；
5. 更新本次 ProcessStore；
```

单帧 Detection 只有：

```cpp
Detection { cls_id, box, score }
```

它没有自己的 `item_id`。只有在本帧处理时，程序才会尝试让它临时关联到：

```text
某条活动 Track，或某个 InventoryItem。
```

注意：无论这一帧匹配是否成功，都不能立刻改 `InventoryDB`。正式库存只在稳定快照比较结束后修改。

### 7.3 手在画面中：更新 Track，不生成快照

只要检测到手：

```text
operation_pending = true;
清空 no_hand_buffer；
本帧不参与稳定快照；
```

此时库存中的 `anchor_box`、`last_seen_box`、`status` 都保持不变。

#### 7.3.1 手刚碰到一个原有物品

例如库存中有苹果：

```text
Inventory item_id=1：apple，anchor_box=L，anchor_valid=true
```

手进入后，YOLO 还能看到苹果的一部分：

```text
Detection：apple，box=L_part
```

处理顺序是：

```text
先尝试匹配已有活动 Track；
若没有匹配的 Track，再与库存中 VISIBLE 的同类物品比较；(如何匹配到 item_id=1？,cls_id要一直, 并且框是包含框)
匹配到 item_id=1 后，创建一条临时 Track。
```

```cpp
Track {
    track_id = 1;             // 本次开门的临时编号
    bound_item_id = 1;        // 引用库存苹果
    cls_id = apple;
    start_box = item#1.anchor_box;
    latest_box = item#1.anchor_box;
    path = [item#1.anchor_box];
    last_hand_box = 当前手框;
}
```

这里 `Track` 是复制 `anchor_box` 的值，不是一直读取库存框。因此手移动期间，库存框始终冻结，Track 自己的 `latest_box` 和 `path` 才会变化。

#### 7.3.2 手在移动，YOLO 仍能看到物品

当前帧苹果已经被移动到中间位置：

```text
Detection：apple，box=M_part
```

它通常已经无法和库存中的旧位置 `L` 严格匹配，但可以与 `Track#1.latest_box` 或 `Track#1.path` (稍宽松)匹配(比较有位移,因此无法严格匹配)：

```text
Detection → Track#1 → bound_item_id=1
```

随后只更新 Track：

```cpp
track.latest_box = 将当前苹果检测结果融合成物品估计框;
track.path.push_back(track.latest_box);
```

库存 `item_id=1` 仍留在旧确认位置 L，直到本次操作最终被确认。

#### 7.3.3 手完全挡住物品，YOLO 看不到物品

例如苹果 Detection 消失，但手仍连续出现。

```text
Track#1 不删除；
Track#1.bound_item_id 仍为 1；
```

此时用手框的位移作为代理：

```cpp
hand_delta = current_hand_box - track.last_hand_box;
track.latest_box = Move(track.latest_box, hand_delta);
track.path.push_back(track.latest_box);
track.last_hand_box = current_hand_box;
```

物品估计框仍保持 `start_box` 的大致尺寸，只是位置跟着手移动。手离开时，不再继续追加手的离开路径；Track 保留当前已有路径，等待后面的稳定快照使用。

#### 7.3.4 新放入的物品

若手中出现一个库存中没有对应项的物品：

```text
Track#2：track_id=2，bound_item_id=空，cls_id=apple
```

它可以记录本次手部过程，但由于尚未确认“确实放入”，不能分配正式 `item_id`，也不能写入库存。

### 7.4 手离开：连续无手帧生成一份稳定快照

手消失后，每帧仍继续 YOLO；此时开始填充 `no_hand_buffer`：

```text
第 101 帧：无手，加入缓冲
第 102 帧：无手，加入缓冲
...
第 N 帧：无手，且结果连续稳定
→ 生成一份 current_stable_snapshot
```

若中途又检测到手或明显移动：

```text
清空 no_hand_buffer；
回到“手在画面中”的处理；
```

稳定快照中的每个物品，是同一位置附近多个检测框投票、融合后的结果：

```cpp
SnapshotItem {
    cls_id;
    box;          // 本次稳定观察框
    score;
    vote_count;
}
```

此时 `box` 还不是库存的 `last_seen_box`；只有比较确认并提交后，它才会写入对应库存项。

### 7.5 用“当前稳定快照”直接比较 InventoryDB

比较前建立本轮临时结果：

```cpp
planned_changes = 空;
每个库存项和 SnapshotItem 都标记为 unprocessed;
```

整个比较过程中不直接修改 `InventoryDB`。

#### 第一步：匹配未移动、仍可见的库存物品

对每个当前 `SnapshotItem B`，尝试匹配库存中状态为 `VISIBLE` 的同类物品 A：

```text
优先比较：B.box 与 A.last_seen_box
必要时参考：A.anchor_box（仅 anchor_valid=true 时）
```

匹配规则仍使用统一的框匹配方法：类别相同、中心距离接近、宽高接近；同一个 A 和 B 只能一对一匹配。多个候选无法唯一确定时，先不强行匹配，留给后续步骤处理。

若匹配成功，表示 A 仍在原位置附近：

```text
B 临时关联 A.item_id；
planned_changes[A.item_id].last_seen_box = B.box；
planned_changes[A.item_id].status = VISIBLE；
```

若 `B.box` 基本包含 `A.anchor_box`，还可以按已约定的规则补强：

```text
planned_changes[A.item_id].anchor_box = B.box；
planned_changes[A.item_id].anchor_valid = true；
```

这一步不使用手部 Track，因为它们是“原位置直接匹配成功”的物品。

#### 第二步：处理整理（物品从旧位置移动到新位置）

这时会留下两类未匹配对象：

```text
库存中原本 VISIBLE、但当前快照没有在旧位置匹配到的物品 A；
当前快照中新出现、尚未匹配库存的物品 B。
```

对每个 A：

```text
查找 bound_item_id=A.item_id 的活动 Track；
再检查 B.box 是否与这条 Track.path 中某个物品估计框匹配。
```

若 A、B 和 Track 三者唯一对应：

```text
这是整理：库存 item_id 不变；
B 临时关联 A.item_id；
```

并写入计划：

```cpp
planned_changes[A.item_id].last_seen_box = B.box;
planned_changes[A.item_id].anchor_box = Track 中与 B 匹配的估计框;
planned_changes[A.item_id].anchor_valid = true;
planned_changes[A.item_id].status = VISIBLE;
```

这里更新 `anchor_box` 是操作结束、整理已经确认后的提交计划，不是手移动过程中修改库存。

整理成立后，还要检查两个位置：

```text
旧位置 A 附近：原来被遮挡的库存物品是否在当前快照重新可见？
新位置 B 附近：原来可见的库存物品是否在当前快照消失并被 B 遮挡？
```

前者计划改为 `OCCLUDED → VISIBLE`，后者计划改为 `VISIBLE → OCCLUDED`。处理过的对象做好标记，避免后续重复处理。

#### 第三步：处理剩余“原来可见、现在没有匹配到”的物品

此时某个库存物品 A 原本 `VISIBLE`，但没有原位置匹配，也没有被 Track 证明整理。

以 A 的 `anchor_box` 为旧位置参考，检查当前快照中是否有物品 C 出现在附近：

```text
附近没有 C
→ A 大概率被拿出，计划 A → OUT。

附近有 C，且 C 能关联到原来 OCCLUDED 的库存物品
→ C 重新可见，计划该库存物品 → VISIBLE；
  A → OUT。

附近有其他 C
→ C 可能是新放入物品，也可能是另一个原有物品移动过来；
  保守地计划 A：VISIBLE → OCCLUDED。
```

这一步不使用 Track 判断“拿出”或“遮挡”；Track 只负责证明整理。

#### 第四步：处理剩余“当前新出现、仍未匹配”的物品

对还没有关联 `item_id` 的当前快照物品 B：

```text
1. 尝试匹配库存中剩余的 OCCLUDED 物品：
   成功 → B 重新可见，继承原 item_id。OCCLUDED → VISIBLE

2. 尝试匹配尚未过期的 OUT 物品：
   成功 → 恢复该 item_id。out → VISIBLE

3. 都不匹配：
   确认是新放入；
   这时才分配新的 item_id，VISIBLE，并计划新增库存项。
```

### 7.6 遮挡计数器与统一提交

所有匹配和判断完成后，才根据最终计划状态检查遮挡物品的计数器。

只要本次开门确实发生过手部操作，即：

```cpp
operation_pending == true
```

才对最终仍为 `OCCLUDED` 的库存物品检查其 `anchor_box` 附近：

```text
附近没有可见物品
→ no_occluder_count + 1；duplicate_count 清零。

附近严格只有一个可见物品
→ duplicate_count + 1；no_occluder_count 清零。

附近情况不明确或有多个候选
→ 两个计数器都清零。
```

计数器 no_occluder_count 达到阈值后，才计划该物品 `OCCLUDED → OUT`。
计数器 duplicate_count 达到阈值后，才计划该物品 `OCCLUDED → VISIBLE`。
这样不会因为用户只开门查看、系统重复生成稳定快照，就不断累加计数。

最后一次性执行：

```text
planned_changes → InventoryDB；
InventoryDB → 后台；
清空 tracks、no_hand_buffer、current_stable_snapshot；
operation_pending = false；
```

新的 InventoryDB 立刻成为下一次稳定快照比较的唯一基准。

## 8. 常见情况速览

| 用户操作 | 稳定快照与库存的结果 | 最终库存结果 |
| --- | --- | --- |
| 什么也没动 | 每个快照物品都直接匹配原 VISIBLE 库存项 | 只更新 `last_seen_box`，必要时补强 `anchor_box` |
| 拿出苹果 A | A 没有对应快照物品，旧位置附近也没有新物品 | A → `OUT` |
| 放入新苹果 B | B 无法关联任何库存项 | 分配新 `item_id`，新增 B |
| 将苹果 A 从左整理到右 | A 未在旧位置匹配；B 匹配绑定 A 的 Track 路径 | A 的 `item_id` 不变，位置更新到右侧 |
| 新物品 C 挡住苹果 A | A 未匹配；A 原位置附近出现 C | A → `OCCLUDED`；C 后续作为新物品入库或关联旧库存项 |
| 移走遮挡物，苹果 A 重新露出 | 当前 B 匹配库存中 A 的 `OCCLUDED` 记录 | A → `VISIBLE`，恢复原 `item_id` |
| 拿出苹果 A，同时放入另一颗苹果 B | A 的 Track 路径不能唯一匹配 B | A → `OUT`；B 获得新的 `item_id` |
| 同一次手部操作先移动 A、再移动 B | 有多条绑定不同 `item_id` 的 Track | 每条 Track 只在唯一匹配时证明各自整理；不唯一就保守按遮挡/拿出/放入处理 |
