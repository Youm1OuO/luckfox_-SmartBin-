conda activate DL_env

1. 备份旧数据
    cd ~/Projects/workspace/luckfox_demo/tools/luckfox_pico_rkmpi_example/example/luckfox_pico_rtsp_yolov5n
    把 dataset 中的数据复制保存一份作为备份
    (其实直接将 dataset 改名成 dataset_old 就行)



2. 清空 dataset_raw
    cd ~/Projects/workspace/luckfox_demo/tools/luckfox_pico_rkmpi_example/example/luckfox_pico_rtsp_yolov5n
    rm -rf dataset_raw/*


3. 临时关掉画框，重新编译（只需做一次）
    /workspace/tools/luckfox_pico_rkmpi_example/example/luckfox_pico_rtsp_yolov5/src/main.cc
    # 找到 Step D 那段代码（画检测框的），在最前面加 /* 在最后面加 */：
    ```cpp
    /*for (const auto& t : tracks) {
        int x1 = (int)t.box.x1;
        ...（中间所有内容）...
        cv::putText(frame, text, cv::Point(x1, y1 - 6),
                    cv::FONT_HERSHEY_SIMPLEX, 0.6, color, 2);
    }*/
    ```
    # 找到紧跟着的 Step E 那段代码（左上角状态文字），同样加 /* */：
    ```cpp
    /*{
        fridge::SystemState ss = session.system_state();
        ...（中间所有内容）...
        cv::putText(frame, osd, cv::Point(8, 22),
                    cv::FONT_HERSHEY_SIMPLEX, 0.6, osd_color, 2);
    }*/
    ```

4. 拍新照片 (在WSL中执行指令, 但是要先打开板子的摄像头, 并且要先类别是, 并且修改 mian, 令色相头无法画框)
    cd ~/Projects/workspace/luckfox_demo/tools/luckfox_pico_rkmpi_example/example/luckfox_pico_rtsp_yolov5n
    mkdir -p dataset_raw
    ffmpeg -i rtsp://192.168.168.100:554/live/0 -vf "fps=2" -q:v 2 dataset_raw/batch1_%04d.jpg -v quiet -stats



5. 整理新照片
    cd ~/Projects/workspace/luckfox_demo/tools/luckfox_pico_rkmpi_example/example/luckfox_pico_rtsp_yolov5n
    python3 tools/make_dataset.py --input dataset_raw --output dataset_new 


6. 重命名+合并
    cd ~/Projects/workspace/luckfox_demo/tools/luckfox_pico_rkmpi_example/example/luckfox_pico_rtsp_yolov5n
    python3 tools/merge_dataset.py --old dataset_old --new dataset_new --output dataset



6. 可以删除 dataset_new 与 dataset_old 了



7. 标注照片 (在windows的cmd中)
    C:\Users\Youm1OuO\AppData\Local\Programs\Python\Python312\Scripts\labelme.exe \\wsl.localhost\Ubuntu\home\muyou\Projects\workspace\luckfox_demo\tools\luckfox_pico_rkmpi_example\example\luckfox_pico_rtsp_yolov5n\dataset\images\train

    C:\Users\Youm1OuO\AppData\Local\Programs\Python\Python312\Scripts\labelme.exe \\wsl.localhost\Ubuntu\home\muyou\Projects\workspace\luckfox_demo\tools\luckfox_pico_rkmpi_example\example\luckfox_pico_rtsp_yolov5n\dataset\images\val



8. 转换标注格式 (wls)
    cd ~/Projects/workspace/luckfox_demo/tools/luckfox_pico_rkmpi_example/example/luckfox_pico_rtsp_yolov5n
    python3 tools/convert_labels.py
