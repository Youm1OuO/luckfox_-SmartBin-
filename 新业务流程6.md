新业务流程6
=====================================================================


一、核心结论
---------------------------------------------------------------------

这个业务流程可以实现。

核心思路仍然沿用新业务流程5：
    我们不要求 YOLO 识别所有被遮挡、被压住的物品。
    我们只要求 YOLO 尽量稳定识别当前应该能看到的物品。
    真正要判断的是：
        1. 拿走
        2. 放入
        3. 整理 / 移动
        4. 遮挡 / 露出


本版本最重要的流程原则：
    不按“有没有识别到手”拆成两套主流程。

    所有库存变化最终都由两份无手稳定快照统一裁决：
        diff_snapshots(baseline_snapshot, stable_snapshot, operation_context)

    中间是否识别到手、手停留多久、物体有没有 ByteTrack 轨迹，
    都只记录到 operation_context 中，作为这次快照差异判断的辅助证据。


这里明确一个重要业务前提：
    如果一个物品处于【遮挡】状态，说明它当前不是直接可见物品。
    正常情况下，用户想拿走这个被压住的物品，需要先把上面的物品移开，让它变成【可见】。

所以本流程继续采用以下规则：
    【遮挡】不能直接变成【出库】

也就是说：
    一个物品要出库，必须先处于【可见】状态。
    状态路径应该是：
        【遮挡】 -> 【可见】 -> 【出库】


这个规则的好处：
    1. 实现简单
    2. 能避免 YOLO 漏检导致的误出库
    3. 符合大多数真实拿取动作
    4. 库存不会因为看不见就被随便删除


这个规则的限制：
    如果用户真的在完全遮挡状态下把底下的物品抽走，而且摄像头全过程都没有看到这个物品露出来，
    那么单摄像头系统没有可靠证据判断它被拿走。

这种情况本流程不强行处理。

如果后续业务确实需要覆盖这种极端情况，需要额外依赖：
    - 重量传感器
    - 多摄像头
    - 关门后的最终盘点
    - 后台库存人工校验

当前版本先不引入这些复杂度。




二、状态定义
=====================================================================

物品状态分为三类：
    【可见】 - 当前可以被 YOLO 稳定识别，物品在冰箱中
    【遮挡】 - 当前看不到或不稳定可见，但业务上认为物品仍在冰箱中
    【出库】 - 物品已经离开冰箱


注意：
    【可见】和【遮挡】都算在库存中。
    【出库】不算在库存中，但仍然保留一段时间的历史信息。


为什么保留【出库】信息：
    1. 防止 YOLO 抖动导致误判后无法恢复 item_id
    2. 处理“刚拿出去又放回来”的情况
    3. 处理短时间误出库后的重新匹配


状态转换规则：
    【可见】 -> 【遮挡】
        物品被新物品挡住，或者被移动物品压住。

    【遮挡】 -> 【可见】
        原本被挡住的物品重新露出来，并且被 YOLO 稳定识别。

    【可见】 -> 【出库】
        原本可见的物品消失，并且附近没有新物品遮挡它，也没有移动匹配证据。

    【出库】 -> 【可见】
        出库历史中的物品又被检测到，说明它被重新放回，或者之前是误出库。


禁止状态转换：
    【遮挡】 -> 【出库】
原因：
    遮挡状态的物品本来就看不到。
    如果允许它直接出库，YOLO 漏检、遮挡变化、光照变化都可能导致错误删除库存。




三、事件定义
=====================================================================

物品状态是库存结果。
事件是用户操作过程。

本流程主要识别以下事件：
    【拿走】
        一个原本【可见】的库存物品消失，并且不是被新物品遮挡，也不是被移动到别处。

    【放入】
        一个新检测到的稳定物品，无法匹配当前库存，也无法匹配出库历史。

    【整理】
        一个库存物品从位置 X 移动到位置 Y，仍然在冰箱中。

    【遮挡】
        一个原本【可见】的物品，因为另一个物品出现在它附近或覆盖它的位置，变成【遮挡】。

    【露出】
        一个原本【遮挡】的物品，因为上方物品被拿走或移开，重新变成【可见】。

    【忽略抖动】
        检测变化不满足稳定条件，或者与库存状态冲突，暂时不修改库存。


事件和状态的关系：
    【拿走】会导致：
        【可见】 -> 【出库】

    【放入】会导致：
        新建库存物品，状态为【可见】

    【整理】会导致：
        物品保持在库存中，状态为【可见】，但更新 bbox / track_id / 外观信息

    【遮挡】会导致：
        【可见】 -> 【遮挡】

    【露出】会导致：
        【遮挡】 -> 【可见】




四、身份匹配标准
=====================================================================

新业务流程5 中只有一种“严格身份匹配”。
新业务流程6 改成分层匹配 / 判断，并增加一个事件级整理判定。

原因：
    不同场景需要不同严格程度。
    如果所有地方都用同一套严格匹配，会把整理误判成“旧物品出库 + 新物品入库”。

匹配 / 判断分层：
    1. snapshot_cluster_match：快照内聚合
    2. snapshots_similar：快照之间差异判断
    3. strict_origin_match：原位严格匹配
    4. reid_match：外观重识别分数

事件级整理判定：
    relocation_match：reid_match + OperationContext 移动证据 + 候选唯一性



4.1 快照内聚合匹配 snapshot_cluster_match
---------------------------------------------------------------------

用途：
    多帧生成一个稳定快照时，把 N 帧里的同一个物品合并到一起。

要求：
    这个匹配不能太严格。
    因为 YOLO 的 bbox 每一帧都会抖动, 
    甚至物体在移动(整理), 这时物体的框的位置可能会变得很厉害 (移动的速度越快的话, 框的位置就会变化得越厉害)。

建议条件：
    1. 类别一致
    2. 中心点距离小于较宽阈值
    3. bbox 面积比例相近
    4. IoU 有一定重叠即可
    5. 颜色特征只作为辅助，不作为强硬条件

示例：
    same_class == true
    center_distance < cluster_center_threshold
    area_ratio 在 0.5 ~ 2.0 之间
    IoU > 0.2



4.2 快照之间差异判断 snapshots_similar
---------------------------------------------------------------------

用途：
    判断 current_snapshot 与 contrast_snapshot 是否已经足够稳定，
    能不能把 current_snapshot 升级为 stable_snapshot。

注意：
    snapshots_similar 不是身份重识别。
    它不回答“这个物品是不是库存里的同一个 item_id”。
    它只回答：
        “这两份无手快照表示的画面是否已经基本不再变化？”

    因此它必须比 snapshot_cluster_match 更严格。
    不能因为大部分旧物品稳定，就忽略某个正在滚动、滑动、晃动的新物品。


基本原则：
    1. 两份快照都必须 hand_present = false。
    2. 先比较物品数量。
    3. 如果数量不同，直接判定为【差异较大 / 不稳定】。
       原因：
           放入、拿走、露出、遮挡都会导致数量变化。
           第一次数量变化只能说明“画面发生了变化”，不能说明“变化已经稳定”。
           必须先把 contrast_snapshot 更新为 current_snapshot，
           等下一份无手快照再次确认。

    4. 数量相同时，必须对两份快照中的物品做一对一原位稳定匹配。
    5. 每个 contrast_snapshot 物品都必须能在 current_snapshot 中找到唯一匹配。
    6. 每个 current_snapshot 物品也只能被匹配一次。
    7. 只要存在未匹配物品，就判定为【差异较大 / 不稳定】。


稳定匹配条件 snapshot_stable_match：
    用于判断“两份候选快照里的某个物品是否还在原来的稳定位置附近”。

    建议条件：
        1. 类别必须一致
        2. 中心点距离必须较小
        3. bbox 面积比例必须相近
        4. IoU 必须达到 strict_origin_match 附近的要求
        5. 外观 / 颜色差异可以作为辅助

    它可以复用 strict_origin_match 的大部分几何阈值，
    但不要使用 relocation_match 或 reid_match 来放宽位置变化。

原因：
    如果用 reid_match 认为“同类且外观相似就是稳定”，
    那么一个圆形物品从 X 滚到 Y 时，
    系统会误以为两份快照差异很小，
    从而提前执行 diff_snapshots，产生重影或误入库。


特殊规则：同类未匹配物品
    如果 contrast_snapshot 中存在一个未匹配成功的物品 A，
    current_snapshot 中也存在一个未匹配成功的物品 B，
    并且 A 与 B 类别一致，
    则不能把 A 和 B 当作稳定匹配。

    即使 A 与 B 外观相似，也不能在 snapshots_similar 中使用 reid_match 把它们连接起来。

    这种情况通常表示：
        1. 新放入物品还在滚动 / 晃动
        2. 用户正在整理同类物品
        3. YOLO bbox 仍在大幅变化
        4. 同类相似物品还没有稳定区分

    处理方式：
        判定为【差异较大 / 不稳定】。
        设置：
            contrast_snapshot = current_snapshot
            current_snapshot = null
            stable_snapshot = null
        等下一份无手快照再次确认。


判定结果：
    snapshots_similar = true：
        1. 两份快照都无手
        2. 物品数量完全一致
        3. 每个物品都能一对一 snapshot_stable_match
        4. 没有未匹配物品
        5. 没有同类物品发生明显位置变化

    snapshots_similar = false：
        1. 任意一份快照有手
        2. 物品数量不同
        3. 存在未匹配物品
        4. 存在同类物品从一个位置变到另一个位置
        5. bbox 位置 / 面积 / IoU 变化超过稳定阈值


例子：新物品放入后滚动
    contrast_snapshot：
        原有物品稳定，新苹果出现在 X

    current_snapshot：
        原有物品稳定，新苹果滚到 Y

    虽然大部分旧物品都稳定，
    但新苹果 X 和 Y 不能通过 snapshot_stable_match 原位匹配。

    结果：
        snapshots_similar = false
        不执行 diff_snapshots
        等苹果最终停稳后，再生成 stable_snapshot


例子：真正稳定后的放入
    contrast_snapshot：
        新苹果在 Y

    current_snapshot：
        新苹果仍在 Y，所有其他物品也原位匹配

    结果：
        snapshots_similar = true
        current_snapshot 可以升级为 stable_snapshot
        再与 baseline_snapshot 做 diff_snapshots


注意：
    snapshots_similar 应该偏保守。
    宁可多等一份快照，也不要在画面仍在变化时提前裁决库存。

    这一步只决定“能不能进入 diff_snapshots”。
    它不直接判断【拿走】、【放入】、【整理】、【遮挡】、【露出】。



4.3 原位严格匹配 strict_origin_match
---------------------------------------------------------------------

用途：
    判断“当前检测到的物品，是否就是库存中原来位置上的那个物品”。

适用场景：
    1. 初始化时，检测结果与后台库存匹配
    2. 快照对比时，判断物品是否仍在原位置
    3. 判断遮挡物品是否重新露出到原位置附近
    4. 判断 YOLO 抖动是否应该忽略

建议条件：
    1. 类别必须一致
    2. 中心位置：center_distance(A, B) < eps_center
    3. 面积差异：area_distance(A, B) < eps_area
    4. IoU：IoU(A, B) > eps_iou
    5. 外观差异：颜色 / patch / 直方图差异小于阈值


注意：
    这个匹配用于“原地是不是同一个物品”。
    不用于判断“移动后是不是同一个物品”。



4.4 外观重识别匹配 reid_match
---------------------------------------------------------------------

用途：
    判断两个不同位置的物品在外观上是否像同一个物品。

注意：
    reid_match 只能回答：
        “A 和 B 看起来像不像同一个物品？”

    reid_match 不能单独回答：
        “A 是否被整理到了 B 的位置？”

    因为两个同类物品可能外观很像。
    仅靠宽松外观匹配，很容易把“旧物品出库 + 新物品放入”
    误判成“整理”。

适用场景：
    1. 为整理判断提供外观分数
    2. 手拿起后又放下
    3. 物品被推到别的位置
    4. 出库物品短时间内又被放回


建议条件：
    1. 类别一致
    2. 外观相似
    3. bbox 尺寸比例相近
    4. 宽高比例相近
    5. 中心距离和 IoU 不作为硬条件


注意：
    reid_match 的置信度一般低于 strict_origin_match。
    如果同类相似物品很多，不要强行绑定 item_id。

    reid_match 的结果应该是分数，而不是直接 true / false：
        reid_score(A, B)

    它只作为 relocation_match 的一个输入。



4.5 整理判定 relocation_match
---------------------------------------------------------------------

用途：
    判断“baseline 中消失的物品 A，是否被整理到了 stable 中新出现的物品 B 的位置”。

relocation_match 不是单纯的身份匹配。
它是事件级判断。

输入：
    A                         # disappeared_objects 中的物品
    B                         # appeared_objects 中的物品
    operation_context         # 两份快照之间收集到的辅助证据


基础条件：
    1. A 和 B 类别一致
    2. A 在 baseline_snapshot 中存在
    3. A 在 stable_snapshot 的原位置消失
    4. B 在 stable_snapshot 中新出现
    5. reid_match(A, B) 外观分数足够高
    6. A 和 B 都没有被前面的匹配步骤消耗


还需要至少一个移动 / 操作证据：
    强证据：
        1. 同一个 track_id 从 A 原位置连续移动到 B 新位置
        2. TrackEvidence 中存在高置信 A -> B 连续轨迹
        3. operation_context.moved_item_candidates 中存在 A -> B
        4. HeldProxyEvidence 中存在 A 由手轨迹代理移动到 B 附近
        5. A 曾经是 confirmed_held，且手轨迹从 A 附近移动到 B 附近

    中等证据：
        1. A 曾经是 candidate_held
        2. operation_context.hand_long_present = true
        3. 有物体 track 在 A 原位置到 B 新位置方向移动，但 track_id 中途断过
        4. track_id 不一致，但断点前后类别、外观、方向、位置都能接上
        5. A / B 附近存在短轨迹片段，能支持 A -> B 方向移动
        6. A 有 held_proxy_evidence，但手轨迹与 B 的对应关系不够明确

    负证据：
        1. A 的 track 明确没有离开 A 原位置
        2. A 的 track 明确移动到了 B 以外的位置
        3. B 的 track 在 A 消失前就已经稳定存在
        4. 同一个 track 被多个候选同时抢占
        5. track 轨迹方向与 A -> B 明显相反


候选唯一性要求：
    如果 A 可以匹配多个 B，或者多个 A 都可以匹配同一个 B，
    不能直接高置信判定整理。

    建议：
        best_score - second_score >= relocation_margin
        才允许高置信绑定 item_id。


判定结果：
    confirmed_relocation：
        reid_match 高分
        并且有强移动证据
        并且候选唯一
        并且没有明显负证据

    low_confidence_relocation：
        reid_match 高分
        但只有中等证据，或者候选不够唯一，
        或者存在轻微 track 冲突

    no_relocation：
        只有外观相似，没有移动 / 操作证据
        或者候选冲突严重
        或者存在明确负证据


注意：
    只有 confirmed_relocation 才直接执行【整理】并更新 item_id 位置。

    low_confidence_relocation 不应该直接强行改库存。
    可以记录候选和日志，必要时回退到普通消失 / 新出现逻辑。

    no_relocation 不能因为 reid_match 外观相似就判整理。

    track_id / ByteTrack 轨迹的使用原则：
        track_id 一致且轨迹连续：
            强烈提高整理置信度。

        track_id 断开或不一致：
            不直接否定整理。
            只能说明没有强轨迹证据，需要靠外观、HeldProxyEvidence、手轨迹和候选唯一性补充。

        track_id 明确冲突：
            降低置信度。
            严重冲突时，阻止 confirmed_relocation。



4.6 外观特征建议
---------------------------------------------------------------------

外观特征可以从简单到复杂逐步实现：
    第一版：
        bbox 裁剪图 resize 到 16x16，计算平均 RGB 差异。

    第二版：
        使用 HSV 直方图，降低光照变化影响。

    第三版：
        分 patch 计算颜色差异，避免局部反光影响整体判断。

    第四版：
        使用轻量 ReID 特征。


注意：
    RGB 差异不要在所有场景下都作为绝对硬条件。
    冰箱灯、反光、手遮挡、包装角度变化都会影响颜色。

建议：
    strict_origin_match 中颜色可以比较严格。
    snapshot_cluster_match 中颜色只做辅助。
    reid_match 中颜色 / 外观应作为主要依据之一，但要允许一定误差。




五、“附近”的量化方法
=====================================================================

继续使用新业务流程5 中的自适应归一化中心距离。

核心公式：
    normalized_distance = center_distance(A, C) / min(A的对角线, C的对角线)

其中：
    center_distance(A, C) = 两个 bbox 中心点的欧氏距离
    A的对角线 = sqrt(A.width^2 + A.height^2)
    C的对角线 = sqrt(C.width^2 + C.height^2)
    min() 选择较小的那个对角线做分母


判定阈值：
    < 0.5
        C 几乎就在 A 的位置上，判定为【附近】

    0.5 ~ 1.0
        C 紧挨着 A，仍然判定为【附近】

    > 1.0
        C 离 A 比较远，判定为【不附近】


建议：
    第一版先使用 1.0 作为阈值。
    如果误判遮挡太多，可以改成 0.7。
    如果漏判遮挡太多，可以改成 1.5。




六、数据结构建议
=====================================================================


6.1 库存物品 InventoryItem
---------------------------------------------------------------------

每个库存物品至少保存：
    item_id
    class_id
    class_name
    state                     # visible / occluded / out
    bbox                      # 最近一次可靠 bbox
    last_visible_bbox         # 最近一次可见时的 bbox
    appearance_feature        # 颜色 / patch / 直方图 / ReID 特征
    track_id                  # ByteTrack 最近关联 ID，可为空
    score                     # YOLO 置信度
    last_seen_snapshot_id
    last_seen_frame_id
    occluded_by_item_id       # 可选，记录被谁挡住
    updated_at


注意：
    【可见】和【遮挡】物品在本地库存清单中。
    【出库】物品不在本地库存清单中，但保留在 out_stock_archive 中。



6.2 快照物品 SnapshotObject
---------------------------------------------------------------------

每个快照物品至少保存：
    snapshot_object_id
    class_id
    class_name
    bbox
    appearance_feature
    score
    count_in_window           # N 帧内出现次数
    track_id                  # 如果 ByteTrack 能关联上
    source_frame_ids



6.3 快照 Snapshot
---------------------------------------------------------------------

每个快照至少保存：
    snapshot_id
    frame_range
    objects
    hand_present
    created_at



6.4 操作上下文 OperationContext
---------------------------------------------------------------------

OperationContext 不是另一套业务流程。

它只是记录从 baseline_snapshot 到下一份 stable_snapshot 之间发生过的辅助证据。

每次 baseline_snapshot 更新后，创建或重置一份 OperationContext：
    context_id
    baseline_snapshot_id
    hand_seen                 # 这段时间是否识别到过手
    hand_frame_count
    hand_track_ids
    hand_long_present         # 手是否长时间停留
    candidate_held_items      # 手可能遮挡/接触过的物品
    confirmed_held_items      # 手遮挡后持续消失、需要手轨迹代理的物品
    held_proxy_evidences      # 物品轨迹被手轨迹临时代理的证据列表
    track_evidences           # ByteTrack 轨迹证据列表
    moving_tracks             # ByteTrack 认为发生明显移动的 track_id 列表
    moved_item_candidates     # 可能发生整理的物品候选
    unstable_frame_count
    created_at
    updated_at


重要约束：
    OperationContext 的起点就是当前 baseline_snapshot。
    它不单独保存“手前快照”和“手后快照”。

    后续无论中间有没有识别到手，
    都是等待下一份无手稳定 stable_snapshot 出现后，
    统一执行一次快照差异判断：
        diff_snapshots(baseline_snapshot, stable_snapshot, operation_context)

    operation_context 只提供辅助证据：
        手是否出现过
        手是否长时间停留
        哪些物品疑似被手遮挡
        哪些物品在被手遮挡后使用手轨迹作为代理轨迹
        哪些物品有 ByteTrack 移动轨迹

    它不直接决定库存变化。
    库存变化仍然由 baseline_snapshot 和 stable_snapshot 的差异统一裁决。

    当出现带手快照时：
        baseline_snapshot 不更新
        contrast_snapshot 不更新为带手快照
        带手快照不作为库存对比基准
        只把“手出现过”的证据写入 operation_context

    当 stable_snapshot 完成一次 diff 后：
        baseline_snapshot = stable_snapshot
        清空并重建 OperationContext

    不能再让同一个 stable_snapshot 进入普通无手对比，
    否则就会把同一次变化重复处理两遍。



6.5 待确认整理 PendingRelocation
---------------------------------------------------------------------

PendingRelocation 用来保存低置信整理候选。

一句话理解：
    PendingRelocation 不是主业务流程。
    它只是一个轻量临时账本，
    记录“这个消失的 A 和这个新出现的 B 可能是一次整理移动”。

    它的目的不是强行判定整理，
    而是在证据不够时，
    防止 A / B 立刻被后续流程拆成【出库】+【放入】。

它不能只放在 OperationContext 中。
原因：
    OperationContext 在每次 stable_snapshot 完成 diff 后会清空并重建。
    但 pending_relocation 需要保留到下一份 stable_snapshot 再确认。


每条 PendingRelocation 至少保存：
    pending_id
    item_id_A
    class_id
    old_bbox                  # A 在 baseline_snapshot 中的位置
    candidate_new_bbox        # B 在 stable_snapshot 中的位置
    snapshot_object_B
    class_id_B
    appearance_feature_B
    reid_score
    evidence_summary
    created_snapshot_id
    last_checked_snapshot_id
    stable_count
    expire_after_stable_count
    status                    # pending / confirmed / cancelled / expired


使用原则：
    pending_relocation 是跨快照的临时记录。
    它不等于【整理】事件。
    它也不等于库存状态。
    它不能替代 diff_snapshots 主流程。

    PendingRelocation 必须保持轻量：
        1. 只保存 A / B 候选关系和少量证据
        2. 只负责冻结 A / B，避免本轮被 10.3 / 10.4 消费
        3. 不直接修改库存
        4. 不直接提交【整理】事件
        5. 不直接提交【出库】或【放入】事件
        6. 不能长期占用候选，超过确认窗口必须取消或回退

    它不能做成“第二套整理主流程”。

    pending 期间：
        A 不立即判【出库】
        B 不立即判【放入】
        A / B 标记为 pending_reserved

    pending 结束时：
        confirmed -> 提交【整理】事件，更新 A 到 B 的位置
        cancelled -> 取消候选，视情况回退普通消失 / 新出现逻辑
        expired -> 记录低置信日志，按当前最新稳定快照重新判断




6.6 待确认新物品 PendingNewObject
---------------------------------------------------------------------

PendingNewObject 用来保存“尚未确认是真的放入”的新出现物品。

一句话理解：
    正在整理物品 A 时，
    如果突然多出来一个与 A 同类的新物品 B，
    不要马上把 B 加入库存。
    等下一份 stable_snapshot 中 B 仍然独立存在时，
    才允许确认 B 是真正【放入】。

它解决的问题和 PendingRelocation 不一样：
    PendingRelocation 处理的是：
        baseline 中有 A 消失，
        stable 中有 B 出现，
        A / B 可能是同一个物品被整理移动。

    PendingNewObject 处理的是：
        baseline 中原有物品都还在，
        stable 中却额外出现了一个同类新物品 B。

这种 B 可能是真正新放入的物品，也可能是：
    1. 用户整理已有同类物品时，YOLO 在移动路径上产生的临时误检
    2. 同一个物品在移动过程中被短暂识别成两个位置
    3. 手、反光、局部包装边缘被误识别成同类物品
    4. 快照投票中某个移动中间态刚好通过了 N*s

如果这类 B 直接进入【放入】逻辑，
就会在库存清单中多出一个假的同类 item_id。

更严重的是：
    一旦假的 B 被加入库存，
    后续系统可能继续把这个假 item_id 当成真实物品参与 HELD / relocation_match，
    甚至对它提交【整理】事件，
    从而把重影“坐实”。


每条 PendingNewObject 至少保存：
    pending_id
    class_id
    candidate_bbox
    snapshot_object_B
    appearance_feature_B
    score
    created_snapshot_id
    last_checked_snapshot_id
    stable_count
    expire_after_stable_count
    related_existing_item_ids   # 同类且最近被手/移动影响过的库存物品
    reason                      # same_class_operation / uncertain_detection / init_extra
    status                      # pending / confirmed / cancelled / merged / expired


生成条件：
    在 10.4 准备把某个 appeared_object B 判定为【放入】之前，
    如果满足以下任意条件，不要立即新建库存 item：
        1. 当前正在整理 / 移动某类物品 A，并且 B 与 A 同类
        2. 当前 OperationContext 中存在同类物品被手接触、遮挡、HELD 或移动过
        3. 当前 OperationContext 中存在同类 moving_track / moved_item_candidate
        4. 当前轮快照中同类数量突然增加，但没有明确“新物品从外部放入”的强证据
        5. B 与某个同类库存物品高度重合，或者几乎在同一位置

    满足时：
        创建 pending_new_object(B)
        B 标记为 pending_reserved
        本轮不提交【放入】事件
        本轮不新建库存 item_id


确认规则：
    下一份 stable_snapshot 到来后：

    如果 B 消失：
        取消 pending_new_object。
        认为 B 是 YOLO 误检、移动中间态或短暂重影。

    如果 B 与某个已有同类库存物品 strict_origin_match，
    或者 bbox 高度重合 / IoU 较高 / 中心几乎同位置：
        取消 pending_new_object。
        将 B 视为已有物品的位置 / bbox 更新，不新增 item_id。

    注意：
        这里不能只用“附近”判断。
        两个真实物品可能上下相邻、左右相邻，距离很近，
        但只要 bbox 没有明显重合、中心不在同一位置，
        就不能把它们当成同一个物品。

    如果某个同类库存物品 A 的旧位置消失，
    并且 B 仍然稳定存在：
        优先把 pending_new_object 转成 pending_relocation(A, B)，
        或者在证据足够强时直接按【整理】处理。
        不直接新增 item_id。

    如果所有已有同类库存物品都仍然稳定存在，
    并且 B 在下一份 stable_snapshot 中仍然独立稳定存在：
        确认 B 是真正新放入物品。
        新建库存 item_id。
        提交【放入】事件。

        如果配置为更保守模式，
        可以要求 B 连续 2 份 stable_snapshot 都独立稳定存在后再 confirmed。

    如果超过确认窗口仍无法判断：
        按“优先保证库存数量正确”的原则处理。
        对同类相似物品，宁可继续 pending 或记录低置信日志，
        不要轻易新增一个可能是重影的 item_id。


重要原则：
    PendingNewObject 不是【放入】事件。
    PendingNewObject 也不是库存状态。

    pending 期间：
        B 不进入本地库存清单
        B 不参与后续 relocation_match 的整理源
        B 只能作为“待确认新物品候选”

    只有 confirmed 后，才允许给 B 分配正式 item_id。


刚确认的新物品 probation：
    即使 pending_new_object 最终 confirmed，
    新建出来的 item_id 也建议保留一个很短的 probation 窗口。

    probation 期间：
        该 item 可以作为库存可见物品展示
        但不要立刻作为 relocation_match 的“消失源 A”
        不要让刚刚确认的新 item 立刻被整理逻辑再次强化

    目的：
        防止误检刚被加入库存后，
        下一轮又被系统当成真实物品继续“整理确认”，
        从而让假的重影变得更难清除。


建议：
    pending_new_object_confirm_frames = 1 ~ 2 个稳定快照周期。
    new_item_probation_frames = 1 个稳定快照周期。



6.7 HELD 代理证据 HeldProxyEvidence
---------------------------------------------------------------------

HELD 的核心目的：
    不是直接判断“物品被拿走”。

    HELD 的主要目的，是在物品被手遮挡、YOLO 无法继续识别物品、
    物品自己的 ByteTrack 轨迹断开时，
    使用手的轨迹临时代理这个物品的运动轨迹。


也就是说：
    物品不是变成手。
    物品的 track_id 也不要直接改成手的 track_id。

    正确做法是在业务层建立代理关系：
        item_id A
        原物品 track_id = object_track_id
        遮挡它的手 track_id = held_by_hand_track_id
        A 的 proxy_trajectory 临时跟随手轨迹


每条 HeldProxyEvidence 至少保存：
    item_id
    original_object_track_id
    held_by_hand_track_id
    last_visible_bbox
    hand_bbox_at_hold_start
    object_to_hand_offset       # 物品最后位置相对手 bbox 的偏移，可选
    proxy_trajectory_points     # 用手轨迹推出来的物品代理轨迹
    held_start_frame_id
    held_end_frame_id
    release_candidate_bbox
    confidence


生成条件：
    1. 手 bbox 与某个【可见】物品 A 的 bbox 明显重叠
    2. A 随后持续不可见，或者 A 的物体 track 断开
    3. 手 track 在后续帧中仍然存在并移动


使用方式：
    如果 stable_snapshot 中出现新位置物品 B，
    并且 B 与 A 外观相似，
    并且 A 的 proxy_trajectory 从 A 原位置移动到 B 附近，
    则强烈提高 relocation_match(A, B) 的置信度。

    如果手离开后 A 在原位置重新出现，
    则取消 HELD，认为只是短暂遮挡。

    如果手离开后 A 消失，
    则 HELD 只是提高【拿走】置信度，
    最终仍然由 diff_snapshots 的消失物品逻辑裁决。


注意：
    不要把手 track_id 写入库存物品的 track_id 字段。
    库存物品可以记录 held_by_hand_track_id / proxy_trajectory，
    但 item_id 和物体原本的身份信息不能被手 track 覆盖。


HELD 代理冲突规则：
    同一个 hand track 同时遮挡多个物品：
        1. 按 hand-object overlap_ratio 排序
        2. 再按遮挡持续时间排序
        3. 再按物品中心到手中心的距离排序
        4. 分数最高的物品可以建立 high_confidence HeldProxyEvidence
        5. 其他物品只能建立 low_confidence HeldProxyEvidence

    同一个物品被多个 hand track 影响：
        1. 选择重叠面积最大
        2. 遮挡持续时间最长
        3. 轨迹最连续的 hand track 作为主代理
        4. 其他 hand track 只作为辅助证据

    同一个 hand track 被多个 item 抢占：
        不允许多个 item 同时拥有 high_confidence HeldProxyEvidence。
        如果无法区分，全部降级为 low_confidence。

    low_confidence HeldProxyEvidence：
        只能提高 relocation_match 的中等证据分。
        不能单独触发 confirmed_relocation。



6.8 轨迹证据 TrackEvidence
---------------------------------------------------------------------

ByteTrack 的 track_id 是临时轨迹 ID，不是库存 item_id。

因此：
    track_id 可以作为强证据。
    track_id 不能作为绝对身份。


每条轨迹证据至少保存：
    track_id
    class_id
    start_bbox
    end_bbox
    trajectory_points         # 中间帧中心点 / bbox 序列
    start_frame_id
    end_frame_id
    lost_frame_count
    associated_item_id        # 可选，轨迹起点附近最可能对应的库存 item_id
    associated_snapshot_id
    associated_snapshot_object_id
    occluded_by_hand          # 是否曾与手 bbox 重叠
    track_confidence


使用原则：
    track_id 连续从 A 原位置移动到 B 新位置：
        强烈提高 relocation_match 置信度。

    track_id 中途断开：
        不直接否定整理。
        可以结合类别、外观、运动方向、断点前后位置，作为中等证据。

    track_id 不一致：
        不直接否定整理。
        只说明 ByteTrack 没有提供强连续证据。

    track_id 明确冲突：
        降低 relocation_match 置信度，严重时阻止 confirmed_relocation。

    明确冲突的例子：
        1. A 的 track 清楚地停在原位置，没有移动到 B
        2. A 的 track 移动到了其他位置
        3. B 的 track 在 A 消失前就已经稳定存在于 B 位置
        4. 同一个 track 同时被多个候选 A / B 抢占




七、初始化流程（开门时）
=====================================================================

开门时：
    1. 向后台请求当前库存清单
    2. 等待画面无手
    3. 采集 N 帧，生成第一份稳定快照
    4. 用第一份稳定快照与后台库存做匹配


初始化匹配规则：
    如果后台库存有历史 bbox / appearance_feature：
        对每个后台库存物品：
            如果能在初始快照中 strict_origin_match：
                状态设为【可见】
                更新 bbox / 外观 / track_id

            如果不能匹配：
                状态设为【遮挡】
                保留后台库存中的原始信息

    如果后台库存只有类别 / 数量，没有可靠 bbox / appearance_feature：
        使用 class_count_match 弱初始化。

        对每个类别：
            1. 统计后台库存数量 backend_count
            2. 统计初始稳定快照中该类别可见数量 visible_count
            3. min(backend_count, visible_count) 个物品设为【可见】
               但标记为 low_confidence_init
            4. backend_count - visible_count 个后台物品设为【遮挡】
            5. visible_count - backend_count 个快照物品进入“待确认新物品”

        注意：
            class_count_match 只能保证类别数量大致合理。
            它不能高置信绑定具体 item_id。
            后续需要依赖稳定快照、外观特征和出入库事件逐步修正。


对初始快照中“后台库存没有匹配上”的物品：
    这类物品可能是：
        1. 后台库存漏记
        2. 上次关门后库存没有同步
        3. YOLO 误检


建议第一版处理：
    如果该物品连续多个稳定快照都存在，则作为【可见】新物品加入本地库存。
    同时记录一条“初始化发现后台未记录物品”的日志。




八、快照生成流程
=====================================================================

由于单帧 YOLO 容易抖动，所以不使用单帧作为快照。

快照由 N 帧投票生成：
    N 必须为奇数。
    第一版建议 N = 3。
    稳定阈值 s 建议为 60%。


生成方式：
    1. 连续采集 N 帧 YOLO 检测结果
    2. 每帧检测结果先送入 ByteTrack
    3. 用 snapshot_cluster_match 将 N 帧中的同一物品聚合
    4. 聚合后 count >= N * s 的物品保留
    5. count 不足的物品剔除


手的特殊规则：
    手是特殊类别。
    N 帧中只要任意一帧检测到手，该快照就标记 hand_present = true。

也就是说：
    普通物品按 count 投票保留。
    手只要出现过一次就保留。

    但 hand_present 只用于判断“这份快照不能参与库存对比”。
    不在快照流程中根据手的位置直接修改库存状态。


原因：
    手出现意味着画面处于操作阶段。
    即使手只被检测到一帧，也不应该把这个快照当作稳定库存快照。




九、稳定快照选择
=====================================================================

快照不是生成出来就立即用于库存对比。

只有满足以下条件的快照才能用于库存对比：
    1. hand_present = false
    2. 当前快照与前一候选快照差异不大
    3. 画面已经进入稳定状态


变量定义：
    baseline_snapshot     # 上一次已经用于库存裁决的无手稳定快照
    contrast_snapshot     # 用来判断当前画面是否稳定的无手候选快照
    current_snapshot      # 当前刚生成的快照
    stable_snapshot       # 新确认的无手稳定快照


流程：
    1. 开门初始化后，设置：
        baseline_snapshot = 初始稳定快照
        contrast_snapshot = 初始稳定快照
        current_snapshot = null
        stable_snapshot = null
        operation_context = new OperationContext(baseline_snapshot)

    2. 获取下一份快照，设置：
        current_snapshot = 新快照

    3. 如果 current_snapshot 中存在"手"：
        这份快照只表示“当前处于操作阶段”。
        它不能参与库存快照对比。
        它也不能用于判断画面是否稳定。

        必须设置：
            current_snapshot = null
            stable_snapshot = null

        并且必须保持：
            baseline_snapshot 不变
            contrast_snapshot 不变

        然后清空本轮 snapshot_buffer，等待后续无手快照。

        注意：
            手的详细判断由 OperationContext 和每帧 ByteTrack 记录为辅助证据。
            不在 current_snapshot 这个分支里处理“拿走 / 放下 / 整理”。

    4. 如果 current_snapshot 与 contrast_snapshot 差异较大：
        差异大小必须按照 4.2 snapshots_similar 的规则判断。

        说明画面仍在变化
        设置：
            contrast_snapshot = current_snapshot
            current_snapshot = null
            stable_snapshot = null
        继续等待下一份快照

    5. 如果 current_snapshot 与 contrast_snapshot 差异较小：
        也就是说 snapshots_similar(contrast_snapshot, current_snapshot) = true。

        说明画面已经稳定
        设置：
            stable_snapshot = current_snapshot

    6. 当 baseline_snapshot 和 stable_snapshot 都存在：
        执行统一快照差异判断：
            events = diff_snapshots(
                baseline_snapshot,
                stable_snapshot,
                operation_context
            )
        判断完成后：
            baseline_snapshot = stable_snapshot
            contrast_snapshot = stable_snapshot
            current_snapshot = null
            stable_snapshot = null
            operation_context = new OperationContext(baseline_snapshot)


注意：
    带"手"快照不参与库存快照对比。
    带"手"快照不能更新 baseline_snapshot。
    带"手"快照不能更新 contrast_snapshot。

    current_snapshot 与 contrast_snapshot 的“差异较小”，
    不能只看全局匹配比例。
    只要存在新放入物品滚动、同类物品位置变化、数量变化、
    或者任何未通过 snapshot_stable_match 的对象，
    就应该认为画面还不稳定。

    baseline_snapshot 永远表示：
        “最后一次已经完成库存裁决的无手稳定快照”。

    contrast_snapshot 永远表示：
        “当前正在用于稳定性判断的无手候选快照”。

    但带手期间 ByteTrack 仍然继续运行，
    并由 OperationContext 维护操作证据。




十、快照差异判断
=====================================================================

快照差异判断的目标：
    对比 baseline_snapshot 和 stable_snapshot，
    判断这段稳定变化中发生了什么库存事件。

统一入口：
    events = diff_snapshots(
        baseline_snapshot,
        stable_snapshot,
        operation_context
    )

其中：
    baseline_snapshot 是上一次已裁决的无手稳定快照。
    stable_snapshot 是新确认的无手稳定快照。
    operation_context 是这两份快照之间收集到的辅助证据。

注意：
    无论中间有没有识别到手，最终都走这个统一入口。
    不再把业务分成“有手流程”和“无手流程”两套主流程。

    普通【拿走】和【放入】主要由快照差异裁决。
    【整理】、长时间遮挡、物体移动时，才重点使用 operation_context 中的 ByteTrack / HELD 证据。


为避免“整理”被误判成“出库 + 入库”，流程分成五步：
    第零步：建立候选关系，但不修改库存
    第一步：优先处理整理 / 移动
    第二步：处理消失物品
    第三步：处理新出现物品 / 待确认新物品
    第四步：例行清理

注意注意：
    这几步处理逻辑，一定要按照这个顺序执行。
    顺序不能乱


整理期间不处理普通进出库：
    如果 OperationContext 显示当前正在整理 / 移动物品，
    那么本轮 diff 中与整理相关的普通【拿走】/【放入】不要急着提交。

    典型触发条件：
        1. 有物品被手接触、遮挡、HELD
        2. 有同类物品发生明显移动
        3. disappeared_objects 和 appeared_objects 中存在同类候选
        4. 新出现物品与正在整理的类别相同

    处理方式：
        1. 优先执行 10.2 整理 / 移动判断
        2. 低置信 A -> B 进入 PendingRelocation
        3. 整理期间额外冒出的同类新物品进入 PendingNewObject
        4. 被 pending_reserved 的对象不进入 10.3 / 10.4 普通出入库
        5. 等下一份 stable_snapshot 再确认

    注意：
        这不是说整个冰箱都禁止进出库。
        只冻结与当前整理上下文相关的候选物品。

        与整理无关、类别不同、证据清晰的普通放入 / 拿走，
        仍然可以按 10.3 / 10.4 正常处理。

    目的：
        避免用户正在整理 A 时，
        系统把 A 的短暂消失判成【出库】，
        或把移动过程中的同类临时框判成【放入】。




10.1 第零步：建立候选关系
---------------------------------------------------------------------

先根据 strict_origin_match (原位严格匹配) 找出：
    same_position_objects (相同位置物品)
        baseline 中有，stable 中也有，而且原位严格匹配成功。

    disappeared_objects (消失物品)
        baseline 中有，stable 中没有原位匹配。

    appeared_objects (新出现物品)
        stable 中有，baseline 中没有原位匹配。


注意：
    这一步只建立候选列表。
    不修改库存。



10.2 第一步：优先处理整理 / 移动
---------------------------------------------------------------------

这一章仍然必须保留。

它解决的是：
    baseline 中有旧物品 A 消失，
    stable 中有新位置物品 B 出现，
    A / B 可能是同一个物品被整理移动。

它不能解决所有重影问题。

如果 baseline 中原有同类物品都还在，
stable 中却额外冒出一个同类新物品 B，
这种情况不属于 10.2 的主要职责，
应该交给 10.4 的 PendingNewObject 处理。


整理不能只靠 reid_match。

本步骤先做候选，再做事件级判断：
    1. 对 disappeared_objects 和 appeared_objects 做 reid_match，得到外观相似候选。
    2. 对外观相似候选执行 relocation_match。
    3. 只有 confirmed_relocation 才判定为【整理】。


对每个 disappeared_object A：
    1. 从 appeared_objects 中找同类候选 B
    2. 计算 reid_score(A, B)
    3. 保留 reid_score 足够高的候选
    4. 将候选交给 relocation_match(A, B, operation_context)


relocation_match 返回 confirmed_relocation：
    说明：
        A 高置信被整理到了 B 的位置。

    必须同时满足：
        1. reid_match 外观分数高
        2. operation_context 中有强移动 / 操作证据
        3. 候选唯一性足够好

    处理方式：
        1. A 保持在库存中
        2. A 状态设为【可见】
        3. 用 B 的 bbox / score / appearance_feature / track_id 更新 A
        4. 记录【整理】事件
        5. 从 disappeared_objects 中移除 A
        6. 从 appeared_objects 中移除 B


relocation_match 返回 low_confidence_relocation：
    说明：
        A 和 B 可能是整理关系，但证据不够强。

    常见原因：
        1. 只有 reid_match 外观相似，但没有 ByteTrack 移动轨迹
        2. 有手遮挡证据，但没有明确 A -> B 方向移动
        3. 有多个同类候选，无法唯一绑定

    处理方式：
        1. 不直接高置信判定【整理】
        2. 创建 pending_relocation(A, B)
        3. 从本轮 active disappeared_objects / appeared_objects 中暂时移除 A 和 B
        4. 将 A / B 标记为 pending_reserved
        5. 不提交【整理】事件
        6. 不提交【出库】事件
        7. 不提交【放入】事件
        8. 等下一份 stable_snapshot 再确认

    注意：
        A / B 必须从本轮 active 候选列表中移除，
        否则后续 10.3 / 10.4 会继续处理它们，
        导致同一组低置信整理被误拆成【出库】+【放入】。

        这里的“移除”不是最终消费。
        它只是本轮冻结，等待 pending_relocation 下一轮确认。

        PendingRelocation 只做轻量冻结。
        不要在这里引入复杂的二次 diff、复杂状态机或长期等待。
        如果下一份 stable_snapshot 仍不能支持 A -> B，
        就取消或回退普通逻辑。


pending_relocation 的下一轮确认规则：
    如果下一份 stable_snapshot 中：
        B 的位置持续稳定，
        A 仍未回到原位置，
        并且 B 与 A 的外观仍然匹配，
        则可以提升为 confirmed_relocation，
        或者根据证据强度继续保持 pending。

    如果 A 回到原位置：
        取消 pending_relocation。
        认为上一轮可能是短暂遮挡、YOLO 抖动或误关联。

    如果 B 消失：
        取消 pending_relocation。
        B 不作为稳定新物品处理。

    如果 pending_relocation 超过确认窗口仍无法确认：
        取消 pending_relocation，
        再回退到普通消失 / 新出现逻辑。

建议：
    pending_relocation_confirm_frames = 1 ~ 2 个稳定快照周期。


relocation_match 返回 no_relocation：
    说明：
        不能证明 A 被移动到了 B。

    处理方式：
        A 和 B 保留在原候选列表中，
        继续交给后续普通逻辑处理。


注意：
    reid_match 只是 relocation_match 的外观输入。
    不能因为 reid_match 成功就直接判定【整理】。

    OperationContext 中的 ByteTrack 轨迹、HELD、手轨迹，
    是把“看起来像”提升为“确实发生移动”的关键证据。


整理时还要处理遮挡和露出：
    检查 A 的新位置 B：
        如果 B 与某个原本【可见】库存物品 C 的 last_visible_bbox 重叠较大，
        并且 C 在 stable_snapshot 中消失，
        则认为 A 移动后挡住了 C：
            C 状态设为【遮挡】
            记录【遮挡】事件

    检查 A 的旧位置：
        如果某个原本【遮挡】物品 D 在 A 移开后重新出现，
        并且 D 与旧位置附近匹配，
        则认为 D 露出来：
            D 状态设为【可见】
            更新 D 的 bbox / 外观 / track_id
            记录【露出】事件



10.3 第二步：处理消失物品
---------------------------------------------------------------------

对剩余 disappeared_objects 中的每个物品 A：
    如果 A 已被 pending_relocation 标记为 pending_reserved：
        本轮跳过 A。
        不判【出库】，不判【遮挡】。
        等 pending_relocation 下一轮确认。

    1. 先检查 A 是否能匹配到本地库存。
        匹配不到：
            无事发生。
            可能是上一次快照中的误检。
            直接处理下一个 disappeared_object。

        匹配到，但库存状态是【遮挡】：
            无事发生。
            因为【遮挡】不能直接变成【出库】。
            直接处理下一个 disappeared_object。

        匹配到，且库存状态是【可见】：
            继续检查 A 原位置附近是否有 stable_snapshot 中的新物品 C。


    2. 只有在 A 匹配到库存且状态是【可见】时，才检查 A 附近的新物品 C。
        C 的候选范围：
            优先从剩余 appeared_objects 中找。
            C 必须出现在 A 原位置附近。
            C 不能是已经被整理步骤消耗掉的物品。

        不存在 C：
            A 被拿走。
            A 状态从【可见】变成【出库】。
            从本地库存清单删除 A。
            将 A 放入 out_stock_archive。
            记录【拿走】事件。

        存在 C，且 C 能匹配本地库存中的【遮挡】物品：
            说明 A 被拿走后，底下旧物品 C 露出来。
            A 状态从【可见】变成【出库】。
            C 状态从【遮挡】变成【可见】。
            更新 C 的 bbox / 外观 / track_id。
            从本地库存清单删除 A。
            将 A 放入 out_stock_archive。
            从 appeared_objects 中移除 C。
            记录【拿走】事件和【露出】事件。

        存在 C，且 C 能匹配本地库存中的【可见】物品：
            大概率是 YOLO 抖动或 bbox 变化。
            暂不修改库存。
            记录【忽略抖动】。

        存在 C，但 C 不在当前库存中：
            说明 C 可能是新放入的物品，并且 C 挡住了 A。
            A 状态从【可见】变成【遮挡】。
            A 仍然保留在本地库存清单中。
            记录【遮挡】事件。
            C 暂时留在 appeared_objects 中，交给第三步处理。


注意：
    这里仍然坚持：
        只有【可见】物品可以被判定为【出库】。



10.4 第三步：处理新出现物品
---------------------------------------------------------------------

对剩余 appeared_objects 中的每个物品 B：
    如果 B 已被 pending_relocation 标记为 pending_reserved：
        本轮跳过 B。
        不判【放入】，不判【放回】。
        等 pending_relocation 下一轮确认。

    如果 B 已被 pending_new_object 标记为 pending_reserved：
        本轮跳过 B。
        不判【放入】，不新建 item_id。
        等 pending_new_object 下一轮确认。

    1. 与本地库存做 strict_origin_match (原位严格匹配) 。
        匹配到【可见】物品：
            说明本来就在库存中。
            更新 bbox / 外观 / track_id。
            不新增库存。

        匹配到【遮挡】物品：
            说明原本被遮挡的旧物品露出来。
            状态从【遮挡】变成【可见】。
            更新 bbox / 外观 / track_id。
            记录【露出】事件。

    2. 如果没有匹配本地库存，则与 out_stock_archive 做 reid_match。
        匹配成功：
            说明物品被拿出去后又放回，或者之前误判出库。
            状态从【出库】变成【可见】。
            重新加入本地库存清单。
            从 out_stock_archive 中删除或标记恢复。
            记录【放回】事件。

    3. 如果本地库存和 out_stock_archive 都匹配失败，
       不要立刻判定 B 是全新物品。
       必须先检查 B 是否是“可疑新出现物品”。

        最重要的一条规则：
            正在整理物品 A 时，
            如果突然多出来一个与 A 同类的新物品 B，
            不要马上把 B 加入库存。
            等下一份 stable_snapshot 中 B 仍然独立存在时，
            才允许确认 B 是真正【放入】。

        B 进入 pending_new_object 的典型条件：
            1. 当前正在整理 / 移动某类物品 A，并且 B 与 A 同类
            2. 当前 OperationContext 中有同类物品被手接触、遮挡、HELD 或移动过
            3. 当前 OperationContext 中有同类 moved_item_candidate
            4. 当前轮同类数量突然增加，但没有明确的新物品放入证据
            5. B 出现在已有同类物品移动路径或手轨迹附近

        如果满足：
            创建 pending_new_object(B)
            将 B 标记为 pending_reserved
            本轮不提交【放入】事件
            本轮不新建库存 item_id

        原因：
            整理已有同类物品时，
            YOLO 可能在移动路径上短暂识别出一个额外同类物品。
            这个额外物品如果直接入库，
            会导致库存清单中出现重影 item_id。

    4. pending_new_object 的下一轮确认：
        如果 B 消失：
            取消 pending_new_object。
            不提交【放入】。

        如果 B 与已有同类库存物品高度重合或几乎同位置：
            取消 pending_new_object。
            更新已有库存物品，不新增 item_id。

        如果某个已有同类物品 A 的旧位置消失，
        并且 B 仍然稳定存在：
            优先转成 pending_relocation(A, B)，
            或者证据足够强时直接按【整理】处理。
            不直接新增 item_id。

        如果所有已有同类物品仍然稳定存在，
        并且 B 在下一份 stable_snapshot 中仍然稳定独立存在：
            确认 B 是真正新放入物品。
            新建库存记录。
            状态设为【可见】。
            记录【放入】事件。

            如果配置为更保守模式，
            可以要求 B 连续 2 份 stable_snapshot 都独立稳定存在后再 confirmed。

    5. 只有当 B 不满足 pending_new_object 条件，
       或 pending_new_object 已经 confirmed 时：
        说明 B 是全新物品。
        新建库存记录。
        状态设为【可见】。
        记录【放入】事件。


注意：
    PendingNewObject 主要防止“整理时额外冒出的同类误检”被立即写入库存。

    它不是为了阻止真正放入。
    真正新放入的物品只会多等 1 ~ 2 个稳定快照周期，
    但库存不会因为短暂误检而永久多一个 item_id。

    对不同类新物品，且没有同类操作上下文时，
    可以继续按普通【放入】快速处理。



10.5 第四步：例行清理
---------------------------------------------------------------------

每次快照差异处理完成后，做一次清理：
    1. 将 out_stock_archive 中的物品与本地库存做匹配。
        如果发现同一个 item_id 或高置信度同一物品已经在库存中：
            删除 out_stock_archive 中的冗余记录。

    2. 将【遮挡】物品与【可见】物品做匹配。
        如果发现某个【遮挡】物品其实已经以【可见】状态存在：
            删除或合并冗余的【遮挡】记录。

    3. 检查同类相似物品。
        如果多个候选都能匹配同一个库存物品：
            不要强行绑定 item_id。
            记录低置信度事件。
            优先保证库存数量正确。

    4. 清理 pending_new_object。
        如果候选 B 已消失：
            取消 pending_new_object。

        如果候选 B 已与某个已有库存物品合并：
            删除 pending_new_object。

        如果候选 B 超过确认窗口仍无法独立稳定：
            取消或继续低置信 pending，
            不要直接新增 item_id。

注意注意(我在提一下)：
    这几步处理逻辑，一定要按照这个顺序执行。
    顺序不能乱




十一、操作上下文与 ByteTrack 辅助逻辑
=====================================================================

原则：
    ByteTrack 每帧都运行。
    OperationContext 每帧都可以更新。
    但库存事件只在新的无手稳定 stable_snapshot 出现后统一裁决。

也就是说：
    看见手，不等于进入另一套“有手业务流程”。
    看见手，只是给当前 OperationContext 增加一条证据。

    普通拿走 / 放入：
        主要由 baseline_snapshot 和 stable_snapshot 的差异判断。

    整理 / 长时间遮挡 / 物体移动：
        在快照差异判断的基础上，
        重点参考 OperationContext 中的 ByteTrack、HELD 代理轨迹、移动轨迹证据。


ByteTrack 和 OperationContext 的作用：
    1. 记录手是否出现过
    2. 记录手是否长时间停留
    3. 记录手 bbox 与哪些可见物品发生过重叠
    4. 记录哪些物品在操作期间持续消失，形成 HELD 代理轨迹
    5. 记录物品 track 的移动轨迹，帮助判断整理
    6. 给 diff_snapshots 提供置信度加权证据



11.1 操作上下文的生命周期
---------------------------------------------------------------------

每一轮库存裁决的生命周期：
    1. 已有一份 baseline_snapshot
    2. 创建 OperationContext
    3. 持续读取视频帧
    4. 每帧运行 YOLO + ByteTrack
    5. OperationContext 持续收集证据
    6. 等待下一份无手稳定 stable_snapshot
    7. 统一执行：
        events = diff_snapshots(
            baseline_snapshot,
            stable_snapshot,
            operation_context
        )
    8. apply_events(events)
    9. baseline_snapshot = stable_snapshot
    10. 清空并重建 OperationContext


注意：
    OperationContext 的生命周期不是“手进来 -> 手出去”。
    它的生命周期是“上一份稳定快照 -> 下一份稳定快照”。

    因此：
        手短暂进出，不会改变主流程。
        手长时间停留，只会让 operation_context 中的证据更丰富。
        最终仍然只做一次 stable_snapshot 差异判断。



11.2 手出现时怎么处理
---------------------------------------------------------------------

当某一帧检测到 hand 类别：
    1. operation_context.hand_seen = true
    2. hand_frame_count 加 1
    3. 记录 hand_track_ids
    4. 当前帧不用于生成可参与库存对比的稳定快照
    5. ByteTrack 继续更新手和物品轨迹


如果手持续出现超过阈值：
    operation_context.hand_long_present = true

建议：
    hand_long_present_threshold = 10 ~ 15 帧


注意：
    手短暂出现，不需要单独判断拿走 / 放入。
    例如用户伸手拿走一个物品，手很快离开：
        最终仍然是 baseline_snapshot 与 stable_snapshot 对比后判定【拿走】。

    只有当手长时间遮挡、物体持续移动、track 轨迹明显变化时，
    OperationContext 才对【整理】和 HELD 代理轨迹判断产生较大影响。



11.3 HELD 代理轨迹怎么记录
---------------------------------------------------------------------

HELD 的核心含义：
    物品被手遮挡后，物品自己的 YOLO 检测和 ByteTrack 轨迹可能会断开。

    这时不直接判定物品被拿走。
    而是在 OperationContext 中记录：
        “这个物品暂时由某个 hand track 代理移动”。


识别到手的帧中，每一帧检查：
    如果手 bbox 与某个【可见】库存物品 bbox 重叠：
        将该物品加入 candidate_held_items

    如果 candidate_held_items 中的物品后续持续不可见：
        将该物品加入 confirmed_held_items
        创建 HeldProxyEvidence
        记录：
            item_id
            original_object_track_id
            held_by_hand_track_id
            last_visible_bbox
            object_to_hand_offset
            held_start_frame_id

    如果 candidate_held_items 中的物品后续重新稳定出现在原位置：
        从 candidate_held_items 中移除
        认为只是短暂遮挡

    如果 confirmed_held_items 中的物品仍然不可见，
    但对应的 hand track 继续移动：
        根据 hand track 更新 proxy_trajectory_points


注意：
    candidate_held_items、confirmed_held_items、HeldProxyEvidence 都只是辅助证据。
    它们不直接修改库存状态。

    不要把物品的 track_id 改成手的 track_id。
    正确做法是：
        物品 item_id 不变
        物品原 track_id 保留
        额外记录 held_by_hand_track_id
        用 proxy_trajectory_points 表示“物品被手代理移动”

    例如：
        A 是 confirmed_held，
        并且 stable_snapshot 中 A 消失，
        那么【拿走】置信度提高。

        A 是 confirmed_held，
        并且 HeldProxyEvidence 的 proxy_trajectory 到达 B 附近，
        同时 stable_snapshot 中 B 与 A 外观相似，
        那么 relocation_match(A, B) 更容易返回 confirmed_relocation。

        A 不是 HELD，
        但 stable_snapshot 中 A 消失，
        仍然可以按 10.3 的普通消失物品逻辑判断【拿走】或【遮挡】。



11.4 移动轨迹怎么辅助整理
---------------------------------------------------------------------

ByteTrack 每帧维护物品轨迹。

如果发现：
    1. 某个库存物品的 track 在一段时间内连续移动
    2. 移动前后类别一致
    3. 外观特征相似
    4. stable_snapshot 中有对应的新位置物品

则把它写入：
    operation_context.track_evidences
    operation_context.moved_item_candidates


在 10.2 “优先处理整理 / 移动”时：
    如果 disappeared_objects 中的 A
    与 appeared_objects 中的 B
    reid_match 外观分数较高，
    并且 operation_context.track_evidences 中也有 A -> B 的移动轨迹证据，
    则 relocation_match 更容易返回 confirmed_relocation。


如果没有 ByteTrack 轨迹证据：
    不能只靠 reid_match 高置信判定整理。

    最多得到 low_confidence_relocation。
    后续需要结合 HeldProxyEvidence、手轨迹、候选唯一性，
    或者回退到普通快照差异逻辑。

如果 ByteTrack 轨迹证据和 reid_match 冲突：
    不要强行判定整理。
    记录 low_confidence，必要时回退到普通快照差异逻辑。


ByteTrack ID 的使用原则：
    track_id 连续一致：
        是强加分项。

    track_id 断开、切换、不一致：
        不是直接否定项。
        因为手遮挡、物品重叠、YOLO 漏检都会让 ByteTrack 断轨或换 ID。

    track_id 明确冲突：
        是负证据。
        例如 A 的轨迹清楚停在原位置，而 B 是另一个早已存在的 track，
        这时不能高置信判定 A -> B 整理。



11.5 最终裁决仍然由快照差异完成
---------------------------------------------------------------------

无论中间有没有手，最终都只在这里裁决库存变化：

    events = diff_snapshots(
        baseline_snapshot,
        stable_snapshot,
        operation_context
    )


典型例子：

    普通拿走：
        baseline_snapshot：有苹果 A
        中间：手短暂出现，也可能没被 YOLO 识别到
        stable_snapshot：苹果 A 消失
        结果：按 10.3 判定 A【拿走】

    普通放入：
        baseline_snapshot：没有牛奶 B
        中间：手短暂出现，也可能没被 YOLO 识别到
        stable_snapshot：出现牛奶 B
        结果：按 10.4 判定 B【放入】

    整理：
        baseline_snapshot：苹果 A 在左边
        中间：手长时间停留，ByteTrack 记录 A 从左移动到右
        stable_snapshot：苹果 A 在右边
        结果：10.2 中 relocation_match 结合外观和移动证据，判定 A【整理】


关键规则：
    OperationContext 只影响事件置信度和归因。
    它不替代快照差异判断。
    同一个 stable_snapshot 只能被 diff_snapshots 消费一次。




十二、没有识别到手时的整理
=====================================================================

有些整理动作可能没有识别到手。

例如：
    1. 手被 YOLO 漏检
    2. 手只在画面边缘出现
    3. 物品被推动，但手没有进入主要画面


处理方式：
    ByteTrack 每帧仍然跟踪物品。
    OperationContext 仍然记录 moving_tracks / moved_item_candidates。

如果发现：
    1. 某个 track 的中心点连续移动
    2. 移动前后类别一致
    3. 外观相似
    4. 稳定快照中该物品从位置 X 到位置 Y

则判定为：
    【整理】


注意：
    没识别到手的整理，置信度低于“手 + HELD + 轨迹”共同确认的整理。
    但仍然比直接判成“出库 + 入库”更合理。

    这仍然不是另一套流程。
    最终还是在 diff_snapshots(baseline_snapshot, stable_snapshot, operation_context) 中裁决。




十三、主循环伪代码
=====================================================================

伪代码：

    operation_context = new OperationContext(baseline_snapshot)

    while door_is_open:

        frame = read_frame()
        detections = yolo.detect(frame)
        tracks = bytetrack.update(detections)

        frame_hand_present = has_hand(detections)

        update_operation_context(
            operation_context,
            detections,
            tracks,
            baseline_snapshot
        )

        if frame_hand_present:
            operation_context.hand_seen = true
            reset_snapshot_buffer()
            current_snapshot = null
            stable_snapshot = null
            continue

        snapshot_buffer.push(detections, tracks)

        if snapshot_buffer.full():
            current_snapshot = make_snapshot(snapshot_buffer)

            if current_snapshot.hand_present:
                operation_context.hand_seen = true
                current_snapshot = null
                stable_snapshot = null
                reset_snapshot_buffer()
                continue

            if contrast_snapshot is null:
                contrast_snapshot = current_snapshot
                current_snapshot = null
                reset_snapshot_buffer()
                continue

            if not snapshots_similar(contrast_snapshot, current_snapshot):
                contrast_snapshot = current_snapshot
                current_snapshot = null
                stable_snapshot = null
                reset_snapshot_buffer()
                continue

            stable_snapshot = current_snapshot

            if baseline_snapshot is not null:
                events = diff_snapshots(
                    baseline_snapshot,
                    stable_snapshot,
                    operation_context
                )
                apply_events(events)

            baseline_snapshot = stable_snapshot
            contrast_snapshot = stable_snapshot
            operation_context = new OperationContext(baseline_snapshot)
            current_snapshot = null
            stable_snapshot = null
            reset_snapshot_buffer()




十四、库存更新原则
=====================================================================

所有事件执行后，都要实时更新本地库存清单。
本地库存清单是后续身份匹配的依据。


更新原则：
    1. 【可见】和【遮挡】物品保留在本地库存中
    2. 【出库】物品从本地库存中删除
    3. 【出库】物品的信息保存在 out_stock_archive
    4. 新物品放入时，先经过 10.4 的 PendingNewObject 检查
    5. 只有 confirmed 的新物品，才添加到本地库存并分配正式 item_id
    6. 遮挡物品露出时，更新 bbox / 外观 / track_id
    7. 物品整理时，不新建 item_id，只更新原 item_id 的位置和外观


每次状态变化都打印调试信息：
    1. 当前事件类型
    2. 物品 item_id
    3. 类别
    4. 原状态
    5. 新状态
    6. 原 bbox
    7. 新 bbox
    8. 匹配方式
    9. 置信度
    10. 本地库存清单
    11. out_stock_archive




十五、容易出错的场景和处理方式
=====================================================================

15.1 同类相似物品
---------------------------------------------------------------------

例如：
    两瓶一样的牛奶
    两个苹果
    两盒一样的酸奶


问题：
    item_id 可能互换。


处理：
    1. 如果无法高置信度区分，不要强行绑定 item_id
    2. 优先保证类别数量正确
    3. 事件标记为 low_confidence
    4. 日志中记录多个候选 item_id
    5. relocation_match 候选不唯一时，不要高置信判定整理



15.2 YOLO 漏检
---------------------------------------------------------------------

处理：
    1. 使用 N 帧投票生成快照
    2. 【遮挡】不能直接出库
    3. 出库历史保留一段时间，允许恢复 item_id



15.3 YOLO 误检
---------------------------------------------------------------------

处理：
    1. 单帧误检不会进入稳定快照
    2. 初始化时发现后台没有的物品，不立即强制入库
    3. 新物品需要在稳定快照中存在
    4. 整理同类物品期间额外出现的新同类物品，先进入 PendingNewObject
    5. PendingNewObject confirmed 之前，不分配正式 item_id



15.4 物品被移动后挡住其他物品
---------------------------------------------------------------------

处理：
    1. 先用 relocation_match 判定整理
    2. 再检查新位置是否遮挡其他可见物品
    3. 被遮挡物品变成【遮挡】



15.5 物品被移开后露出底下物品
---------------------------------------------------------------------

处理：
    1. 检查旧位置附近是否出现库存中的【遮挡】物品
    2. 如果出现，改为【可见】
    3. 更新 bbox / 外观 / track_id



15.6 整理时额外冒出同类重影
---------------------------------------------------------------------

现象：
    库存中原本只有两个 apple。
    用户整理其中一个 apple 时，
    YOLO / 快照中突然多出第三个 apple。
    这个第三个 apple 可能只是移动过程中的临时位置或误检，
    但如果 10.4 直接把它当成【放入】，
    库存清单就会永久多出一个假的 apple item_id。

处理：
    1. 正在整理物品 A 时，突然多出来的同类物品 B，不要马上入库
    2. 先创建 pending_new_object(B)
    3. B 在 pending 期间不参与 relocation_match 的整理源
    4. 如果下一份 stable_snapshot 中 B 消失，则取消
    5. 如果 B 与已有 apple 高度重合或几乎同位置，则合并到已有 item，不新增
    6. 如果下一份 stable_snapshot 中 B 仍然独立存在，且所有已有同类物品也都稳定存在，才确认【放入】

原则：
    正在整理物品 A 时，
    突然多出来的与 A 同类的物品不要马上入库。
    下一份稳定快照中它还独立存在时，
    再确认它是真正新物品。




十六、推荐参数初始值
=====================================================================

第一版建议参数：
    snapshot_window_N = 3
    snapshot_vote_ratio = 0.6
    hand_long_present_threshold = 10 ~ 15 帧
    move_track_window = 5 ~ 10 帧
    move_center_threshold = 根据 bbox 对角线自适应
    pending_relocation_confirm_frames = 1 ~ 2 个稳定快照周期
    pending_new_object_confirm_frames = 1 ~ 2 个稳定快照周期
    new_item_probation_frames = 1 个稳定快照周期
    held_proxy_overlap_threshold = 0.3
    held_proxy_conflict_margin = 0.15

    nearby_threshold = 1.0

    cluster_iou_threshold = 0.2
    strict_iou_threshold = 0.5

    strict_center_threshold = 根据画面分辨率和 bbox 尺寸自适应
    cluster_center_threshold = strict_center_threshold 的 1.5 ~ 2 倍

    appearance_rgb_avg_diff_threshold = 20


调参建议：
    误判遮挡太多：
        降低 nearby_threshold，例如从 1.0 改成 0.7

    漏判遮挡太多：
        提高 nearby_threshold，例如从 1.0 改成 1.5

    快照太容易抖动：
        增大 N，例如从 3 改成 5

    响应太慢：
        减小 N，或者降低 move_track_window

    HELD 代理误关联太多：
        提高 held_proxy_overlap_threshold
        提高 held_proxy_conflict_margin

    低置信整理太容易被拆成出入库：
        增大 pending_relocation_confirm_frames

    整理时偶尔多出同类重影：
        启用 pending_new_object
        增大 pending_new_object_confirm_frames
        启用 new_item_probation_frames




十七、最终实现顺序建议
=====================================================================

建议按以下顺序实现：
    1. 基础数据结构
        InventoryItem
        SnapshotObject
        Snapshot
        OutStockArchive
        PendingNewObject

    2. 快照生成
        N 帧投票
        hand_present 特殊规则
        带手快照只丢弃，不进入库存对比
        snapshot_cluster_match

    3. 初始化
        后台库存 -> 本地库存
        初始快照 strict_origin_match
        后台只有类别 / 数量时使用 class_count_match
        可见 / 遮挡状态初始化

    4. 原位快照对比
        strict_origin_match
        可见 -> 出库
        可见 -> 遮挡
        遮挡 -> 可见
        新物品进入 PendingNewObject 或 confirmed 后放入

    5. out_stock_archive
        出库历史保留
        拿出又放回
        误出库恢复 item_id

    6. 整理识别
        reid_match 外观分数
        relocation_match 事件级整理判定
        pending_relocation
        pending_reserved 候选冻结
        ByteTrack 强移动证据
        OperationContext HELD / 手轨迹 / 移动证据
        候选唯一性检查
        位置更新

    7. 操作上下文
        OperationContext
        PendingRelocation
        PendingNewObject
        HeldProxyEvidence
        TrackEvidence
        hand_seen
        hand_long_present
        CANDIDATE_HELD
        HELD 代理轨迹
        HELD 代理冲突处理
        track_evidences
        held_proxy_evidences
        moving_tracks
        moved_item_candidates
        stable_snapshot 单次 diff 消费

    注意：
        PendingRelocation 是跨快照临时记录。
        PendingNewObject 也是跨快照临时记录。
        OperationContext 每轮 diff 后会重建，
        PendingRelocation / PendingNewObject 不能只存在 OperationContext 里。

    8. 调试日志
        每次状态变化打印库存
        每次事件打印匹配依据




十八、最终规则总结
=====================================================================

本版本最重要的规则：
    1. YOLO 不需要识别被压住的物品
    2. 【可见】和【遮挡】都算在库存中
    3. 【遮挡】不能直接变成【出库】
    4. 物品出库前必须先处于【可见】
    5. 快照对比只在无手且稳定时执行
    6. 带手快照只作为“操作中”信号，不能更新 baseline_snapshot / contrast_snapshot
    7. 不区分“有手主流程”和“无手主流程”
    8. 无论中间是否识别到手，最终都统一执行 diff_snapshots(baseline_snapshot, stable_snapshot, operation_context)
    9. OperationContext 只提供手、HELD 代理轨迹、ByteTrack 移动轨迹等辅助证据
    10. 普通拿走 / 放入主要靠稳定快照差异判断
    11. 整理要优先处理，但不能只靠 reid_match 判定
    12. 同一个 stable_snapshot 只能被消费一次
    13. reid_match 只负责外观重识别分数
    14. relocation_match = reid_match + OperationContext 移动证据 + 候选唯一性
    15. ByteTrack track_id 是强证据，但不是库存 item_id，不能当作绝对身份
    16. track_id 连续提高整理置信度，track_id 断开不直接否定，track_id 明确冲突会降权
    17. HELD 的主要作用是让被手遮挡后断轨的物品临时使用手轨迹作为代理轨迹
    18. 不要把物品 track_id 直接改成手 track_id，要在业务层记录 held_by_hand_track_id
    19. 同一只手代理多个物品时，只有最高置信候选可以 high_confidence，其余降级
    20. low_confidence_relocation 进入 pending_relocation，不立即拆成出库 + 入库
    21. pending_relocation 中的 A / B 必须 pending_reserved，避免本轮继续被 10.3 / 10.4 消费
    22. PendingRelocation 是跨快照临时记录，不能只放在 OperationContext 中
    23. PendingRelocation 必须保持轻量，只保存候选和冻结关系，不能变成第二套主流程
    24. 整理期间，与整理上下文相关的普通【拿走】/【放入】先冻结，不急着提交
    25. 这不是全局禁止出入库；与整理无关且证据清晰的操作仍可正常处理
    26. 整理期间额外冒出的同类新物品进入 pending_new_object，不立即分配 item_id
    27. PendingNewObject confirmed 前不能参与 relocation_match 的整理源
    28. PendingNewObject 是跨快照临时记录，不能只放在 OperationContext 中
    29. 本地库存清单实时更新，是后续身份匹配的依据
    30. 同类相似物品无法高置信区分时，优先保证数量正确


一句话总结：
    新业务流程6 保留“遮挡物品不直接出库”的简化前提，
    同时增加匹配分层、relocation_match 整理判定、PendingNewObject 待确认新物品、
    OperationContext 辅助证据和 stable_snapshot 单次消费规则，
    使流程更容易实现，也更不容易把整理误判成出入库。
