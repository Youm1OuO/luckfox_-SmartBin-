在目前这个业务流程的基础上进【二合一】：
    即：我决定明确这个代码还行，但是还不够好
    所以我但是用目前在业务流程的代码作为辅助，然后使用新的业务流程来作为主要的（新的业务流程我接下来会详细介绍）



思路：
    库存清单 + 手部动作识别 + 位置稳定性 + 空间分析



======================================================================================================
一、对比标准
======================================================================================================

身份匹配（严格）：用于判断"这个新检测到的东西，是不是库存里那个旧东西"
    三个条件全部满足才算"是同一个物品"：
    1. 中心位置：center_distance(A, B) < 15 像素（1280x720 画幅）
    2. 框大小：IoU(A, B) > 0.7，或面积比在 0.8~1.2 之间
    3. 像素颜色：两个 bbox 裁剪图 resize 到 16x16 后，计算平均 RGB 差异
       每通道差 < 20（0~255 范围），三通道取平均

    代码参考：
    // 身份匹配（严格）
    bool is_same_item(item A, item B) {
        return center_distance(A, B) < 15.0f
            && IoU(A, B) > 0.7f
            && color_diff(A.crop, B.crop) < 20.0f;
    }
    // 像素颜色对比（轻量）
    float color_diff(crop_a, crop_b) {
        将两个裁图 resize 到 16x16
        计算平均 RGB
        返回三通道差的平均值
    }


遮挡检测（宽松）：用于判断"这个消失的物品是不是被别人挡住了"
    只需检查空间重叠，不需要类别相同、颜色相同（因为遮挡物和被遮挡物本来就不是同一个东西）
    条件：消失物品的 last_bbox 与另一个物品的 current_bbox 的 overlap_ratio > 0.3

    代码参考：
    // 遮挡检测（宽松）
    bool might_be_occluded(item A, item B) {
        return overlap_ratio_of_smaller(A.bbox, B.bbox) > 0.3f;
    }


注意：这两套标准用途不同，不要混用！
    身份匹配回答的是"是不是同一个东西" -> 严格
    遮挡检测回答的是"有没有被别人挡住" -> 宽松



======================================================================================================
二、状态定义
======================================================================================================

给每个物品加 stable_frames 计数（连续被看到、存在的帧数）
物品有以下状态：
    【查收（原有物品）】-- 开门时从后台库存匹配到的物品，YOLO 能识别到
    【被遮挡】         -- 在后台库存中但 YOLO 识别不到（被其他物品压住了）
    【新物品】         -- 手操作后新出现的物品，且与库存中所有物品都不匹配
    【HELD】           -- 临时状态：物品消失且有手覆盖，等待确认是被拿走还是遮挡
    【被拿走】         -- 手离开后物品依然没出现，确认已离开冰箱



======================================================================================================
三、初始化（开门时）
======================================================================================================

开门的时候我们向后台申请要一份冰箱中的库存（假设这个库存中的物品全都是存在的）
并且在打开冰箱门时（在手还没进来时）我们先拍一张快照检测识别一下这些物品：
    - YOLO 识别到的物品 与 后台库存清单 做身份匹配（严格对比）
    - 匹配上 -> 【查收（原有物品）】
    - 没匹配上但在库存清单中 -> 【被遮挡】（YOLO 没识别到，大概率是被压住了）
注意：
    一开门时库存清单中所有物品都增加 stable_frames 计数
    （无论是否被 yolo 识别到，我们都给他的 stable_frames 计数增加，因为我们假设库存必然是对的，我们只是没识别到而已）



======================================================================================================
四、HELD 状态机（手在时的主逻辑）
======================================================================================================

当手进来的时候（yolo 识别到）：
    手进来，当手部遮住某个物品时，我们就假设这个物品是被手抓住了（只是假设）
    然后后续去一直检查这个物品是否仍然被手抓住了
    如果这个物品只是凑巧被手挡住的话，他肯定迟早会被再次识别到，如果这个物品被手挡之前与之后的位置没有变的话，就说明这个物品并没有被手抓住，只是凑巧挡住而已

简单来说：
    手进来 -> 物品消失 -> 先标 HELD（不判拿走）
    手离开画面 -> 物品依然没出现 -> 判【拿走】
    手离开画面 -> 物品重新出现 -> 取消 HELD，没被拿走

注意：手在画面中时，拿走/放入/整理的判断优先用这个 HELD 状态机。
      当手不在 / 手识别不到时，走下面的快照兜底逻辑。



======================================================================================================
五、具体操作判断
======================================================================================================

------------------------------------------------------------------------------------------------------
【放入】（主逻辑：手驱动 + 快照兜底）
------------------------------------------------------------------------------------------------------
    手进来，导致有新的物品被 yolo 识别到，判断顺序如下：
    1) 先跟"刚消失的物品"做身份匹配（严格对比） -> 匹配上 = 旧物品挪位/整理，不是新物品
    2) 都匹配不上 -> 检查有没有现有物品因此被遮挡了（见下方遮挡处理）
    3) 确认入库为【新物品】

【放入】时处理【被挡住】的问题：
    物品被【放入】之后，很有可能会导致原来位置的物品被挡住（也有可能没有挡）
    如果一个物品一直都存在与冰箱中（即一直都被识别到），然后突然识别不到了，
    并且这个物品的 last_bbox 有空间重叠（overlap_ratio > 0.3）的新物品出现，
    那么就说明这个物品被遮挡了 -> 变成【被遮挡】状态



------------------------------------------------------------------------------------------------------
【拿走】（快照兜底 -- 手不在 / 手识别不到时使用）
------------------------------------------------------------------------------------------------------
    手进来，导致有旧物品无法被 yolo 识别到，这个物品与库存中所有的物品做身份匹配（严格对比），
    匹配上 -> 认为这个物品是【被拿走】
    并且没有新的物品被识别到

    注意：当手在画面中时，拿走的判断优先用 HELD 状态机（见第四节），
          这里是手不在时的兜底逻辑。

【拿走】时处理【被挡住】的问题：
    旧物品被拿走之后，很有可能会导致原来位置被遮挡的物品变得没有被挡住（也有可能没有）
    对这个突然识别到的物品与库存中【被遮挡】状态的物品做身份匹配（严格对比）
    匹配上 + 原来是【被遮挡】状态 -> 解除【被遮挡】



------------------------------------------------------------------------------------------------------
【整理】（保留原有代码逻辑）
------------------------------------------------------------------------------------------------------
    整理操作保留原有的 HELD + 平滑移动检测逻辑

【整理】时处理【被挡住】的问题：
    整理 = 放入 + 拿走同时发生，遮挡处理按【放入】和【拿走】的遮挡逻辑同时执行

    即：物品 A 从位置 X 被移到位置 Y
      ->
    检查位置 Y：有没有其他物品的 last_bbox 与 A 的 new_bbox 有空间重叠（overlap_ratio > 0.3），
              且该物品现在消失了？
      -> 有 = A 挡住了它 -> 被挡物品变成【被遮挡】
      ->
    检查位置 X：有没有之前【被遮挡】状态的物品，现在重新出现了？
      -> 有 = A 挪走后它露出来了 -> 解除【被遮挡】
